# CLAUDE.md — PGD (Plateforme de Gestion des Dégrèvements)

Contexte permanent du dépôt. À lire avant toute intervention.

---

## Projet

Plateforme de gestion des dégrèvements pour **Orange Côte d'Ivoire** (DSI / AIP — Études & Ingénierie IT). Architecte SI : Aroun KONÉ. Méthode **BMAD**.

Elle dématérialise le cycle : **saisie → routage automatique → validation multi-niveaux → restitution SI → contrôle a posteriori → archivage**, sur trois circuits :

| Circuit | Périmètre | Code process |
|---|---|---|
| **DOBB** | B2B | `PO2_B-17` |
| **DXC** | B2C | `PO5-G-07` |
| **DF** | Wholesale / Opérateurs | `PO6-07` |

---

## Stack

**Monorepo** pnpm workspaces + Turborepo · TypeScript strict partout
**Backend** NestJS · Prisma · PostgreSQL 16 · Redis (cache + verrous) · RabbitMQ (bus de messages, `@golevelup/nestjs-rabbitmq` ou `amqplib`)
**Frontend** Next.js (App Router) · React · Tailwind
**Sécurité** Active Directory (LDAP/LDAPS) · MFA Cisco DUO **ou** TOTP · JWT + session Redis
**Infra** Docker Compose · migrations Prisma versionnées

---

## Structure

```
apps/api        NestJS — API REST
apps/worker     NestJS standalone — consumers AMQP (RabbitMQ)
apps/web        Next.js — interface
packages/contracts   Zod + types — SOURCE UNIQUE des contrats
packages/database    Prisma schema, migrations, seeds
packages/config      validation Zod de l'environnement
packages/messaging   topologie RabbitMQ, connexion, publisher, retry/DLX — utilisé par apps/worker
packages/ui          design system
docs/                livrables BMAD 00 → 07
```

**Règle de dépendance :** `apps/*` dépendent de `packages/*`. `packages/*` n'importent **jamais** `apps/*`. Aucune dépendance croisée entre `apps/`.

---

## Règles non négociables

1. **Aucune règle métier codée en dur.** Matrice pivot, catalogue de rôles, motifs, taux, calendrier SLA, paliers de subdélégation : tous en base, cachés en Redis, invalidés à l'écriture. Un seuil écrit dans le code est un bug.
2. **Invariants de sécurité côté serveur uniquement.** RBAC, SoD, verrous : Guards NestJS et transactions Prisma. Un contrôle côté client est un confort, jamais une garantie. **Ceci vaut aussi en lecture, pas seulement en écriture.** Un `@Roles()`/`@Authenticated()` au niveau route répond à « cet utilisateur est-il authentifié / détient-il ce rôle ? », jamais à « a-t-il le droit d'accéder à CE périmètre précis ? » — les deux questions sont distinctes, et confondre la première pour la seconde a produit la même classe de faille sept fois de suite en Phase 8 (guards de portée manquants sur les routes de tâches/délégation/demandes) puis une huitième fois sur une lecture agrégée (`profil` de `GET /api/kpi`, non vérifié contre l'identité réelle de l'appelant). Toute nouvelle route — lecture ou écriture — doit répondre explicitement à la question de portée, pas seulement à celle d'authentification.

   **Contremesure structurelle à la récidive** : chaque correctif ci-dessus a été vérifié individuellement au moment où il a été écrit, mais aucune suite ne les rejouait ensemble — `guard-order.spec.ts` ne verrouillait que l'ORDRE des guards sur trois routes, pas leur PRÉSENCE sur les autres. `apps/api/test/guard-coverage.spec.ts` (Phase 8, post-8.5) lit les métadonnées de guards (comme `guard-order.spec.ts`) contre une **table explicite route → guards attendus** pour `DemandesController`, `TachesController` et `KpiController`, et échoue dans les deux sens : un guard attendu disparaît d'une route déjà répertoriée, **ou** une nouvelle route d'écriture (ou une nouvelle route de `KpiController`) apparaît sans entrée dans la table. Ce second sens compte autant que le premier — sans lui, une route ajoutée en Phase 9/10 sans garde de portée passerait silencieusement. **Toute nouvelle route mutative ou toute nouvelle route de `KpiController` doit gagner une entrée dans une des trois tables de ce fichier, dans le même changement qui l'introduit.**
3. **Journaux append-only.** Aucun `UPDATE` ni `DELETE` applicatif sur `JOURNAL_AUDIT` et `JOURNAL_SECURITE`. Rétention 10 ans (SOX).
4. **PostgreSQL est la source de vérité.** Redis est un reflet volatil intégralement reconstructible.
5. **Affectation par corbeille (pull).** Jamais de désignation nominative en routage nominal ; la délégation est l'exception tracée.
6. **Français** pour tout le domaine : entités, colonnes, énumérations, libellés, messages d'erreur utilisateur.
7. **Montants** en XOF, `numeric(15,2)` (jamais de flottant), plancher 0. Le **routage s'appuie sur le TTC**.
8. **SLA en heures ouvrées** via `CALENDRIER_SLA` (jours ouvrés, plage horaire, fériés).
9. **`packages/contracts` est la source unique des types.** Pas de DTO dupliqué entre back et front.

---

## Règles métier (R1 → R24)

| Réf. | Règle |
|---|---|
| `R1` | Routage sur le montant TTC |
| `R2` | Au-delà des seuils : terminaison DF puis DGA/DG |
| `R3` | **SoD** — un agent ne valide pas l'étape N s'il est intervenu à l'étape N-1 du même dossier |
| `R4` | Une tâche n'est traitée que par un membre du rôle de sa corbeille |
| `R5` | Toute transition d'état est journalisée |
| `R6` | Re-routage après modification selon les paliers en vigueur |
| `R7` | Claim atomique — conflit → `409` |
| `R8` | Plancher des montants à 0 |
| `R9` | SLA en heures ouvrées |
| `R10` | La validation finale déclenche la restitution SI |
| `R11` | Aucune règle métier en dur |
| `R12` | Contrôle **FRA obligatoire** au-delà de 5 000 000 FCFA |
| `R13` | Pièces obligatoires du motif présentes à la soumission |
| `R14` | **Commentaire obligatoire** à la soumission |
| `R15` | Ligne `RESILIE` → bloquée ou justification renforcée (`PARAMETRE_GLOBAL`) |
| `R16` | Restitution SI **idempotente** |
| `R17` | Une **formule** sélectionnée par ligne retenue |
| `R18` | Le TTC agrège les lignes retenues |
| `R19` | Une seule formule courante par ligne |
| `R20` | `statut_ligne` figé dans `DEMANDE_LIGNE` à la sélection |
| `R21` | **SoD étendu à la délégation** — vérifier délégataire **et** délégant (`DELEGATION.delegant_id`) sur l'étape N-1 |
| `R22` | Pas de deux délégations actives concurrentes sur le même `(delegant_id, role_code)` avec périodes recouvrantes — **contrainte `EXCLUDE` GIST en base**, pas seulement en service |
| `R23` | `HISTORIQUE_MONTANT` porte `acteur_id` et `demande_ligne_id` (tous deux nullables) — `SF-PGD-042`. `CHECK (acteur_id IS NOT NULL OR origine = 'RECALCUL')` : **implication, pas équivalence** — un recalcul peut avoir un acteur |
| `R24` | **SoD étendu au contrôle a posteriori** — un acteur ne réalise pas le contrôle de l'étape N (`typeActeur = 'C'`, ex. `FRA`) s'il est intervenu à l'étape bloquante N-1 du même dossier — même mécanisme que R3, `SodService.verifier` ne distingue jamais `typeActeur` (trouvé en essai de bout en bout, Phase 9, verrouillé par `sod-service.integration.spec.ts`, describe `R24`) |
| `R25` | **Traçabilité de la saisie manuelle TSC/TVA** — toute écriture via `PATCH /api/demandes/{id}/taxes` (bascule `tscManuelle`/`tvaManuelle`, `montantTscManuel`/`montantTvaManuel`, `assietteTva`) enregistre une entrée `HISTORIQUE_MONTANT` avec `origine='MODIFICATION'` et `acteurId` = l'acteur réel de la session — extension explicite du principe de traçabilité déjà posé par `R23`, pas une règle nouvelle de nature différente |

---

## Mécanismes structurants

### Claim à double verrou
```
1. Redis  : SET lock:tache:{id} {userId} NX PX {ttl}     → absorbe la contention
2. Postgres (transaction, source de vérité) :
     UPDATE tache SET etat='RECLAMEE', agent_claim=…, date_claim=now(),
            verrou_expire_at = now() + …
     WHERE id=:id AND etat='EN_CORBEILLE' RETURNING id;
3. Aucune ligne → 409 Conflict + libération du verrou Redis
```

Cycle : `EN_ATTENTE → EN_CORBEILLE → RECLAMEE → APPROUVEE | REJETEE` (+ `POST_CLOTURE` pour le contrôle).

### Configurabilité complète (principe de conception)

**La plateforme doit rester complètement configurable — l'ajout de motifs, libellés, processus, étapes de workflow, ou tout autre référentiel doit se faire par la donnée (admin CRUD), jamais par une modification de code.** Ce n'est pas un principe nouveau : c'est la formalisation explicite de la règle non négociable 1 (« Aucune règle métier codée en dur… Un seuil écrit dans le code est un bug ») et de `R11` (« Aucune règle métier en dur »), déjà en vigueur depuis la Phase 1. Rappelé ici comme principe de conception à part entière parce qu'il s'applique aussi à du contenu qui ne ressemble pas à première vue à une « règle métier » — une liste d'options de formulaire (ex. `LIBELLE`) codée en dur dans un composant React est le même bug qu'un seuil ou un taux codé en dur dans un service, même si la conséquence semble plus bénigne.

### Mécanismes d'interaction contraignants (principe de conception, posé Phase 10.6septies poursuite)

**Les mécanismes d'interaction validés par la maquette (déclenchement automatique vs bouton manuel) sont contraignants au même titre que le design visuel.** Une divergence n'est légitime que si le mécanisme automatique impliquerait d'afficher un contrôle actif pour une capacité qui n'existe pas côté serveur — jamais pour une simple prudence d'implémentation. Si un comportement automatique pose un risque technique réel (ex. écriture d'audit à chaque interaction), la réponse est un ajustement technique qui préserve le comportement visible attendu (debounce, throttle), jamais l'ajout d'un contrôle manuel absent de la maquette.

Posé après avoir trouvé, dans le panneau « Calcul automatique » (`NouvelleDemandeScreen`), deux boutons manuels (« Prévisualiser », « Enregistrer les taxes ») ajoutés par prudence d'implémentation là où la maquette (`screens1.jsx`) ne montre aucun bouton — les deux panneaux s'y recalculent en continu, sans action explicite. Le risque réel identifié (une écriture `HISTORIQUE_MONTANT`, R25, à chaque clic/frappe si le panneau Taxes devenait lui aussi purement automatique) a été traité par un **debounce côté client** (persistance serveur différée et regroupée après une pause dans la saisie), pas en ajoutant un bouton — l'aperçu affiché, lui, reste instantané et purement local. Même principe déjà appliqué à `ApercuRoutage` (auto-refresh après toute sauvegarde pertinente, y compris le tout premier affichage — plus de bouton « Prévisualiser » du tout).

### Moteur de règles pivot
Cinq variables — `circuit`, `segment`, `sous_flux`, `montant TTC`, `type_acteur` — produisent une chaîne ordonnée de tâches. Format de règle :
```
RÈGLE = { circuit, segment, sous_flux, borne_min, borne_max, étapes[] }
```
Lecture en cache Redis, rechargement PostgreSQL si absent. Toute écriture d'administration invalide la clé.

### Convention R12 — contrôle FRA (à ne pas casser)

`EtapeRegle` n'a pas de champ dédié pour distinguer un contrôle FRA d'un contrôle générique N1/N2 (`typeActeur` reste seulement `V`/`A`/`C`, non glosé — cf. Questions ouvertes). R12 (« TTC > 5M exige un contrôle FRA ») repose donc sur une convention purement par donnée, posée en Phase 5 faute d'un champ de schéma dédié :

> Un contrôle FRA = une `EtapeRegle` avec `roleCode = 'FRA'` **et** `typeActeur = 'C'` (`RuleEngineService.ROLE_CODE_FRA`, `possedeControleFra()`).

**Fragilité assumée** : le catalogue de rôles est à 25/34 et sera corrigé. Si le rôle `FRA` est renommé ou supprimé sans mettre à jour cette convention, R12 ne lève **aucune erreur** — une chaîne sans étape `FRA` se lit silencieusement comme « aucun contrôle requis ». Le test `rule-engine.integration.spec.ts` (« le rôle FRA existe dans le référentiel des rôles ») est le seul garde-fou : il échoue en CI si `ROLE_CODE_FRA` disparaît du référentiel. **Ne pas renommer/supprimer le rôle `FRA` sans mettre à jour `RuleEngineService` et ce test dans le même changement.**

### Panneau « Taxes appliquées » — assiette TVA configurable + saisie manuelle (R25, Phase 10.6septies)

Confirmation métier (`docs/09_Specifications_Fonctionnelles_PROD_v3.md` §7, `docs/10_Remarques_Metier_Maquette_240626.txt`, remarques DOBB #1/#2/#6) — `MontantService.calculer()` n'implémentait jusqu'ici qu'une seule formule en dur (cascade `(HT+TSC)×tauxTva`, jamais autre chose). Devenu configurable, à deux niveaux :

- **Par circuit** (`ParametreCalcul.assietteTvaDefaut`) : défaut appliqué à toute nouvelle demande de ce circuit, modifiable en admin (mêmes garde-fous que `tauxTsc`/`tauxTva` — `AdminParametresCalculService.recalculerBrouillons` recalcule les brouillons, gèle les demandes déjà `SOUMIS`).
- **Par dossier** (`Demande.assietteTva`, `tscManuelle`/`montantTscManuel`, `tvaManuelle`/`montantTvaManuel`) : portée dossier entier (HT agrégé), jamais par ligne. Une saisie manuelle « remplace le calcul automatique pour ce dossier » — elle survit à un recalcul déclenché par un changement de ligne, elle n'est jamais silencieusement réécrasée.

`PATCH /api/demandes/{id}/taxes` (`DemandeWorkflowService.modifierTaxes`, `InitiateurDemandeGuard`) : recalcule via `MontantService.calculer`, écrit une entrée `HISTORIQUE_MONTANT` (`origine='MODIFICATION'`, `acteurId` réel — **R25**), puis **réutilise le mécanisme de re-routage R6** (`reRouterSiEngage`, extrait de `modifierAvecReRoutage`) si le dossier est déjà engagé dans un circuit : un changement de taxes change le TTC, donc peut faire franchir un seuil de palier ou le plafond R12 — exactement le même risque de contournement que `modifierAvecReRoutage` traite déjà pour les lignes. Vérifié en lisant `modifierAvecReRoutage` avant d'écrire `modifierTaxes`, pas supposé : le mécanisme existant (supprimer les tâches non décidées → resélectionner le palier sur le TTC courant → revérifier R12 → réinstancier la chaîne → remettre `etapeCourante` à zéro → `journalAudit` « re-routage ») s'applique tel quel, aucune divergence entre les deux déclencheurs (lignes vs taxes) qui justifierait un mécanisme différent.

**Défaut de schéma trouvé et corrigé pendant le sweep de ce chantier, pas laissé en l'état** : `EnumAssietteTva` a deux valeurs, `HT` (nouvelle règle) et `HT_TSC` (ancienne règle, cascade — la seule jamais implémentée avant ce chantier). Le schéma avait été écrit avec `@default(HT)` sur les deux colonnes (`ParametreCalcul.assietteTvaDefaut` et `Demande.assietteTva`) — un défaut qui aurait silencieusement fait basculer tous les circuits existants (aucun seed n'écrit `assietteTvaDefaut` explicitement) vers la **nouvelle** formule dès l'application de la migration, sans qu'aucune décision métier ne l'ait demandé. Détecté par un test préexistant (`admin-parametres-calcul.integration.spec.ts`, assertion sur un TTC calculé en cascade HT+TSC) qui a échoué pendant le sweep — pas anticipé, trouvé en vérifiant que le sweep est vraiment vert plutôt qu'en le supposant. Corrigé : défaut `HT_TSC` sur les deux colonnes (schéma, `migration.sql`, et rows déjà écrites en base de dev par une exécution antérieure de la même migration, corrigées par `UPDATE` explicite). Le sweep complet (`typecheck`/`lint`/`build`/`test`, 211/211) est vert seulement après cette correction.

**Frontend — `dateDemande` éditable + `creeLe` en lecture seule** (`NouvelleDemandeScreen.tsx`) : champ « Date de demande » pré-rempli à la date du jour (fuseau local, pas `toISOString().slice(0,10)` qui bascule sur UTC), modifiable tant qu'aucun dossier n'existe, envoyé dans `creerDemande`. « Date de saisie » (`creeLe`) affichée seulement une fois le dossier créé, jamais éditable.

**Panneau « Taxes appliquées » devenu interactif** — remplace le panneau lecture-seule d'avant ce chantier. Avant création du dossier : affiche les défauts du circuit (`ParametreCalcul`, y compris `assietteTvaDefaut`). Une fois le dossier créé : toggles TSC/TVA actif, sélecteur d'assiette (visible seulement si TSC **et** TVA actifs — sans TSC actif, HT+TSC coïnciderait avec HT seul), bascules « Saisir manuellement » + champ montant pour TSC/TVA, aperçu client recalculé à chaque frappe (formule vérifiée contre `MontantService.calculer()` avant d'être portée, comme le panneau admin), bouton « Enregistrer les taxes » câblé sur `PATCH /api/demandes/{id}/taxes`.

**Mécanisme « Prévisualiser » (`ApercuRoutage`) — onBlur + auto-rafraîchissement, bouton manuel conservé** (décision explicite utilisateur) : un compteur opaque (`apercuDeclencheur`, `NouvelleDemandeScreen`) s'incrémente à chaque sauvegarde réussie qui touche un champ pertinent au routage — `handleEnregistrerLignes` (bouton **et** `onBlur` du champ Montant HT d'une ligne, uniquement si le dossier existe déjà — avant, rien à sauvegarder implicitement, cf. le risque de brouillons orphelins déjà écarté ailleurs dans ce fichier) et `handleEnregistrerTaxes`. `ApercuRoutage` relance `previsualiser()` à chaque changement de ce compteur, sauf au tout premier montage du panneau (le bouton manuel reste nécessaire pour ce premier affichage).

**Bug trouvé en vérification live, pas en lecture de code — React StrictMode (`next.config.js`, `reactStrictMode: true`) double-invoque les effects au montage en dev.** Le premier garde-fou écrit (`useRef(true)` + `if (premierRendu.current) { premierRendu.current = false; return; }`) se faisait piéger par ce double appel : la première invocation de l'effet le désactivait, la **seconde** (StrictMode, toujours pendant le même montage réel) le laissait passer et appelait `previsualiser()` — l'aperçu de routage apparaissait donc automatiquement dès le premier affichage du panneau, sans aucune action utilisateur, contredisant directement la décision actée. Vérifié par script Playwright authentifié (session réelle jean.kouassi, `POST /api/demandes` → `PUT .../lignes` → lecture du DOM) : `APERCU_AUTO_AU_PREMIER_MONTAGE=true` avant correctif. **Corrigé** en remplaçant le booléen par une comparaison à la dernière valeur vue (`const dernierVu = useRef(declencheur)`, effet : `if (declencheur === dernierVu.current) return;`) — `useRef` ne réinitialise sa valeur qu'une fois, y compris à travers les deux passes de StrictMode, donc le premier effet (quel que soit le nombre de fois où StrictMode le rejoue) voit toujours `declencheur === dernierVu.current` et ne déclenche rien. Revérifié après correctif, dans les deux sens : `APERCU_AUTO_AU_PREMIER_MONTAGE=false`, `APERCU_APRES_CLIC_MANUEL=true`, et — via écoute réseau Playwright plutôt que simple lecture du DOM — exactement 0 appel `apercu-routage` après création, 1 après le clic manuel, 2 après une sauvegarde de taxes **sans reclic** (auto-rafraîchissement confirmé). **Généralisable** : tout composant qui distingue « premier montage » d'un « changement ultérieur » via un booléen simple, dans une app avec `reactStrictMode: true`, porte le même risque — la comparaison à la dernière valeur vue est le pattern robuste, pas le booléen.

**R25 vérifiée en direct, pas seulement testée unitairement** : dossier DOBB réel créé (session jean.kouassi), taxes modifiées deux fois (assiette, saisie manuelle TVA), `HISTORIQUE_MONTANT` lu directement en base — chaque écriture porte `origine='MODIFICATION'` et `acteur_id` résolu vers `jean.kouassi@orange.ci` (jointure `utilisateur`), jamais `NULL`. Trois dossiers de vérification nettoyés ensuite via de vrais appels `DELETE /api/demandes/{id}` (statut `BROUILLON`, jamais soumis).

**Défaut `assietteTva`/`assietteTvaDefaut` — bascule confirmée le 2026-08-11, `HT` devient le défaut sur les trois circuits.** Historique de la question (à conserver pour comprendre pourquoi ce n'était pas un simple flip de valeur) : `docs/design/screens1.jsx:139/503/507` et `docs/design/engine.jsx:60` défaultent tous les deux à `"ht"`, `docs/09_Specifications_Fonctionnelles_PROD_v3.md` §7 (SF-PGD-040) marque la formule HT seul 🟢 « implémenté… à porter à l'identique », `docs/10_Remarques_Metier_Maquette_240626.txt` (remarque DOBB #6) qualifie HT+TSC d'« ancien calcul » — trois sources convergentes, mais **délibérément pas suffisantes** pour changer un défaut financier sans décision métier distincte et explicite (raisonnement posé en revue de clôture, `HT_TSC` gardé à ce moment précisément pour ne pas déduire une décision financière d'un texte de spec, même clair). Cette confirmation est arrivée explicitement, transmise directement par la personne pilotant le projet (pas déduite d'une relecture de doc) : le défaut passe de `HT_TSC` à `HT` pour `ParametreCalcul.assietteTvaDefaut` (DOBB/DXC/DF) et `Demande.assietteTva`, tout nouveau dossier créé à partir de maintenant applique la nouvelle règle (TVA sur HT seul) sauf bascule manuelle vers « Ancienne règle » — les deux options restent sélectionnables sans restriction (DOBB a explicitement demandé de garder l'ancien calcul disponible). Un dossier déjà `SOUMIS` garde son assiette figée (même gel que `tauxTsc`/`tauxTva`, `AdminParametresCalculService.modifier`) ; un `BROUILLON` suit le nouveau défaut et se recalcule via le même mécanisme `recalculerBrouillons`/`HISTORIQUE_MONTANT` (`origine=RECALCUL`) déjà construit pour les changements de taux — vérifié en lisant le code avant d'agir, pas supposé : `tauxChange` (le déclencheur du recalcul) inclut déjà `dto.assietteTvaDefaut !== undefined` depuis Phase 10.6septies, aucun mécanisme séparé à construire. Migration : `schema.prisma` + `migration.sql` changent le `@default` des deux colonnes (nouvelles installations/seed) ; les 3 lignes `ParametreCalcul` déjà existantes en base sont basculées via le vrai endpoint admin (`PATCH /api/admin/parametres-calcul/{circuit}`), pas par `UPDATE` SQL direct — un `UPDATE` brut aurait contourné `recalculerBrouillons`/la traçabilité `HISTORIQUE_MONTANT`, contrairement au correctif de bug de Phase 10.6septies (aucun brouillon réel affecté à l'époque, un `UPDATE` direct était sans risque ; ici la même méthode aurait silencieusement laissé les brouillons existants sur l'ancienne assiette).

**Bug de saisie manuelle trouvé dans le même inventaire, corrigé — nature différente de la question ci-dessus, aucune ambiguïté métier.** `MontantService.calculer()` appliquait une valeur manuelle (`tscManuelle`/`tvaManuelle`) même si la taxe correspondante était inactive (`tscActive`/`tvaActive` à `false`) — vérifié contre `docs/design/screens1.jsx:158-159` (`tscManu = f.applyTsc && f.tscManuelle`), qui subordonne toujours la saisie manuelle à l'interrupteur actif. Concrètement : désactiver la TSC avec une saisie manuelle encore renseignée dans le champ ne ramenait PAS la TSC à 0, contrairement à ce que l'interrupteur affiche. **Corrigé** : `calculer()` vérifie désormais `tscActive`/`tvaActive` en premier — taxe inactive → 0, sans jamais consulter la valeur manuelle. Verrouillé par `montant.service.spec.ts` (describe dédié « saisie manuelle subordonnée à la taxe active », taxe inactive + valeur manuelle renseignée → 0 dans les deux sens TSC/TVA, plus un cas de non-régression confirmant que la saisie manuelle continue de s'appliquer quand la taxe est active). Frontend (`NouvelleDemandeScreen.tsx`, `previsualiserTaxes`) aligné sur la même logique, plus un message explicite (« TSC/TVA inactive — une saisie manuelle resterait sans effet ») affiché quand une saisie manuelle est configurée sur une taxe désactivée.

**Panneau Taxes — fidélité UI corrigée après inventaire champ par champ contre la capture réelle** (`docs/design/screens1.jsx:484-537`, le panneau qui a motivé ce chantier) : libellés du sélecteur d'assiette alignés mot pour mot (« Nouvelle règle — TVA sur le montant HT » / « Ancienne règle — TVA sur HT + TSC », remplaçant « HT seul »/« HT + TSC (cascade)) »), ligne intermédiaire « HT + TSC » ajoutée (visible seulement quand TSC et TVA actives, assiette `HT_TSC`, et TVA non manuelle — même condition que `f.applyTsc && f.applyTva && f.tvaBase === "htTsc"` dans la maquette), suffixe « · sur HT+TSC / sur HT / saisie manuelle » ajouté à la ligne TVA. Appliqué aux deux écrans qui reproduisent cette formule (`NouvelleDemandeScreen` et `ParametresCalculAdminTab`) pour rester cohérents entre eux, bien que `ParametresCalculAdminTab` n'ait pas de source maquette directe pour ce choix précis (`CalcConfigView`, `screens3.jsx`, n'a qu'une bascule d'affichage `afficherHtPlusTsc`, pas un choix d'assiette — `assietteTvaDefaut` est une capacité réelle construite au-delà de ce que montre cet écran précis).

### Pré-enregistrement des utilisateurs AD (analyse + Temps 1 + Temps 2, 12/08/2026)

Demande métier significative, analysée avant tout code : un utilisateur avec des identifiants LDAP valides mais aucun accès pré-provisionné ne doit jamais obtenir de session. Séquencée en deux temps délibérément séparés — **Temps 1** (additif, modèle de données + recherche LDAP + écran admin) et **Temps 2** (retrait du JIT, changement du flux de connexion) — tous deux clos.

**Analyse préalable, vérifiée avant tout code, pas supposée** :
- **Comportement actuel (avant Temps 2)** : `AuthController.login()` appelle `RbacResolutionService.resoudre()`, qui fait un `upsert` sur `Utilisateur` par `identifiantAd` (provisionnement JIT, SF-PGD-007) puis dérive les rôles **uniquement** des groupes AD (`Role.groupeAd`) — sans aucune vérification qu'une ligne `MembreRole` préexistait. Un utilisateur inconnu avec des identifiants LDAP valides mais des groupes AD ne correspondant à aucun rôle obtient donc aujourd'hui une **session pleinement valide, avec `roles: []`**, silencieusement — `AuthGuard` ne vérifie que la validité du JWT, `RbacGuard` ne bloque que les routes portant `@Roles()`. Ce comportement change en Temps 2, pas ici.
- **`LdapPort` ne savait pas rechercher** — seulement `authentifier(identifiantAd, motDePasse)` (bind complet) et une résolution de DN interne par égalité exacte (`mail=<identifiantAd>`), jamais exposée, jamais un filtre partiel. Capacité ajoutée en Temps 1 (`LdapPort.rechercher()`, ci-dessous).
- **Condition de « pré-enregistré » resserrée** (décision explicite) : pas « une ligne `Utilisateur` existe », mais « au moins un `MembreRole` rattaché » — sinon le nouveau mécanisme reproduirait le trou qu'il doit fermer (un `Utilisateur` sans rôle, aujourd'hui déjà possible via l'upsert JIT). `MembreRole` n'a pas de colonne `actif` dans le schéma — la présence de la ligne suffit, aucune ambiguïté à trancher.
- **Tension trouvée avant de coder, pas après** : `RbacResolutionService.resoudre()` ne se contente pas de créer `Utilisateur` — il **resynchronise `MembreRole` à chaque connexion depuis les groupes AD du moment** (`deleteMany({roleCode: notIn: codesRoles})` puis upsert). Si le pré-enregistrement assigne un rôle manuellement (indépendant des groupes AD), ce mécanisme l'aurait supprimé à la connexion suivante si le groupe AD ne mappe vers aucun rôle connu — annulant silencieusement le pré-enregistrement. **Décision actée** : le pré-enregistrement devient la **seule** source de rôle — la resynchronisation par groupe AD est retirée du flux de connexion en Temps 2 (pas une option parmi d'autres : l'API AD réelle ne renvoie aucun groupe pour la recherche construite ici, la resynchronisation est de toute façon impossible à alimenter pour un utilisateur qui n'y passe jamais). `Role.groupeAd` redevient informatif (affiché dans l'écran de recherche), jamais déclencheur automatique.
- **Aucune dépendance cachée au JIT** — vérifié avant de coder le Temps 1 : `RbacResolutionService.resoudre()` a un seul appelant dans tout le dépôt (`AuthController.login()`), aucun job/export/autre lecture de `codesRoles`. `MembreRole` est lu ailleurs (`notification.service.ts`, `delegant-membre-role.guard.ts`, `kpi-engine.service.ts`, `taches.controller.ts`) mais toujours comme état courant, jamais en dépendant de la resynchronisation elle-même — rien ne s'oppose au retrait en Temps 2.
- **Impact sur les 15 identités de test persistantes vérifié, pas supposé** — requête directe : les 15 ont chacune ≥1 `MembreRole` (jean.kouassi en a deux), aucune ne serait bloquée par la condition resserrée. 7 lignes `Utilisateur` supplémentaires trouvées avec 0 `MembreRole` (`test.parametres@orange.ci` etc.) — pas des identités de connexion, ce sont des fixtures créées par `prisma.utilisateur.upsert()` direct dans des tests d'intégration (jamais dans l'annuaire LDAP), sans rapport avec ce chantier.

**Modèle de données (Temps 1)** : `Utilisateur.directionId`/`serviceId`, nullable, même référentiel que `Demande.directionRespId`/`serviceRespId` (`DirectionResponsabilite`/`ServiceResponsabilite`) — pas une nouvelle table. Distinct sémantiquement de son homonyme sur `Demande` (qui porte la responsabilité d'un dossier, pas le rattachement de l'agent lui-même). Migration `20260812102836_utilisateur_direction_service` : `@default` inchangé côté schéma pour ce chantier (nullable), FK `ON DELETE SET NULL` (même convention que `Demande`).

**`LdapPort.rechercher(motCle)`** (`LdapProvider`) : filtre partiel `(|(cn=*motCle*)(mail=*motCle*))` sur `ou=users` — les seuls attributs réellement présents dans l'annuaire (`uid`/`cn`/`sn`/`givenName`/`mail`, vérifié contre `docker/openldap/seed.ldif`, aucun `displayName`). `sizeLimit: 20` borne un joker large. Groupes résolus pour affichage informatif uniquement (cf. tension ci-dessus). Vérifié en direct (Testcontainers, `ldap-provider.integration.spec.ts`) : recherche par fragment de `cn`, par fragment de `mail`, tableau vide sans erreur si rien ne correspond.

**Écran admin « Utilisateurs »** (`AdminScreen`, 8ᵉ onglet — comble le trou documenté depuis Phase 9 : « aucune route ne sert cet onglet »). Layout inspiré de la maquette (`screens3.jsx:929-993`, `UsersView`/`UserModal` — table + modal), logique de démo non reprise (`D3.USERS` fictif, `serviceDobb` hors périmètre — concept de pré-remplissage de sous-flux DOBB sans rapport avec ce chantier). **Aucune saisie libre de l'identifiant AD** : la recherche annuaire est un préalable obligatoire à toute création — décision actée, une faute de frappe créerait un compte jamais authentifiable, l'identifiant réel restant la clé d'appariement avec LDAP. Sélectionner un résultat déjà pré-enregistré bascule automatiquement en édition plutôt qu'en création (comparaison sur `identifiantAd`). `mfaMethode` (DUO/TOTP) inclus dans le même formulaire — champ trivial (un ajout de plus sur la même route), distinct de la génération/transmission d'un secret TOTP (mis de côté, question de sécurité opérationnelle à trancher séparément, cf. Questions ouvertes). Aucune suppression physique — `actif: false` est le mécanisme de retrait d'accès (cohérent avec l'absence de `DELETE` sur ce référentiel).

**Routes** (`admin/utilisateurs`, `ADMIN_PGD` uniquement) : `GET .../annuaire?q=` (recherche AD, route littérale déclarée **avant** `:id` dans le contrôleur — même discipline que le bug d'ordre de routes déjà trouvé sur `AuditController` en Phase 8, reproduit ici par construction), `GET .../` (liste, filtrée `membresRole: { some: {} }` — même condition que celle qui gouvernera le refus à la connexion en Temps 2, pas une coïncidence), `GET .../:id`, `POST .../` (création, valide que chaque rôle soumis existe réellement en base avant d'écrire), `PATCH .../:id` (édition, `roles` si fourni remplace l'ensemble complet — même convention que les pièces d'un motif ou les étapes d'un palier, jamais de fusion partielle ; `roles: []` reste rejeté par le `min(1)` hérité du schéma de création). Pas d'entrée `guard-coverage.spec.ts` nécessaire — `ADMIN_PGD` simple, aucun guard de portée au-delà du rôle (contrairement à `InitiateurDemandeGuard`/`CorbeilleRoleGuard`).

**Vérifié en direct, dans les deux sens** (Playwright, session `jean.kouassi`, plus un utilisateur AD jetable ajouté à l'annuaire de dev le temps du test) : recherche trouve l'utilisateur non encore enregistré (badge « + Pré-enregistrer ») → formulaire de création, identifiant en lecture seule, bouton Enregistrer désactivé tant qu'aucun rôle n'est coché → après création, la même recherche affiche « Déjà pré-enregistré — modifier » → édition ouvre bien « Modifier », désactivation du compte (`actif: false`) persistée. Confirmé directement en base après chaque étape (direction/service/mfaMethode/rôle exacts, `actif` correct). Identité de test et entrée LDAP jetable supprimées après vérification.

**Dette de test trouvée en sweep complet, pas dans ce tour mais exposée par lui** : `admin-parametres-calcul.integration.spec.ts` (« recalcule les brouillons du circuit et gèle... ») avait un calcul de TVA attendu (`210_000`) qui supposait implicitement l'assiette **ambiante** du circuit DOBB (`HT_TSC`) sans jamais la fixer explicitement dans l'appel à `modifier()` — passé silencieusement tant que le défaut DOBB était `HT_TSC`, cassé dès la bascule confirmée vers `HT` (cf. section dédiée ci-dessus). Corrigé en fixant `assietteTvaDefaut: "HT_TSC"` explicitement dans ce test précis — il porte sur le recalcul de taux, pas sur l'assiette (déjà couverte par son propre test dédié), aucune raison qu'il dépende d'un état ambiant non contrôlé. Sweep complet (37/37 suites, 218/218 tests) vert seulement après ce correctif.

**Incident conteneur trouvé en vérifiant, pas en supposant le sweep hôte suffisant** : après la migration de schéma, le conteneur `api` a échoué en compilation (`TS2551`/`TS2339` sur `serviceId`/`direction`) malgré un `docker compose restart api` — cf. section dédiée « Incident — `prisma generate` sur l'hôte ne met jamais à jour le Prisma Client d'un conteneur dev » ci-dessus. Résolu en régénérant le client Prisma **dans** les conteneurs `api` et `worker`, pas seulement sur l'hôte.

**Temps 2 — couverture de test construite AVANT le comportement, comme convenu explicitement.** Aucun test du projet n'exerçait `RbacResolutionService`/le comportement JIT avant ce tour (vérifié, pas supposé — recherche dédiée). Deux fichiers écrits en premier, contre le code INCHANGÉ, puis exécutés pour confirmer qu'ils échouent pour la bonne raison (pas un bug du test lui-même) avant d'écrire une seule ligne de comportement :
- `apps/api/test/rbac-resolution.integration.spec.ts` (intégration réelle, Postgres, sans HTTP) — 5 cas, tous rouges contre l'ancien code avant modification (`resultat.statut` `undefined` puisque l'ancien `resoudre()` ne renvoyait pas ce champ ; le test « rôle manuel survit » et le test « nom jamais réécrit » ont d'ailleurs révélé, en étant rouges, exactement les deux comportements que Temps 2 devait supprimer — resynchronisation AD et upsert JIT).
- `apps/api/test/auth-provisionnement.e2e-spec.ts` (HTTP réel via `supertest`, `LdapProvider` remplacé par un faux `authentifier()` toujours réussi — pas besoin d'identité LDAP réelle, ce test porte sur ce que fait le SERVEUR une fois l'AD franchi) — 2 cas de refus rouges (200 reçu au lieu de 401 attendu, l'ancien JIT accordait silencieusement une session à rôles vides) ; le 3ème cas (déjà pré-enregistré → session accordée) était VERT dès le départ — preuve que le test capture fidèlement le comportement actuel plutôt que d'inventer un échec, et confirmation independante que le chemin heureux n'a pas besoin d'être changé.
- `demarrerAppE2e()` (`apps/api/test/helpers/e2e-app.ts`) étendu avec un paramètre `overrides` optionnel (`overrideProvider` NestJS) — rétrocompatible, les 6 e2e existants ne le passent jamais.

**Nouveau contrat de `RbacResolutionService.resoudre()`** — une simple LECTURE, plus aucune écriture sur `Utilisateur`/`MembreRole` :
```ts
type ResolutionRbac =
  | { statut: "AUTORISE"; utilisateur: Utilisateur; roles: string[] }
  | { statut: "NON_PROVISIONNE"; utilisateurId: string | null };
```
`identifiantAd` inconnu → `NON_PROVISIONNE` avec `utilisateurId: null` (aucune ligne `Utilisateur` créée — JIT retiré). `Utilisateur` existant sans aucun `MembreRole` → `NON_PROVISIONNE` avec l'id réel (distinction utile pour le journal). `Utilisateur` avec ≥1 `MembreRole` → `AUTORISE`, rôles lus tels quels depuis `MembreRole` — `utilisateurAd.groupes` n'est **plus lu du tout** (resynchronisation AD supprimée, décision option (a) actée avant Temps 1). `nom` n'est plus jamais réécrit à la connexion : propriété exclusive de l'écran admin désormais (cohérent avec un champ `nom` éditable dans `UtilisateurModal`).

**`AuthController.login()`** — nouvelle branche juste après `resoudre()`, avant la vérification MFA : `statut === "NON_PROVISIONNE"` → `journal.consigner({evenement: "ACCES_NON_PROVISIONNE", facteur: "AD", succes: false, utilisateurId: resolution.utilisateurId ?? undefined})` puis `401 COMPTE_NON_PROVISIONNE` (« Ce compte n'a pas été pré-enregistré. Contactez votre administrateur. »). Distinct de `LOGIN succes:false` (mot de passe invalide) — `ACCES_NON_PROVISIONNE` nouvelle valeur `EnumEvenementSecurite` (migration `20260812110000_acces_non_provisionne`, `ALTER TYPE ... ADD VALUE`), `facteur: "AD"` (l'échec n'est pas au niveau MFA).

**Vérifié en direct, sur l'application réelle, pas seulement en test automatisé** : identité LDAP jetable (`e2e.temps2@orange.ci`, `docker/openldap`) sans aucun `MembreRole` → `LoginScreen` affiche littéralement « Ce compte n'a pas été pré-enregistré. Contactez votre administrateur. », aucun cookie de session posé (confirmé par capture de la page réelle, pas seulement l'appel API) ; `curl` direct confirme `401`/`COMPTE_NON_PROVISIONNE`, et `JOURNAL_SECURITE` porte bien l'entrée `ACCES_NON_PROVISIONNE`. Identité déjà pré-enregistrée (`responsable.df@orange.ci`, sans MFA) → `200`, JWT décodé confirme `roles: ["RESPONSABLE_DF"]` (rôle réel lu depuis `MembreRole`, pas depuis un groupe AD). **Incident rencontré et documenté à cette occasion** : le premier essai a renvoyé `200` avec `roles: []` (comportement de l'ANCIEN code) malgré des fichiers source déjà à jour dans le conteneur — `docker compose restart api` seul n'avait pas suffi à faire recompiler `nest start --watch` ; un second `restart` a forcé la recompilation et corrigé le résultat. Symptôme de la même famille que le gotcha déjà documenté pour `web` (watcher qui rate silencieusement un changement) — **vaut donc aussi pour `api`**, pas seulement pour `web` comme le texte existant le limitait implicitement ; toujours revérifier après tout redémarrage de conteneur suivant une édition de code, ne jamais supposer qu'un simple `restart` a recompilé.

Suite complète revérifiée après l'implémentation (hôte, `worker` arrêté pour la déterminisme de `si-service`) : 38/38 suites, 223/223 tests (`pnpm test`) + 7/7 suites, 20/20 tests (`pnpm test:e2e`, `ldap-provider.integration.spec.ts` compris) — zéro régression.

**Effet de bord positif retrouvé en vérifiant, pas anticipé** : l'identité de test `responsable.df@orange.ci` (une des 4 réactivations documentées plus bas, cf. « Identités de test persistantes ») avait déjà servi lors de l'essai de bout en bout de clôture de Phase 9 puis a été réutilisée sans modification pour cette vérification — aucun geste supplémentaire nécessaire, le socle de 15 identités reste valide tel quel sous le nouveau comportement (conforme à l'audit déjà fait avant Temps 1 : chacune porte ≥1 `MembreRole`).

### Renvoi ou clôture d'un dossier rejeté (13/08/2026)

Cul-de-sac fonctionnel trouvé en Phase 10.6bis (aucune route ne permettait de corriger et resoumettre un dossier `REJETE`), confirmé par la source primaire (`docs/12_Exigences_Fonctionnelles_Source_Originale.md`, diapositive 17 : « la demande rejetée doit être soit renvoyée à l'initiateur, soit clôturée selon les règles du workflow »). Conception proposée avant tout code (deux options de faisabilité technique, quatre options de distinction renvoi/clôture, compatibilité R6 vérifiée) puis tranchée par la personne pilotant le projet : **combinaison des options « choix explicite du rejeteur » et « renvoi par défaut »** — `rejeter()` renvoie systématiquement le dossier à l'initiateur pour correction, sauf si le rejeteur coche explicitement « et clôturer » au moment même du rejet, auquel cas un motif de clôture devient obligatoire, distinct du motif de rejet. Aucune règle automatique sur un compteur de rejets ou sur l'étape/le rôle du rejeteur — délibérément écarté.

**Contrat** (`rejeterRequeteSchema`, `packages/contracts/src/tache.ts`) : `clore: z.boolean().default(false)`, `motifCloture: z.string().min(1).optional()`, avec un `.refine()` qui rend `motifCloture` obligatoire uniquement quand `clore=true` — jamais l'inverse.

**`TacheWorkflowService.rejeter()`** — même transaction, deux branches :
- **Par défaut (`clore` absent ou `false`)** : réutilise **exactement** le mécanisme déjà écrit pour `rappeler()` — purge totale de `Tache` pour ce dossier (`tx.tache.deleteMany({demandeId})`, y compris la tâche tout juste marquée `REJETEE`), `Demande.statut → BROUILLON`, `etapeCourante → 0`, `dateCloture → null` explicitement (un dossier redevenu `BROUILLON` n'est plus « clôturé »), plus une entrée `JournalAudit` dédiée (`action: "renvoi-correction"`). L'initiateur corrige et resoumet par le chemin `BROUILLON → SOUMIS` déjà entièrement construit et testé — **R6/R12 s'appliquent alors normalement, sans aucune logique spécifique au rejet** (vérifié, pas supposé : c'est précisément pourquoi cette option a été préférée à la seule alternative envisagée — assouplir `verifierAucuneDecision` en gardant le dossier `REJETE` aurait laissé une tâche orpheline en base, `reRouterSiEngage()` ne purgeant jamais `APPROUVEE`/`REJETEE`).
- **`clore: true`** : comportement terminal identique à l'ancien fonctionnement (seul chemin possible avant ce chantier) — `Demande.statut → REJETE`, `dateCloture` posée — plus une entrée `JournalAudit` **distincte** (`action: "cloture"`, `commentaire: motifCloture`), jamais fusionnée avec l'entrée `"rejet"` (`commentaire: motif`).

**Bug trouvé et corrigé avant tout test** : la première version retournait `this.tacheService.trouver(tacheId)` après le commit — échoue en 404 dans la branche renvoi, puisque la ligne `Tache` vient d'être supprimée dans la même transaction. Corrigé en capturant le `TacheVue` **à l'intérieur** de la transaction (juste après le `tx.tache.update(...REJETEE...)`, avant toute suppression éventuelle), retourné tel quel dans les deux branches.

**Piège d'orphelinage trouvé en écrivant le test, pas deviné** : dans la branche renvoi, l'entrée `JournalAudit` de type `"rejet"` référence `tacheId` — mais la tâche est supprimée dans la **même transaction**, juste après. `journal_audit_tache_id_fkey` est `ON DELETE SET NULL` : la ligne d'audit survit (comme toujours), mais `tache_id` y est déjà `NULL` **avant même la fin de la transaction** — un test qui filtre sur `{tacheId, action: "rejet"}` échoue avec `NotFoundError`, pas parce que l'entrée n'existe pas mais parce que la FK a déjà été résolue. Corrigé en filtrant sur `{demandeId, action: "rejet"}` uniquement — même famille d'orphelinage que celle déjà documentée pour `JournalAudit` ailleurs dans ce fichier, jamais un test cassé par erreur.

**Frontend** (`TacheActionBanner.tsx`) : case à cocher « Et clôturer le dossier, au lieu de le renvoyer à l'initiateur pour correction » dans la modale de rejet existante, avec microcopy expliquant le comportement par défaut ; un champ « Motif de clôture » apparaît conditionnellement quand la case est cochée ; le bouton de confirmation change de libellé (« Confirmer le rejet et le renvoi » / « Confirmer le rejet et la clôture ») pour refléter l'action réelle avant de cliquer.

**Vérifié en direct, dans les trois cas, sur l'application réelle** (session `responsable.dobb`, dossiers DOBB jetables) : rejet sans `clore` → `Demande.statut=BROUILLON`, `etapeCourante=0`, `dateCloture=NULL`, 0 tâche restante, `JournalAudit` porte `"rejet"` et `"renvoi-correction"` ; rejet avec `clore=true` + motif → `Demande.statut=REJETE`, `dateCloture` posée, étape suivante toujours `EN_ATTENTE`, deux entrées `JournalAudit` distinctes (`"rejet"`/`"cloture"`) avec leurs commentaires respectifs ; `clore=true` sans `motifCloture` → `400 VALIDATION_ECHOUEE` avec l'erreur exacte sur le bon champ. UI vérifiée dans le navigateur réel jusqu'à la soumission complète (case à cocher → champ conditionnel → libellé de bouton → statut « Rejeté » → onglet Journal d'audit affichant les deux entrées) — **conteneur `web` redémarré en cours de route** (watcher n'avait pas pris en compte l'ajout de la case à cocher, même famille de gotcha déjà documentée pour ce conteneur, cf. incident dédié plus bas).

Suite complète revérifiée après l'implémentation (hôte, `worker` arrêté pour la déterminisme de `si-service`) : 38/38 suites, 229/229 tests (`pnpm test`) + 7/7 suites, 20/20 tests (`pnpm test:e2e`) — zéro régression.

### Sous-flux — référentiel SF-PGD-109 construit, mécanisme de dérivation encore en conception (14/08/2026)

`docs/09` §13.3 (« Motifs & circuits : référentiel des sous-flux et motifs par circuit ») et `docs/12` diapositive 17 (« création de processus… sous-flux ») convergent : le sous-flux est un référentiel réel par circuit, pas un champ texte inventé. Vérifié avant de construire, pas supposé — quatre points, chacun avec citation exacte :

- **`etapesDe(tranche, service)`** (`docs/design/engine.jsx:276`, appel dans `submit()`) reçoit **`dossier.sousFlux`**, jamais `dossier.responsabiliteService` — sans ambiguïté.
- **`parService`** (`docs/design/data.jsx:83-87`) est keyé sur `Object.keys(_dobbHead)` = `["Réclamation", "Recouvrement", "ADV", "Facturation"]` (`data.jsx:58-63`) — exactement `CIRCUITS.DOBB.sousFlux`, pas un vocabulaire différent.
- **`responsabiliteService`** (`engine.jsx:224`, parsé depuis le champ « Responsabilité — direction/service ») correspond à `RESP_SERVICE` (`data.jsx:223`, 14 valeurs) — la **même** liste déjà comparée aux 4 sous-flux dans le rapport de réconciliation (2/4 correspondances exactes, 1 désalignement de granularité sur ADV, 1 absence totale sur Réclamation B2B) : pas un troisième vocabulaire, et de toute façon jamais lu par `etapesDe()`.
- **Les rôles « tête » de `_dobbHead`** sont exclusifs à un seul sous-flux (`RRB2B`/`MRB2B` → Réclamation seule, etc.), **sauf `MSOC`** (Manager Service Opérations Client), partagé entre ADV et Facturation (`_dobbHead["ADV"]`/`_dobbHead["Facturation"]`) — la dérivation par rôle détenu fonctionnerait pour un initiateur (rôle de tête exclusif), pas de façon générale pour toute la chaîne. Aucun rôle granulaire de ce type n'est aujourd'hui seedé (seul `INITIATEUR_DOBB` générique existe) — dépend du chantier de rôles du rapport de réconciliation, pas encore construit.

**Construit** : `SousFlux` (modèle déjà présent au schéma, jamais seedé avant ce tour) — seed réel (`sous-flux.seed.ts`) : DOBB = Réclamation B2B/Recouvrement/ADV/Facturation (libellé exact de `docs/11`, source primaire — préféré à « Réclamation » sans suffixe de la maquette) ; DXC = Réclamation/Geste commercial ; DF = Réclamation opérateur (`docs/11` ne donne aucun sous-flux pour ces deux circuits, seule `data.jsx` en fournit — utilisée faute d'alternative, jamais en contradiction avec une source plus autoritaire). CRUD complet (`AdminSousFluxService`/`AdminSousFluxController`, `ADMIN_PGD`, `/api/admin/sous-flux`) — pas de champ `actif` (absent du modèle), pas de garde `P2003` à la suppression (`Demande.sousFlux`/`ConfigurationCircuit.sousFlux` restent des chaînes libres, aucune FK vers cette table). Écran `SousFluxAdminTab`, même onglet « Motifs & libellés » que `MotifsAdminTab`/`LibellesAjustementAdminTab`.

**Vérifié en direct** : requête HTTP authentifiée réelle (`supertest`, session `jean.kouassi`/`ADMIN_PGD`, Postgres réel — pas un mock) sur `GET /api/admin/sous-flux` → les 7 valeurs seedées, correctement réparties par circuit. Capture d'écran non obtenue — l'injection de cookie de session dans le navigateur de l'environnement de développement est bloquée (`document.cookie` reste vide après écriture, sur deux origines et deux tentatives distinctes, cause non élucidée côté harnais) ; `pnpm build` (compilation de production complète, TypeScript inclus) et les logs du conteneur `web` (aucune erreur de compilation) restent le niveau de vérification atteint pour l'écran lui-même — cohérent avec le composant, qui reproduit **verbatim** le patron déjà vérifié en direct de `LibellesAjustementAdminTab`.

**Reste bloqué, comme convenu** : aucun champ ajouté sur `Utilisateur`, aucune conception de dérivation automatique. `NouvelleDemandeScreen`'s champ « Sous-flux » reste un texte libre, inchangé. Question suivante posée par la personne pilotant le projet, en attente : pour DXC, le sous-flux se déduit-il du profil comme envisagé pour DOBB, ou l'initiateur le choisit-il lui-même à la création ?

### `Utilisateur.sousFluxId` — préremplissage de session, DOBB et DXC (14/08/2026)

Suite directe de SF-PGD-109 ci-dessus, après confirmation métier complète. **Correction de trajectoire trouvée en cours de conception, pas supposée** : la piste envisagée d'abord (dérivation du sous-flux depuis le rôle détenu, `MembreRole`, sans aucun champ sur `Utilisateur`) contredisait un écran déjà validé de la maquette — `data.jsx:238`/`screens1.jsx:130`/`screens3.jsx:981` montrent un champ dédié (`serviceDobb`) sur la fiche utilisateur, peuplé par un menu déroulant admin, jamais un rôle granulaire par sous-flux. Signalé explicitement avant de construire quoi que ce soit ; la personne pilotant le projet a confirmé l'option champ dédié, avec la distinction suivante : la règle « la source primaire l'emporte » (docs/11 vs maquette) s'applique à un **fait métier disputé**, jamais à un **choix de modélisation RBAC déjà incarné dans un écran de maquette validé** — deux règles différentes, ne pas les confondre à l'avenir.

**Conception actée** :
- Colonne **générique unique** (`Utilisateur.sousFluxId`, FK nullable vers `SousFlux`) — pas `sousFluxDobbId`/`sousFluxDxcId` séparés : un seul mécanisme, valable pour n'importe quel circuit.
- **DXC utilise le même mécanisme que DOBB** — question qui semblait ouverte (« l'initiateur choisit-il lui-même pour DXC ? ») refermée explicitement par la personne pilotant le projet : « le sous-flux se déduit du profil pour DXC exactement comme pour DOBB ».
- **Exposition en session : JWT à la connexion, jamais résolu à la lecture** — même précédent que `roles` (déjà JWT-embedded). Décision motivée explicitement : « je choisis la cohérence plutôt que la fraîcheur… s'ils n'obéissaient pas tous à la même règle de "prend effet quand", un administrateur changeant plusieurs champs à la fois observerait un comportement incohérent. » Le message « effectif à la prochaine connexion » déjà affiché pour les rôles (`UtilisateurModal`) couvre donc aussi ce champ, sans texte supplémentaire.
- Nouvelle route publique `GET /api/referentiels/sous-flux?circuit=` (même patron que `motifs`/`libelles-ajustement` — filtrée, ouverte à tout authentifié, réutilise `AdminSousFluxService.lister()` déjà exporté par `AdminModule`), distincte de `GET /api/admin/sous-flux` (`ADMIN_PGD`, CRUD complet) : `NouvelleDemandeScreen` consomme la première, `UtilisateursAdminTab`/`SousFluxAdminTab` la seconde.
- Sur `NouvelleDemandeScreen` : préremplissage depuis `utilisateur.sousFluxId` (session), **reste éditable** — jamais une valeur figée. Le champ est passé d'un texte libre à un `<select>` scopé au circuit courant (mêmes valeurs que le référentiel `SousFlux`, dont `Demande.sousFlux`/`ConfigurationCircuit.sousFlux` restent des chaînes libres — matchées par égalité de libellé, pas de FK). Si le `sousFluxId` de session ne correspond à aucune entrée du circuit sélectionné (profil DOBB ouvrant un dossier DXC, par exemple), aucune présélection — l'initiateur choisit manuellement, comme motif/libellé.
- **Obligatoire à la soumission, pas à la création** — décision explicite, même mécanisme cumulable que R13/R14 : un `BROUILLON` peut exister sans sous-flux, `POST .../soumettre` le refuse (`422 REGLE_METIER_VIOLEE`, code `SOUS_FLUX_REQUIS`, cumulable avec les autres erreurs de soumission). S'applique aux **trois circuits** (DOBB/DXC/DF), pas seulement DOBB — la maquette montre le champ uniformément sur les trois, et SF-PGD-109 a seedé les trois.

**Migration** (`20260814090000_utilisateur_sous_flux`) : `Utilisateur.sousFluxId` nullable, `ON DELETE SET NULL` (même convention que `directionId`/`serviceId`) — régénération du client Prisma faite sur l'hôte et dans les deux conteneurs (`api`/`worker`), cf. incident dédié plus haut dans ce fichier.

**Effet de bord sur la suite de tests existante, trouvé et corrigé, pas anticipé** : rendre le sous-flux obligatoire à la soumission a cassé silencieusement tout test qui soumettait un dossier sans en fixer un — `demande-workflow.integration.spec.ts` (succès + R12 + R1/R11 + abandon/rappel) et les cinq e2e HTTP réels par circuit (`dobb-circuit`/`dxc-circuit`/`df-circuit`/`df-circuit-palier1`/`df-circuit-palier3`/`demandes-controller`). Corrigé en ajoutant `sousFlux: "<libellé seedé du circuit>"` à chaque payload de création qui mène à une soumission réussie ; les tests qui vérifient un rejet par `arrayContaining` (R12 notamment) n'avaient besoin d'aucun changement — la présence additionnelle de `SOUS_FLUX_REQUIS` dans le tableau d'erreurs ne casse pas une assertion qui ne vérifie pas l'exhaustivité.

**Piège d'environnement retrouvé en vérifiant, pas en le supposant réglé** : lancer la suite complète depuis Git Bash (`source .env`) a d'abord fait échouer 5 suites sans rapport avec ce chantier (`demande_initiateur_id_fkey`, puis dans un second run `Expected ConnectionOpenOk; got <ConnectionClose channel:0>` sur RabbitMQ) — exactement l'incident déjà documenté plus bas dans ce fichier (« Git Bash + RabbitMQ — une variable d'environnement commençant par `/` se fait mangler »), mais il touche en réalité **toute variable `.env` chargée via `source` en Git Bash**, pas seulement `RABBITMQ_VHOST` : `TOTP_ISSUER=PGD Orange CI` (valeur non citée, espaces) fait échouer `source` avec `Orange: command not found` et corrompt le chargement des variables suivantes. Reproduit dans les deux sens : la suite complète est passée à 100 % (39/39 suites, 234/234 tests ; e2e 7/7, 20/20 ; worker 7/7, 25/25) une fois relancée en **PowerShell**, avec les variables `.env` chargées via `[System.Environment]::SetEnvironmentVariable` plutôt que `source`. Généralise la leçon déjà posée pour RabbitMQ : pour tout `pnpm test` qui a besoin d'un `.env` non trivial, **PowerShell est le shell fiable sur ce poste, jamais Git Bash** — pas seulement pour les variables commençant par `/`.

**Vérifié en direct, HTTP réel de bout en bout** (script `supertest` jetable, session mintée directement — pas de login LDAP, injection de cookie navigateur toujours bloquée dans cet environnement) : session porte `sousFluxId` (`GET /api/auth/session`) ; `GET /api/referentiels/sous-flux?circuit=DOBB` renvoie l'entrée ; soumission sans sous-flux → `422`, `SOUS_FLUX_REQUIS` présent ; `PATCH .../taxes` remplacé ici par `PATCH /api/demandes/{id}` posant `sousFlux` (texte libre, valeur du libellé référentiel) puis nouvelle soumission → `200`, `statut: "SOUMIS"`. Script et identités jetables supprimés après vérification.

**Reste à faire (P2, pas ce tour)** : le préremplissage n'a d'effet pratique que si un rôle/palier différencie réellement par sous-flux — aujourd'hui seul un rôle générique (`INITIATEUR_DOBB`) existe, aucune chaîne DOBB/DXC ne varie encore selon le sous-flux choisi. Chantier de rôles/paliers différenciés (DOBB : 9 rôles nouveaux + 6 paliers ; DXC : `VER_DXC` comme étape réelle distincte de l'initiateur + 5 rôles + 5 paliers ; DF : renommage cosmétique) confirmé par la personne pilotant le projet, séquencé après ce chantier, pas encore construit.

### Messagerie RabbitMQ (`apps/worker`)

Exchange topic durable `pgd.events` · exchange `pgd.dlx` + file `q.dead-letter`.

| File | Routing key | Rythme | Rôle |
|---|---|---|---|
| `q.locks-sweeper` | `lock.sweep` | cron 5 min → publie | Libère les verrous expirés sans décision |
| `q.sla-escalation` | `sla.check` | cron 15 min → publie | Escalade les tâches dont le SLA est dépassé |
| `q.notifications` | `notification.*` | événement | in-app + SMTP (bouchon) |
| `q.si-push` | `si.push` | validation + rejeu | Restitution au SI, idempotente, `prefetch=1` |

**Non négociable côté messagerie :**
- Publication `persistent` + `publisher confirms`.
- `ack` **manuel**, émis **après** le commit de la transaction Prisma — jamais avant.
- Retry avec back-off exponentiel, 5 tentatives max, puis routage vers `pgd.dlx`.
- Tout consumer est **idempotent** : RabbitMQ garantit *at-least-once*, jamais *exactly-once*. Protéger par mise à jour conditionnelle SQL ou par clé d'idempotence (`si_idempotency_key`).
- Les déclenchements périodiques restent portés par `@nestjs/schedule` dans `apps/worker`, qui **publie un message** au lieu d'exécuter le traitement en ligne (préserve retry et DLX).
- Redis n'est **pas** un broker ici : cache de configuration et verrous de claim uniquement.

**R9 dans `SlaEscalationService`** — la nouvelle échéance après escalade est calculée avec `ajouterHeuresOuvrees` (`packages/database/src/calendrier-sla.util.ts`), **jamais** un delta brut en millisecondes : un bug initial (Phase 6) faisait `now + slaHeures*3600*1000`, ce qui aurait poussé une échéance en dehors des heures/jours ouvrés. La fonction est désormais **partagée** entre `CalendrierSlaService` (`apps/api`) et `SlaEscalationService` (`apps/worker`) via `@pgd/database` — ne jamais la dupliquer dans un des deux apps, R9 doit rester une seule implémentation.

**Portée volontairement partielle de `SlaEscalationService`** : il incrémente `niveau_escalade` et repousse `echeance_sla`, il ne réaffecte **jamais** `role_corbeille` vers une « corbeille N+1 » — cf. Questions ouvertes.

**Gotcha dev (`docker-compose.override.yml`)** : le service `worker` tourne en `nest start --watch` avec bind-mount du dépôt. Le compilateur TypeScript en mode watch ne surveille pas `node_modules` : si `packages/database` (ou tout autre package partagé) est reconstruit (`pnpm --filter @pgd/database build`) pendant que le conteneur tourne, il faut `docker compose restart worker` pour que le nouveau `dist`/`.d.ts` soit pris en compte — sinon le conteneur reste bloqué sur la dernière erreur de compilation vue avant la reconstruction.

### Notifications (PGD-073, Phase 8.3)

`NotificationService` (`apps/worker/src/notifications/notification.service.ts`) consomme `q.notifications` (liaison générique `notification.*`, aucune topologie nouvelle) et distingue deux familles de destinataire :

- **Déterminé par la donnée** : `NOUVELLE_TACHE` (tous les `MembreRole` du `role_corbeille` de la tâche), `AVANCEMENT`/`REJET`/`VALIDATION` (l'`initiateur_id` de la demande) — jamais de doute possible, résolu directement.
- **Paramétrable, jamais deviné** : `ESCALADE` (superviseur SLA) et `ERREUR_SI` (exploitation) — aucune source (docs/01/03/04) ne nomme le rôle destinataire (cf. Questions ouvertes, deux entrées distinctes). Chaque type lit sa propre clé `PARAMETRE_GLOBAL` (`destinataire_notification_escalade_sla`, `destinataire_notification_erreur_si`, toutes deux seedées à `{"roleCode": null}`) — **pattern réutilisé de R15** (config globale plutôt qu'endpoint dédié). Tant que `roleCode` est `null`, le service ne notifie **personne** et journalise un `WARN` explicite ; il ne devine jamais un rôle par défaut. Vérifié en direct (Phase 8.3) dans les deux sens : rôle configuré → notification créée + `SmtpPort.envoyer()` appelé avec l'identifiant AD réel du membre ; rôle non configuré → zéro ligne `Notification`, aucune erreur.

Publication toujours **après commit**, jamais avant (même principe que `si.push`) : `TacheWorkflowService.approuver/rejeter` et `SlaEscalationService.escalader`/`SiPushService.traiter` publient sur les routing keys dédiées (`notification.nouvelle_tache`, `notification.avancement`, `notification.rejet`, `notification.validation`, `notification.escalade`, `notification.erreur_si`) une fois la transaction Prisma commitée.

### KPI (PGD-074, Phase 8.4)

`KpiEngineService` (`apps/api/src/modules/kpi/services/kpi-engine.service.ts`) agrège directement sur `DEMANDE` (jamais via `JOURNAL_AUDIT`), piloté entièrement par `KPI_DEFINITION` (26 lignes seedées, 6 familles) — aucun indicateur en dur.

**Convention « reçu vs traité »** (docs/04 §3.2/§1008) : le schéma n'a pas de colonne booléenne `degrevement_saisi_si` — `KpiDefinition.surDossiersTraites` force `si_etat = CONFIRME`. Même fragilité assumée que la convention FRA de R12 : si le drapeau est mal positionné en base, aucune erreur ne se déclenche.

**Cache Redis TTL court (60 s), sans invalidation à l'écriture** — décision volontaire, pas un oubli : trop de chemins d'écriture touchent `DEMANDE` (soumission, approbation, rejet, si-push, contrôle, correction de montant) pour qu'une invalidation ciblée reste fiable ; une invalidation non fiable donnerait une illusion de fraîcheur pire qu'un TTL assumé. Vues matérialisées **non implémentées** — docs/04 les recommande pour les KPI « lourds », mais c'est une optimisation prématurée sans problème de performance démontré ; les index de Phase 4 (`universFmiCode+facteurCode+siEtat+dateCloture`, etc.) couvrent déjà les dimensions du référentiel.

**Périmètre de `profil` — vérifié contre des données réelles, jamais déclaratif** (trouvé en revue avant 8.5, même classe de faille que l'audit RBAC d'avant 8.2, mais sur une lecture agrégée) :
- `profil=initiateur` : forcé à `Demande.initiateurId = appelant.id`, quoi que le client demande — aucune ambiguïté, la colonne existe déjà.
- `profil=valideur` : forcé aux dossiers portant une `Tache.roleCorbeille` réellement détenu par l'appelant (`utilisateur.roles` du JWT, même source que `RbacGuard` — pas une requête `MembreRole` supplémentaire).
- `profil=pilotage` (ou profil absent, le cas par défaut le plus large) : **aucun rattachement utilisateur → direction/service n'existe dans le modèle** pour vérifier un droit de portée globale — décision d'accès binaire (pas un filtre de données), portée par `KpiPerimetreGuard` (`apps/api/src/common/guards/kpi-perimetre.guard.ts`) au niveau route, exactement comme `CorbeilleRoleGuard` pour les tâches. Refus journalisé (`RBAC_REFUS`) comme toute violation de rôle. `ADMIN_PGD` reste un choix provisoire (cf. Questions ouvertes) mais la route n'est plus ouverte à tout authentifié. `KpiEngineService` ne revérifie pas ce périmètre — il suppose l'autorisation acquise (même principe que `TacheWorkflowService` vis-à-vis de `SodGuard`).
- La clé de cache Redis intègre l'identité/les rôles de l'appelant quand le profil scope sur eux (`init:{id}` / `val:{roles triés}`) — sans ça, deux appelants différents demandant le même profil partageraient un résultat en cache pendant le TTL.

### Maquette de référence (frontend)

Source de vérité visuelle, **versionnée dans le dépôt** — export Claude Design, pas d'import à la demande :

- Emplacement : **`docs/design/`**
- Structure : export **modulaire** JSX + CSS (`app`, `engine`, `data`, `ui`, `screens1→4`, `screens_auth`, `tweaks-panel`, `styles.css`, `_shots/`)
- Écarts tranchés : `docs/design/DIVERGENCES.md`
- Provenance : `https://claude.ai/design/p/0a54ca6a-9e31-48e2-8f3c-c9476f354136?file=PGD+-+Plateforme+Du00E9gru00E8vements+%28autonome%29.html`

**Fait foi :** mise en page, charte, typographie, espacements, états et libellés des composants. **Correction (Phase 9.0/9, étape 3)** : les codes couleur de statut de ligne (ACTIF/SUSPENDU/RESILIE) n'ont **jamais fait partie de la maquette** — mention erronée introduite en rédaction sans vérification de source. `docs/design/` ne colore le statut de ligne nulle part (recherche dédiée, négative). Le mapping `--statut-ligne-actif/suspendu/resilie` (`packages/ui/tokens/semantic.ts`) est une **décision de design assumée sans source dans la maquette** — vert/jaune/rouge choisis par cohérence avec les autres familles sémantiques (statut demande, urgence SLA), pas extraits. Voir `docs/design/DIVERGENCES.md`.
**Ne fait pas foi :** règles métier, autorisations, calculs, routage, validations. En cas de divergence, **le PRD l'emporte**, l'écart est consigné dans `DIVERGENCES.md`.

**Les choix visuels sont contraignants, pas une simple référence (acté Phase 9, clôture).** Les Directions Métiers ont validé la mise en page, les illustrations, l'iconographie, les couleurs de marque, le contenu textuel statique et la structure des sections de la maquette au même titre que les règles de données et les exigences du PRD. Une divergence avec la maquette n'est légitime que si elle relève de l'une de ces cinq catégories, déjà établies :

1. La maquette contredit le PRD ou le modèle de données (le PRD l'emporte, cf. ci-dessus).
2. La maquette montre une fonctionnalité sans contrepartie serveur réelle (`réaffecter`, `débloquer`, export dossier, etc. — déjà catalogués dans « Questions ouvertes »).
3. La maquette invente une structure de données que le schéma réel n'a pas (ex. champs nommés par circuit alors que `Demande.champsCircuit` est générique) — la structure de données reste générique (R3 l'exige), mais la présentation visuelle de la section doit s'inspirer de la maquette dans toute la mesure du possible : la contrainte porte sur le SCHÉMA, pas sur l'apparence.
4. La maquette décrit son propre état de prototype d'une manière qui serait fausse une fois portée (ex. un texte affirmant une « authentification simulée » sur un système qui authentifie réellement).
5. La maquette expose une information sans contrôle de portée serveur (ex. résolution de nom d'agent) — la sécurité prime toujours sur la fidélité visuelle.

**Toute autre divergence est un défaut d'implémentation à corriger, pas un choix à assumer.** Le silence ou l'oubli n'est pas une catégorie 1 à 5 — voir la section suivante, qui traite spécifiquement du silence sur une fonctionnalité spécifiée (un cas différent de l'omission d'un élément purement visuel). Cette règle ne rouvre **pas** les exclusions déjà décidées et documentées (`DemoAccounts`/`RoleMenu`, pied de page de la Sidebar, états inventés de `WorkflowStepper`, etc.) — ces décisions restent valides, elles relèvent des catégories 2 ou 4 ci-dessus ; ne pas les annuler par excès de zèle.

**`getComputedStyle` ne peut pas détecter l'absence d'un bloc entier** (illustration, liste, icône) — il vérifie des propriétés numériques sur des éléments déjà choisis, jamais la présence/absence d'éléments. Trouvé en clôture de Phase 9 sur `LoginScreen` par capture d'écran réelle, pas par cette méthode : illustration du panneau sombre absente, liste de fonctionnalités absente, icône avant le titre absente, couleur du bouton principal divergente (orange attendu, noir observé) — quatre défauts d'implémentation (aucune des cinq catégories ne s'applique), invisibles à `getComputedStyle` puisqu'aucune de ces vérifications ne portait sur les bons éléments. Un audit de fidélité visuelle doit comparer des captures d'écran bloc par bloc, pas relever des propriétés CSS ponctuelles.

**Capturer un écran dans son état par défaut ne voit qu'une seule branche d'un fichier qui en contient plusieurs — trou de méthode distinct de celui ci-dessus, trouvé en Phase 10.6bis.** Le formulaire mémo DF (`screens1.jsx:400-424`) existait dans `docs/design/` depuis le début, mais n'a été atteint ni par l'analyse documentaire de 9.0 ni par l'audit visuel de 9.3 : les deux ont chargé la persona par défaut (`aya.koffi`, circuit DOBB) sans jamais faire basculer `circuit`/rôle/persona pour exercer activement les autres branches (`circuit === "DF"`, sélecteur de vue par rôle de `DashboardScreen`, etc. — cf. `docs/design/DIVERGENCES.md`, section « Inventaire exhaustif des branches conditionnelles »). Ni la lecture du code ni la capture d'écran ne suffisent seules : une branche conditionnelle (`circuit === `, `roles.includes`, `typeActeur ===`, `etat ===`) reste invisible tant qu'aucune vérification ne déclenche explicitement la persona/l'état qui l'active. **Geste à répéter pour tout futur audit de `docs/design/` ou de tout écran à rendu conditionnel** : recenser d'abord les branches par grep systématique, puis vérifier explicitement, persona par persona/état par état, laquelle a déjà été vue — ne jamais déduire qu'une branche est couverte parce que l'écran qui la contient a, lui, déjà un commit d'audit.

**Le silence de la maquette n'est pas une décision de conception, et n'a aucune autorité sur le périmètre — seulement sur la mise en page.** La règle ci-dessus (« le PRD l'emporte ») couvrait le cas d'une contradiction ; elle ne couvrait pas le cas, distinct, d'une fonctionnalité **spécifiée** (PRD, MCD/MLD, contrats d'API, user stories) que la maquette n'a **jamais représentée** — ni bien ni mal, simplement absente. Ce silence n'autorise pas à omettre la fonctionnalité : elle se construit d'après les sources réelles (PRD, modèle de données, contrats `packages/contracts`, `docs/04_UserStories_Realisation.md`), au même titre qu'un écran qui contredirait la maquette. Déjà appliqué plusieurs fois sans être nommé comme règle : statut de ligne (coloré nulle part dans la maquette, exigé par PGD-081), état SI à quatre valeurs (la maquette le réduit à un booléen), anneau de focus clavier (la maquette ne le pose que sur les champs de saisie), scoping KPI par `profil` réel plutôt que le rattachement direction/service inventé par la maquette pour sa démo. **Nuance qui compte** : ça ne rouvre pas « ne rien inventer ». Combler un silence s'appuie sur une source réelle identifiée, jamais sur un choix libre — quand ni la maquette ni aucune source ne répond, ça reste une question ouverte à remonter (même traitement que `reaffecter`/`debloquer`), pas une occasion d'inventer. Chaque instance comblée ainsi est consignée dans `docs/design/DIVERGENCES.md`, catégorie dédiée « Spécifié, absent de la maquette » — distincte des écarts tranchés (contradiction) ci-dessus.

**`NouvelleDemandeScreen` est unique pour les trois circuits — DOBB, DXC, DF — avec des champs et blocs conditionnels par circuit, jamais des écrans séparés.** Doublement fondé : (1) la maquette elle-même n'a qu'une route (`app.jsx:187`) ; (2) les utilisateurs métier sont formés sur ce design, une scission de l'écran serait risquée pour l'adoption même si un futur document le suggérait. Toute correction issue d'un audit de réconciliation (spec métier, remarques métier sur la maquette, etc.) qui ajoute ou modifie un champ/comportement propre à un circuit s'ajoute à cet écran unique — un bloc conditionnel de plus (même pattern que la carte « Identification » commune vs la carte « DOBB/DXC » vs la carte « Mémo Wholesale » DF, Phase 10.6) — jamais une nouvelle route ni un nouveau composant d'écran par circuit.

**Hors build** : ne rien copier dans `apps/web`, ne rien importer, ne rien compiler. En dériver des composants React typés consommant l'API réelle.

**`engine.jsx` et `data.jsx` ne sont jamais portés.** Ils contiennent la logique de simulation du prototype — routage, seuils de palier, calculs — reproduite côté client pour la démo. Toute décision métier qui s'y trouve vient de l'**API** (`docs/06_Contrats_API.md`), jamais du frontend : `R3` vaut aussi côté client. Les lire sert à comprendre les formes de données attendues, pas à les recopier.

**`tweaks-panel.jsx` est hors périmètre** — panneau de réglages de prototypage, à ne pas porter.

Fichiers volumineux (`screens3.jsx` ≈ 93 Ko) : lecture **par section ciblée**, jamais en entier.

### Contraintes d'intégrité à poser en SQL manuel

Prisma ne sait pas les exprimer : elles vont dans la migration, pas dans le schéma.

| Table | Contrainte |
|---|---|
| `configuration_circuit` | `EXCLUDE USING gist (circuit =, segment =, coalesce(sous_flux,'') =, numrange(borne_min, borne_max, '[]') &&) WHERE (actif)` — anti-chevauchement des paliers |
| `delegation` | `EXCLUDE USING gist (delegant_id =, role_code =, tstzrange(debut, fin) &&) WHERE (active)` — `R22` |
| `delegation` | `CHECK (fin > debut)` · `CHECK (delegant_id <> delegataire_id)` |
| `historique_montant` | `CHECK (acteur_id IS NOT NULL OR origine = 'RECALCUL')` — `R23`, **jamais** une équivalence |
| `formule` | Index unique partiel `ON formule(ligne_id) WHERE courante` — `R19` |
| `demande` | `CHECK` de plancher 0 sur `montant_ht`, `montant_tsc`, `montant_tva`, `montant_ttc` |

`ligne.formule_courante_id` doit pointer vers une formule **de cette ligne** : non exprimable en FK simple, à vérifier en service (ou trigger).

### Index de performance à poser en SQL manuel

Le mode `insensitive` de Prisma compile en `ILIKE`, qu'aucun index btree standard (fonctionnel ou non) ne peut servir dès qu'il y a un joker en tête (`'%…%'`). Un `@@index` simple dans `schema.prisma` ne suffit donc jamais à lui seul dès qu'une recherche insensible à la casse est en jeu — vérifier par `EXPLAIN ANALYZE`, pas par supposition.

| Table | Index | Sert |
|---|---|---|
| `ligne` | `CREATE INDEX idx_ligne_nd_lower ON ligne (LOWER(nd))` | Recherche par ND (`SF-PGD-310`) — égalité stricte, la requête compare `LOWER(nd) = LOWER($1)`, jamais `ILIKE` |
| `compte_client` | `CREATE INDEX ... USING gin (numero_compte gin_trgm_ops)` + idem `nom_client` (extension `pg_trgm`) | Recherche compte (`SF-PGD-052`) — sous-chaîne réelle (`q=` tape une partie du numéro ou du nom), un index fonctionnel `LOWER()` ne l'accélère pas ; seul un index trigramme sert un `ILIKE '%…%'` |

### Ports d'intégration
| Port | Phase 1 |
|---|---|
| `LdapPort`, `MfaPort` (DUO + TOTP) | **Réels** |
| `JadePort`, `CrmPort`, `GedPort`, `SmtpPort`, `BillingSiPort` (BSCS, GAIA) | Bouchons commutables par variable d'environnement |

### API AD réelle — détails reçus, vérification seule (12/08/2026)

Détails de l'API d'authentification AD réelle, communiqués et documentés **tels quels**, avant toute construction. `LdapProvider`/`LdapPort` (dev, OpenLDAP) **non touchés** — cette section est de la documentation de préparation, pas un changement de comportement.

```
URI    : http://192.168.31.78:8080/ci.orange.ldap/rs-interface/authenticate
Method : POST
Headers: Content-Type: application/json
Body   : { "username": "", "password": "" }

Réponse succès (forme confirmée) :
  { "check": "true", "nom": "...", "prenom": "...", "email": "...",
    "commonName": "...", "fonction": "...", "department": "...",
    "matricule": "...", "mobile": "..." }
```

**Deux points de sécurité signalés, non tranchés ici** :
- **HTTP, pas HTTPS** — mot de passe transmis en clair sur le réseau. N'est acceptable que si le futur serveur PGD de production est bien sur le même segment réseau interne qu'AD (à confirmer par la personne pilotant le projet — cf. Questions ouvertes). Rien ne doit être construit qui suppose cette réponse avant confirmation explicite.
- **Aucune authentification de service sur l'endpoint** — pas de clé API, pas de jeton, seulement les identifiants de l'utilisateur final dans le corps. Contrairement à `LDAP_BIND_DN`/`LDAP_BIND_PASSWORD` (compte de service) déjà utilisé par `LdapProvider` en dev — cette API réelle n'a apparemment pas d'équivalent.

**Deux points déjà actés, à ne pas re-décider** :
- `"check"` est une **chaîne** (`"true"`/`"false"` probable, jamais confirmé côté échec) — toute comparaison doit être stricte sur la chaîne, jamais une coercition booléenne implicite.
- **Le domaine de l'email est `orange.com` — confirmé le 18/08/2026, transmis directement par la personne pilotant le projet.** `orange.ci` reste exclusivement le domaine du socle de test dev (les 15 identités persistantes, l'OpenLDAP éphémère) — n'a jamais été et ne devient pas le domaine réel. Le principe déjà posé reste valable tel quel malgré la confirmation : ne coder aucun des deux en dur dans la logique applicative, factoriser en configuration — c'est déjà le cas (`LDAP_USER_DOMAIN`, `packages/config`), même si cette variable précise n'est aujourd'hui lue par aucun code applicatif (vérifié par recherche dans `apps/*/src`/`packages/*` — déclarée dans le schéma Zod, jamais consultée ailleurs).

**Information manquante, pas devinée** : la forme d'une réponse d'**échec** (mauvais mot de passe, identifiant inconnu) est inconnue — aucune source ne la donne, et rien ne permet de supposer qu'elle serait symétrique au succès (ex. `{"check": "false"}` seul, ou un code HTTP différent avec un corps différent, ou une exception serveur). Tentative de vérification en direct : hôte `192.168.31.78:8080` **injoignable depuis cet environnement de développement** (timeout TCP, pas de route — attendu, IP privée du réseau interne Orange, hors de portée d'un poste hors de ce réseau). Aucun identifiant de test réel pour cette API (les 15 identités de test documentées plus bas sont pour l'OpenLDAP de **dev**, sans rapport avec ce système). **À obtenir séparément** : soit la forme exacte d'une réponse d'échec communiquée directement, soit un identifiant jetable + autorisation explicite pour qu'un appel réel soit fait depuis un poste ayant accès au réseau interne.

**Script de vérification isolé, écrit mais jamais exécuté avec succès (hôte injoignable)** : scratchpad de session, jamais commité, jamais dans `apps/` — POST paramétré par variables d'environnement (`AD_TEST_URI`/`AD_TEST_USERNAME`/`AD_TEST_PASSWORD`, aucun identifiant en dur), affiche la réponse brute (statut HTTP, en-têtes, corps, tentative de parse JSON) sans aucune logique d'interprétation. À exécuter depuis un poste sur le réseau interne, avec de vrais identifiants de test, avant d'écrire une seule ligne d'intégration réelle dans `LdapProvider`.

### Joignabilité de l'API AD réelle — changement constaté (19/08/2026)

Le 12/08/2026 (section ci-dessus), `192.168.31.78:8080` était injoignable depuis cet environnement de développement (timeout TCP, pas de route). Le 19/08/2026, ce n'est plus le cas : le conteneur `api` de ce dépôt atteint réellement cet hôte.

**Constaté, pas supposé** : sonde directe depuis l'intérieur du conteneur `api` (`node -e`, `fetch()` avec des identifiants vides — même forme que `AdApiProvider.estDisponible()`, aucun identifiant réel en jeu) vers `http://192.168.31.78:8080/ci.orange.ldap/rs-interface/authenticate` → réponse HTTP réelle reçue, pas un timeout :
```json
{"code":"MethodArgumentNotValidException","status":400,"message":"Bad Request",
 "timestamp":"2026-08-19T11:17:07.408",
 "invalidParameters":[{"parameter":"password","message":"must not be blank"},
                       {"parameter":"username","message":"must not be blank"}]}
```
Troisième forme réelle d'échec observée (après `IncorrectLoginOrPassword` le même jour) — distincte : ici la requête elle-même est rejetée comme malformée (validation côté fournisseur sur des champs vides), pas les identifiants. `GET /api/health/ready` confirme `ad:true` de façon cohérente avec cette sonde, pas de façon isolée.

**Cause confirmée, pas cette infrastructure** : la personne pilotant le projet s'est connectée au réseau interne Orange entre les deux vérifications (12/08 → 19/08) — ce n'est pas un changement de configuration réseau de cet environnement de développement lui-même, qui n'a pas bougé. Le poste depuis lequel ce dépôt tourne s'est retrouvé, via cette connexion, sur un chemin réseau qui atteint `192.168.31.78:8080` ; rien dans `docker-compose.yml`/le réseau Docker/l'hôte n'a été modifié pour produire ce résultat.

**Ce que ça ne changeait pas, au moment de cette découverte** : la joignabilité réseau, seule, ne valait pas authentification réelle avec de vrais identifiants — celle-ci a été faite séparément, plus tard le même jour (19/08/2026), voir la section dédiée à la bascule `LDAP_PROVIDER=ad-api` (« REVÉRIFICATION CONTRE LE VRAI SYSTÈME — FAITE ») pour le résultat.

Verrouillé par test — `apps/api/test/ad-api-provider.spec.ts`, describe « forme réelle observée de l'API AD (400 MethodArgumentNotValidException) » : corps exact, refus fermé prouvé, `codeEchec`/`messageEchec` capturés (`"MethodArgumentNotValidException"`/`"Bad Request"`).

### `identifiantAd` = username brut, pas une adresse e-mail — confirmé et corrigé (19/08/2026)

Confirmé directement par la personne pilotant le projet : l'API AD réelle (section ci-dessus) authentifie sur le **`username`** soumis tel quel dans `{ "username": "", "password": "" }`, jamais une adresse e-mail — cohérent avec le nom même du champ, jamais remarqué comme un signal avant cette confirmation explicite.

**Vérifié avant toute construction, pas supposé** :
- **Aucune intégration réelle de cette API REST n'existe dans ce dépôt** — recherche exhaustive (`rs-interface`, `192.168.31.78`, tout provider nommé `AdRest`/`AdApi`) : zéro occurrence hors la section de documentation ci-dessus. Seul `LdapProvider` (dev, bind LDAP contre l'OpenLDAP éphémère) existe. Il n'y a donc **aucun code à citer pour « la résolution d'identifiantAd via l'API AD réelle »** — cette intégration reste à construire, comme déjà documenté.
- **Dans le code qui existe réellement** (`auth.controller.ts:64,70` → `ldap.provider.ts:15-37`) : `identifiantAd` est parsé du corps de la requête (`connexionRequeteSchema.parse(body)`), transmis tel quel à `LdapProvider.authentifier()`, et renvoyé strictement inchangé dans `UtilisateurAd` (`return { identifiantAd, nom, groupes }`, ligne 37 — jamais dérivé de `mail`/`cn`/un autre attribut). `identifiantAd` n'est donc **jamais transformé** dans le seul chemin de code qui existe aujourd'hui — cohérent avec la confirmation, même si ce chemin n'est pas celui de l'API réelle.
- **`connexionRequeteSchema`** (`packages/contracts/src/auth.ts:7`, `z.string().min(1)`) n'impose aucun format — la route de connexion elle-même acceptait déjà un identifiant brut sans modification nécessaire.

**Deux ruptures réelles trouvées en vérifiant la coexistence, pas supposées** :
1. **`preEnregistrerUtilisateurRequeteSchema.identifiantAd`** (`packages/contracts/src/admin-utilisateur.ts`) imposait `/^[^\s@]+@[^\s@]+\.[^\s@]+$/` — un `@` et un domaine à point obligatoires. Un identifiant brut (`c_afofana6`) était **rejeté** par ce schéma, donc par la route admin réelle, par `AnnuaireRechercheModal` (miroir client) et par `creerCompteEtEnroler()` (`apps/api/scripts/lib/enrolement.ts`) — le mécanisme même demandé pour créer ce compte. **Corrigé** : `estFormeIdentifiantAdValide()`, exportée depuis `packages/contracts`, accepte la forme e-mail (socle de test dev, `prenom.nom@orange.com`/`.ci`) **ou** un identifiant brut sans espace ni `@` — une seule fonction, réutilisée telle quelle par `AnnuaireRechercheModal.tsx` (jamais une réimplémentation locale du critère). Aucun test existant ne visait ce regex (recherche dédiée, négative) — aucune régression à ce niveau.
2. **`LoginScreen.tsx:46-48` — trouvé le 19/08/2026, corrigé le même jour sur décision explicite.** Complétait automatiquement tout identifiant sans `@` avec `@orange.com` avant l'envoi. Question de produit posée (« retirer la complétion, ou la garder pour le socle e-mail ? ») tranchée sans ambiguïté par la personne pilotant le projet : **deux formats coexistent légitimement, le client ne devine ni ne transforme jamais** — la saisie part telle quelle. Complétion automatique retirée ; le badge visuel `@orange.com` à droite du champ (qui promettait cette même transformation) retiré aussi, plutôt que laissé à mentir sur un comportement qui n'existe plus ; placeholder générique (« identifiant AD ») remplaçant « prenom.nom », qui présupposait la forme e-mail.

**Distinction posée pour la suite** : ceci concerne uniquement le **client** (ne jamais deviner/transformer côté navigateur). Rien n'empêche un futur besoin métier réel de normaliser un identifiant côté **serveur** (ex. si l'AD réelle s'avère insensible à la casse) — ce serait alors une règle explicite et documentée dans `LdapProvider`/le futur provider AD REST, pas une complétion silencieuse dans un composant d'écran.

**Compte créé** : `Utilisateur.identifiantAd = "c_afofana6"` (aucune transformation, confirmé par lecture directe en base), `nom = "Abou Fofana"`, `directionId` résolu vers `DSI` (catalogue réel, `direction_responsabilite`), `roles = ["ADMIN_PGD"]` (catalogue réel vérifié — `SELECT code FROM role WHERE code ILIKE '%ADMIN%'` ne renvoie que `ADMIN_PGD`, jamais `PGD_ADMIN`), `mfaMethode = TOTP`, secret généré par le même mécanisme que le reste du socle (`enrolement-comptes.ts`, `lib/enrolement.ts`).

**Vérifié en direct, dans la mesure du possible aujourd'hui** : l'API AD réelle n'existe pas encore en code et son hôte (`192.168.31.78`) reste injoignable depuis cet environnement (cf. section ci-dessus) — impossible de vérifier littéralement « le vrai flux d'authentification AD ». Vérification faite à la place contre le seul chemin d'authentification réel du dépôt (`LdapProvider`, bind LDAP) : entrée LDAP **jetable** ajoutée à l'OpenLDAP de dev (`uid=c_afofana6`, `mail: c_afofana6` — sans domaine, syntaxiquement valide côté LDAP, jamais committée dans `seed.ldif` qui reste réservé au socle de test), `POST /api/auth/login` avec `identifiantAd: "c_afofana6"` exact → bind LDAP réel réussi → `requiresMfa:true, methode:"TOTP"` → code TOTP réel généré depuis le secret déchiffré en base → `POST /api/auth/mfa/verify` → `200` → `GET /api/auth/session` confirme `identifiantAd:"c_afofana6"`, `roles:["ADMIN_PGD"]`. Entrée LDAP jetable supprimée après vérification ; la ligne `Utilisateur`/`MembreRole`/le secret TOTP restent, eux, permanents.

**Revérifié une seconde fois après le retrait de la complétion automatique — via l'écran réel, pas seulement `curl`.** Après le correctif `LoginScreen.tsx` ci-dessus, l'entrée LDAP jetable a été réajoutée le temps du test, `docker compose restart web` forcé (même gotcha déjà documenté ailleurs dans ce fichier — le watcher Next.js peut rater une édition), puis le parcours conduit par navigateur réel (Claude Browser) : champ « identifiant AD » rempli avec `c_afofana6` tel quel (plus aucune complétion visible ni appliquée), soumission → requête réseau interceptée confirme `POST /api/auth/login` a transmis exactement `c_afofana6` (la réponse `200 requiresMfa:true` n'aurait été possible qu'avec ce littéral exact, `LdapProvider` cherchant `mail=c_afofana6` — un `@orange.com` ajouté aurait échoué en 401) → code TOTP réel saisi dans l'écran MFA → `POST /api/auth/mfa/verify` → `200` → écran d'accueil affiché (« Bonjour Abou ») → `GET /api/auth/session` confirme `identifiantAd:"c_afofana6"`, `roles:["ADMIN_PGD"]`. Entrée LDAP jetable supprimée une seconde fois après ce test. `pnpm --filter @pgd/web typecheck/lint` verts après le correctif.

**QR TOTP transmis directement à l'utilisateur** (jamais laissé sur disque plus que nécessaire) — un premier envoi a été manqué par erreur (fichier supprimé avant transmission dans le même geste de nettoyage que les identités de test jetables) ; régénéré à l'identique depuis le secret déjà stocké en base (`authenticator.keyuri()` avec le même secret déchiffré — un QR encode un secret, le régénérer ne le change pas) puis transmis. **Lecon retenue** : le QR d'un compte réel destiné à un usage humain n'est jamais un artefact « jetable » au même titre qu'une identité de test — ne pas le supprimer avant confirmation explicite que la personne concernée l'a récupéré.

Sweep ciblé (packages touchés uniquement — `contracts`/`web`, aucun changement côté `apps/api/src`) : `pnpm --filter @pgd/contracts build/typecheck/lint`, `pnpm --filter @pgd/web typecheck/lint/build` verts. Pas de suite de tests dédiée à `preEnregistrerUtilisateurRequeteSchema` (confirmé par recherche, cf. ci-dessus) — rien à rejouer à ce niveau.

### Intégration réelle de l'API AD — `AdApiProvider` (SF-PGD-001, 19/08/2026)

Suite directe de la section précédente : l'intégration réelle qui manquait ("aucune intégration réelle de cette API REST n'existe dans ce dépôt") est désormais construite, derrière `LdapPort`, coexistant avec `LdapProvider` (dev, OpenLDAP) — jamais un remplacement.

**Règle non négociable — échec fermé, toujours.** Écrite en commentaire directement au-dessus de la vérification dans `apps/api/src/modules/auth/providers/ad-api.provider.ts` (`authentifier()`), pas seulement ici : seule une réponse strictement conforme au contrat documenté (statut HTTP 200 **ET** corps JSON valide **ET** `check === "true"`, comparaison stricte sur la chaîne) accorde l'accès. Tout écart — statut non-200, corps JSON malformé ou vide, `check` absent, `check` booléen (`true` ou `false`, aucun des deux n'est la chaîne `"true"`), `check: "false"`, délai dépassé, erreur réseau — retourne `null`, jamais une exception qui remonterait ni un accès accordé par défaut sur un cas non prévu.

**Coexistence, pas remplacement** : `AuthModule` fournit désormais `LDAP_PORT` (token déjà déclaré depuis Phase 2, `ldap.port.ts`, jamais utilisé pour une sélection avant ce chantier) via une factory qui choisit `LdapProvider` ou `AdApiProvider` selon `LDAP_PROVIDER` (`packages/config`, défaut `"ldap"` — même mécanique que les bouchons `CRM_PORT`/`GED_PORT` déjà déclarés pour Phase 3/7, cf. « Ports d'intégration »). Les trois points d'injection qui construisaient jusqu'ici `LdapProvider` en dur (`AuthController`, `HealthController`, `AdminUtilisateursService`) injectent maintenant `@Inject(LDAP_PORT)` — `HealthModule`, qui déclarait sa propre instance séparée de `LdapProvider` plutôt que d'importer `AuthModule`, a été corrigé au passage (sinon `/api/health/ready` aurait sondé un fournisseur différent de celui réellement utilisé par le flux de connexion). `LdapProvider`/l'annuaire OpenLDAP de dev restent inchangés et pleinement fonctionnels — `LDAP_PROVIDER` reste `"ldap"` par défaut partout (`.env.example`, `docker-compose.yml`) : rien ne bascule sans un choix explicite.

**Contrat implémenté tel que documenté** (`CLAUDE.md`, « API AD réelle ») : `POST {AD_API_URL}/ci.orange.ldap/rs-interface/authenticate`, corps `{ "username", "password" }`, `AD_API_URL`/`AD_API_TIMEOUT_MS` en variables d'environnement (jamais l'hôte/port en dur), `AbortController` pour un délai d'attente explicite (défaut 5000 ms, même valeur que le `timeout`/`connectTimeout` déjà utilisés par `LdapProvider`/ldapjs).

**`identifiantAd`** : transmis tel quel dans `{ username: identifiantAd, ... }`, jamais transformé — cohérent avec la confirmation et le correctif `LoginScreen` de la section précédente. Aucun test ne le fait passer par un format différent en entrée/sortie.

**`department`** : capturé et journalisé (`this.logger.log`) quand présent, **sans aucune logique d'assignation vers `directionId`** — la piste reste ouverte, non tranchée, non construite ici (test dédié : `department` ne fuite nulle part dans `UtilisateurAd`, qui n'a structurellement que `identifiantAd`/`nom`/`groupes`).

**Groupes** : `groupes: []` toujours — cette API n'en renvoie aucun (confirmé), et un test prouve qu'un `groupes` malicieusement présent dans la réponse simulée serait quand même ignoré. Cohérent avec la décision déjà actée (Temps 2, pré-enregistrement) : la résolution des rôles reste exclusivement `MembreRole`.

**`rechercher()`** : renvoie toujours `[]` — cette API n'expose aucune capacité de recherche (confirmé). Le pré-enregistrement passe par la saisie manuelle (`AnnuaireRechercheModal`) quand `ad-api` est sélectionné.

**`estDisponible()`** — décision de conception assumée, pas dans le contrat documenté littéralement : aucun endpoint de santé n'existe pour cette API, et sonder avec de vrais identifiants consommerait un compte réel pour de faux. Sonde donc la seule chose vérifiable sans identifiants réels — que l'API réponde HTTP (quel que soit son verdict) au contrat documenté plutôt que de timeout/refuser la connexion — un signal de joignabilité réseau pour `/api/health/ready`, jamais une validation d'identifiants.

**Vérification — serveur HTTP simulé, pas le vrai système (hôte injoignable depuis cet environnement au moment de ce chantier — cf. section « Joignabilité de l'API AD réelle » plus bas pour l'évolution de ce point précis).** `apps/api/test/ad-api-provider.spec.ts` (21 tests, état au 19/08/2026) : un serveur `node:http` local reproduit exactement la forme documentée du succès, plus chaque cas non documenté que la règle d'échec fermé doit couvrir — statut 401/500, `check` absent, `check` booléen `false`/`true`, `check: "false"`, corps vide, corps JSON malformé, corps JSON valide mais non-objet, délai dépassé (timeout), plus les trois formes d'échec réelles observées ce même jour (`IncorrectLoginOrPassword`, `MethodArgumentNotValidException` — cf. sections dédiées). Chaque cas aboutit à un `null` (puis `{statut:"ECHEC"}` après le passage au type discriminé, cf. section dédiée) prouvé par assertion, pas supposé. `apps/api/test/ad-api-provider-erreur-reseau.spec.ts` (2 tests, fichier séparé) : `AD_API_URL` pointé sur un port injoignable dès le premier appel du fichier — nécessaire car `loadEnv()` (`packages/config`) mémoïse au premier appel *par registre de modules Jest*, donc par fichier ; changer `AD_API_URL` après le premier test d'un même fichier n'a aucun effet, seul un fichier séparé (nouveau registre) le permet.

**Preuve que les tests mordent réellement, pas une hypothèse** : la comparaison centrale (`donnees.check !== "true"`) temporairement inversée (`===`) → 10 des 18 tests de `ad-api-provider.spec.ts` échouent précisément sur les cas qu'ils sont censés couvrir (y compris le cas `check: true` booléen, qui aurait alors accordé l'accès) ; réversion → 20/20 verts (les deux fichiers). Sweep complet revérifié après correctif : `apps/api` 41/41 suites, 254/254 tests ; e2e 7/7, 20/20 ; `pnpm lint`/`pnpm typecheck`/`pnpm build` verts sur les 13 packages/apps ; conteneur `api` redémarré et `GET /api/health/ready` confirme `ad:true` en conditions réelles (LDAP_PROVIDER par défaut, LdapProvider/OpenLDAP dev toujours fonctionnel après le rebranchement par token).

**`c_afofana6` non touché, comme demandé** — ni le compte ni sa vérification déjà faite (section précédente) n'ont été modifiés par ce chantier.

**REVÉRIFICATION CONTRE LE VRAI SYSTÈME — FAITE, 19/08/2026, RÉUSSIE.** `LDAP_PROVIDER` basculé sur `ad-api` dans le `.env` réellement utilisé (`AD_API_URL=http://192.168.31.78:8080` — devenu joignable depuis cet environnement ce même jour, cf. section dédiée « Joignabilité de l'API AD réelle » ci-dessous), conteneur `api` recréé pour le prendre en compte. Premier login réel contre `AdApiProvider`, effectué par la personne pilotant le projet elle-même — jamais par cette session, le mot de passe réel n'a jamais été saisi ni vu ici (cf. limite posée explicitement au moment de la bascule) : `c_afofana6`, vrai mot de passe AD, écran `LoginScreen` réel de bout en bout, code TOTP réel, session obtenue.

Vérifié après coup, directement en base, pas supposé :
- `journal_securite` : `LOGIN`/`succes=true`/`facteur=AD`, `code_echec`/`message_echec` NULL sur cette ligne de succès (2026-08-19 11:29:20 UTC).
- Deux entrées `MFA_CHALLENGE`/`succes=true`/`facteur=TOTP` — la première ~10 ms après le `LOGIN` (démarrage du défi), la seconde ~15 s plus tard (code vérifié) — l'écart correspond à une saisie humaine réelle du code, pas un script.
- `membre_role` de `c_afofana6` : une seule ligne, `ADMIN_PGD` — inchangée depuis la création du compte, seule source des `roles` embarqués dans la session ; la session obtenue porte donc `roles: ["ADMIN_PGD"]`.

**Ce que ça confirme, et ce que ça ne confirme pas.** Confirme : `AdApiProvider` authentifie réellement contre l'API AD réelle, `identifiantAd` transmis tel quel (`c_afofana6`, jamais transformé) y est bien reconnu, le contrat documenté (`check:"true"`, etc.) correspond à ce que le vrai système renvoie sur un succès, et le reste du pipeline (RBAC, MFA TOTP, session) fonctionne sans changement au-delà du fournisseur LDAP. Ne confirme pas : le comportement d'`AdApiProvider` face à d'autres formes d'échec réelles non encore rencontrées au-delà des trois déjà documentées (`IncorrectLoginOrPassword`, `MethodArgumentNotValidException`, et l'échec réseau générique) — chaque nouvelle forme observée reste à verrouiller par test au moment où elle apparaît, comme les trois précédentes.

**`LDAP_PROVIDER=ad-api` est resté la valeur active du `.env` réellement utilisé depuis cette bascule** — pas seulement pendant la vérification elle-même. Conséquence directe, rencontrée le 19/08/2026 en diagnostiquant un bug de navigation sans rapport (cf. section « Navigation sans path réel » plus bas) : toute tentative de connexion avec une identité de test du socle dev (`jean.kouassi@orange.com` et les 14 autres, qui n'existent que dans l'OpenLDAP local, jamais dans l'AD Orange réel) échoue en `401` — `AdApiProvider` refuse fermé, correctement, une identité que le vrai système ne connaît pas. Ce n'est pas un bug ; c'est le comportement attendu tant que `ad-api` reste sélectionné. **Si une future session doit retravailler avec le socle de test dev, vérifier `LDAP_PROVIDER` dans `.env` avant de chercher un bug ailleurs** — un `401 NON_AUTHENTIFIE` sur une identité `@orange.com` connue de ce fichier peut être entièrement expliqué par cette seule variable, avant d'envisager une régression applicative. Toute bascule diagnostique vers `ldap` doit être restaurée à `ad-api` après usage (recréation du conteneur `api` dans les deux sens, `docker compose up -d --force-recreate api`) — sans quoi une future vérification contre le vrai système reprendrait par erreur `LdapProvider`/l'annuaire dev.

### `JOURNAL_SECURITE` — `codeEchec`/`messageEchec` (19/08/2026)

Suite directe de la section précédente : une première forme réelle d'échec est arrivée le jour même (`{"code": "IncorrectLoginOrPassword", "status": 401, "message": "Login ou mot de passe incorrect.", "timestamp": "..."}`, jamais vue avant), avec trois demandes de vérification avant tout changement de code, traitées dans l'ordre.

**1. Échec fermé déjà correct sur ce cas précis, vérifié pas supposé.** `reponseHttp.status !== 200` (401) rejette avant même de lire `check` — ce corps exact, ajouté tel quel comme cas de test dédié (`ad-api-provider.spec.ts`, describe « forme réelle observée »), était déjà refusé avant tout changement de code.

**2. `JOURNAL_SECURITE.evenement=LOGIN` portait-il déjà un statut d'échec ?** Oui, vérifié : `succes: Boolean` est une colonne générique de `JournalSecurite` (pas propre à `LOGIN`), et `LOGIN` représente déjà les deux issues aujourd'hui (`AuthController.login()` écrivait `succes:true` en cas de succès, `succes:false` en cas d'échec, avant même ce chantier). Un type d'événement ne correspond donc pas à un seul résultat — `ACCES_NON_PROVISIONNE` est l'unique exception, posée pour une étape *distincte* du parcours (après l'authentification AD), pas pour une sous-cause d'échec de la même étape.

**Décision actée, sur cette base — Option B, pas de nouveau type d'événement.** Deux colonnes génériques ajoutées à `JournalSecurite` (`codeEchec`/`messageEchec`, migration `20260819120000_journal_securite_code_message_echec`) plutôt qu'une valeur d'enum `ECHEC_AUTHENTIFICATION_AD` — un type par étape distincte du parcours, jamais un type par cause d'échec rencontrée, cohérent avec ce que la structure existante fait déjà. Jamais peuplées sur `succes:true` — vérifié par test, pas supposé.

**`LdapPort.authentifier()` change de forme — même motif que `RbacResolutionService.resoudre()` (`ResolutionRbac`, `AUTORISE`/`NON_PROVISIONNE`), demandé explicitement plutôt qu'une exception à intercepter.** `ResultatAuthentificationAd` (`ldap.port.ts`) :
```ts
type ResultatAuthentificationAd =
  | { statut: "AUTHENTIFIE"; utilisateur: UtilisateurAd }
  | { statut: "ECHEC"; codeEchec?: string; messageEchec?: string };
```
`codeEchec`/`messageEchec` ne participent JAMAIS à la décision d'accès elle-même (extraits du corps HTTP *avant* le bloc à échec fermé dans `AdApiProvider`, mais purement pour journalisation — le bloc d'échec fermé reste géographiquement et logiquement le même qu'avant, inchangé dans sa structure de décision). `LdapProvider` (annuaire dev) implémente le même type mais ne renseigne jamais `codeEchec`/`messageEchec` — un bind LDAP échoué n'a pas de détail structuré comparable, « et ce n'est pas grave » (confirmé explicitement). `AuthController.login()` transmet `resultatAd.codeEchec`/`messageEchec` à `journal.consigner()` sur la branche `ECHEC`, jamais renvoyés au client HTTP (le message générique « Identifiants invalides. » reste inchangé — même discipline qui empêche déjà de distinguer identifiant inconnu de mot de passe invalide au niveau HTTP, pour ne pas faciliter une énumération de comptes).

**Un appelant cassé par le changement de signature, trouvé par le typecheck, pas oublié** : `auth-provisionnement.e2e-spec.ts` substitue `LdapProvider` par un faux (`authentifier()` toujours réussi) pour tester le pré-enregistrement Temps 2 sans dépendre d'un annuaire réel — son faux retournait l'ancienne forme (`UtilisateurAd | null`), corrigé vers `{statut: "AUTHENTIFIE", utilisateur: {...}}`.

**Vérifié dans les deux sens, pas seulement supposé** (exigence explicite : « preuve que codeEchec/messageEchec sont bien capturés pour AdApiProvider et bien absents pour LdapProvider ») :
- `ad-api-provider.spec.ts` : le cas réel `IncorrectLoginOrPassword` vérifie désormais `codeEchec`/`messageEchec` capturés avec les valeurs exactes, en plus du refus ; un cas supplémentaire (`check:"false"` + `code`/`message` présents à 200) prouve la capture indépendamment du statut HTTP ; tous les autres cas d'échec (401/500 génériques, corps vide, JSON malformé, timeout) vérifient explicitement `codeEchec`/`messageEchec` **absents** — rien n'est inventé quand la source ne fournit rien.
- `ldap-provider.integration.spec.ts` (Testcontainers, LDAP réel) : les deux cas d'échec (mauvais mot de passe, identifiant inconnu) vérifient désormais `{statut: "ECHEC"}` strictement — pas de `codeEchec`/`messageEchec`, même absents implicitement (`toEqual` exact, pas un `toMatchObject` partiel qui les laisserait passer inaperçus si un jour ajoutés par erreur).
- **`apps/api/test/journal-securite-service.integration.spec.ts` (nouveau, intégration réelle Postgres)** : ferme la boucle entre « le provider renvoie le bon détail » et « la base l'écrit réellement » — un fichier séparé n'existait pas avant ce tour (`JournalSecuriteService` n'avait aucune couverture dédiée). Trois cas : `codeEchec`/`messageEchec` persistés avec les valeurs exactes quand fournis ; `NULL` en base (jamais chaîne vide ni littéral `"undefined"`) quand omis ; jamais écrits sur `succes:true` (documente le comportement réel du service seul, pas une garantie supplémentaire construite ici — `AuthController` ne les fournit de toute façon jamais sur ce chemin).

**Preuve que les tests mordent réellement, deux fois** : (1) comparaison `check !== "true"` déjà re-vérifiée dans la section précédente ; (2) `codeEchec`/`messageEchec` retirés temporairement de `JournalSecuriteService.consigner()` → 2 des 3 tests de `journal-securite-service.integration.spec.ts` échouent précisément (persistance et lecture par `codeEchec`) ; réversion → 3/3 verts.

**Vérifié en direct sur le conteneur réel, pas seulement en test — via `LdapProvider` (annuaire dev), PAS via `AdApiProvider`.** Précision volontaire, pour qu'une relecture future de ce paragraphe ne le confonde jamais avec une vérification d'`AdApiProvider` en conditions réelles — ce qu'il n'est pas, cohérent avec la limite déjà posée juste au-dessus (« REVÉRIFICATION CONTRE LE VRAI SYSTÈME — EN ATTENTE »). Migration appliquée dans le conteneur `api` (`prisma migrate deploy` + `prisma generate`, cf. incident dédié ci-dessous — jamais un `prisma generate` hôte seul), conteneur redémarré, `GET /api/health/ready` confirme `ad:true`. Tentative de connexion réelle avec un mauvais mot de passe (`jean.kouassi@orange.com`, `LDAP_PROVIDER` laissé à son défaut `"ldap"` — `AdApiProvider` n'était pas sélectionné pour ce test) → `401 NON_AUTHENTIFIE` côté client (message générique inchangé) → ligne `journal_securite` lue directement en base : `evenement=LOGIN, succes=f, code_echec=NULL, message_echec=NULL` — confirme que `LdapProvider`, et seulement lui, n'invente rien en conditions réelles. Aucune conclusion à en tirer sur le comportement d'`AdApiProvider` contre un système réel : cette preuve-là reste entièrement à faire (cf. section précédente).

Sweep complet : `apps/api` 42/42 suites (259/259 tests, `journal-securite-service.integration.spec.ts` compris) ; e2e 7/7 (20/20) ; `pnpm lint`/`pnpm typecheck`/`pnpm build` verts sur les 13 packages/apps.

**Reste hors périmètre, signalé plutôt que construit** : `journalSecuriteVueSchema`/`AuditSecuriteScreen` (`packages/contracts/src/audit.ts`) n'exposent pas encore `codeEchec`/`messageEchec` — les deux colonnes sont capturées et persistées, mais actuellement invisibles depuis l'écran d'audit sécurité admin. Pas construit ici (portée demandée : migration, implémentation, tests) — un futur besoin d'affichage passera par ces deux points précis, une fois demandé explicitement.

**`timestamp` du corps d'échec — confirmé non consommé, resté tel quel.** Aucune ambiguïté de fuseau à traiter tant que rien ne le lit (`YYYY-MM-DDTHH:mm:ss.SSS`, sans `Z` ni offset — noté pour référence future si ce champ devient un jour pertinent, ex. corrélation temporelle avec les logs du système AD lui-même).

### Incident — `prisma generate` sur l'hôte ne met jamais à jour le Prisma Client d'un conteneur dev (trouvé Phase « Pré-enregistrement des utilisateurs AD », Temps 1)

**Symptôme** : après une migration de schéma (`ALTER TABLE ... ADD COLUMN`) appliquée et un `prisma generate` réussi sur l'hôte, `docker compose restart api` fait repartir le conteneur mais `nest start --watch` échoue aussitôt en compilation avec des erreurs `TS2551`/`TS2339` sur les champs tout juste ajoutés (`Property 'serviceId' does not exist on type...`) — alors que le même code typecheck sans erreur sur l'hôte.

**Cause, confirmée en lisant les logs du conteneur, pas supposée** : le Prisma Client généré (`node_modules/.pnpm/@prisma+client.../node_modules/@prisma/client`) vit dans le volume `node_modules` **propre à chaque conteneur** (même famille que l'incident `EACCES` ci-dessous — ces volumes ne sont jamais partagés avec l'hôte ni entre conteneurs). `prisma generate` régénère ce client à l'endroit où il est exécuté seulement : sur l'hôte, il ne touche jamais le volume `pgd_node_modules_api`/`pgd_node_modules_worker`. Un simple `docker compose restart` ne suffit donc jamais après un changement de schéma — le conteneur redémarre avec l'ancien client, désormais désynchronisé du schéma réel.

**Geste** : après toute migration de schéma, régénérer le client **dans chaque conteneur qui dépend de `@pgd/database`**, avant tout `restart` :
```bash
docker compose exec api sh -lc "cd /repo && pnpm --filter @pgd/database exec prisma generate"
docker compose exec worker sh -lc "cd /repo && pnpm --filter @pgd/database exec prisma generate"
docker compose restart worker   # api recompile déjà en watch, redémarrage pas systematiquement nécessaire s'il a survécu à l'erreur de compilation ; worker, lui, ne watch pas les dépendances de la même façon et doit être relancé
```
Distinct de l'incident « rebuild `dist`/`.d.ts` » déjà documenté ci-dessous (celui-ci porte sur le **code compilé** de `packages/database`, propagé par le bind-mount source ; celui-ci porte sur le **client Prisma généré**, un artefact `node_modules` jamais partagé) — les deux se ressemblent (« un changement dans `packages/database` n'apparaît pas dans le conteneur ») mais ont des causes et des gestes différents, à ne pas confondre.

### Incident récurrent — `EACCES` sur les volumes `node_modules` en conteneur dev

Deuxième occurrence (Phase 4 sur `api`, Phase 6 sur `worker`) du même incident — à ne plus redécouvrir en Phase 7/8.

- **Symptôme** : `pnpm install` exécuté à l'intérieur d'un conteneur `docker-compose.override.yml` (mode dev, `user: node`) échoue avec `EACCES: permission denied, mkdir '/repo/node_modules/.pnpm/...'`.
- **Cause** : les volumes nommés `pgd_node_modules_*` (`pgd_node_modules_root`, `pgd_node_modules_api`, `pgd_node_modules_worker`, etc.) sont créés au premier démarrage du conteneur et prennent l'UID du processus qui les initialise. Si ce premier remplissage se fait sous un autre UID que `node` (ex. étape de build image en `root`, ou un volume recréé après ajout d'une dépendance), les écritures suivantes en tant que `node` sont refusées.
- **Geste** : exécuter l'install en root, puis rendre la propriété à `node` :
  ```bash
  docker compose exec -u root <service> sh -lc "cd /repo && pnpm install --filter <package>... && chown -R node:node /repo/node_modules /repo/apps/<service>/node_modules /repo/packages/*/node_modules"
  docker compose restart <service>
  ```
- **Ne pas** : supprimer/recréer les volumes par réflexe — l'incident se résout par un `chown`, pas par une perte de cache d'installation.

**Incident apparenté (Phase 7, reproduit en Phase 8)** — un `pnpm install` exécuté ainsi (root, dans le conteneur Linux) contre le dépôt monté en bind (`.:/repo`, partagé avec l'hôte Windows) peut corrompre les symlinks `node_modules` d'un **package partagé aussi consommé directement sur l'hôte** (ex. `packages/messaging`, utilisé à la fois par les conteneurs et par `pnpm --filter @pgd/messaging build` lancé depuis PowerShell/Git Bash). Symptôme : `tsc -p tsconfig.json` échoue en hôte avec `Cannot find module '...\node_modules\typescript\bin\tsc'` alors que le lien existe (`ls`/`readlink` le montrent) — un symlink POSIX créé côté conteneur Linux ne se résout pas toujours côté Windows natif à travers le partage de fichiers Docker Desktop. Geste : `rm -rf packages/<nom>/node_modules && pnpm install` **depuis l'hôte** (jamais depuis le conteneur) pour ce package précis.

**Déclencheur précis identifié en Phase 8** : ce n'est pas seulement un `pnpm install` ciblant directement `packages/messaging` qui casse le symlink — un `pnpm install --filter @pgd/api...` (le suffixe `...` inclut les dépendances de workspace) lancé dans le conteneur `api` relie **aussi** `@pgd/messaging` en cascade, silencieusement, sans qu'aucune commande n'ait mentionné ce package. Symptôme observé cette fois côté **Jest** (pas `tsc`) : `Cannot find module 'amqplib' from '.../packages/messaging/dist/connexion.js'`, alors que `ls packages/messaging/node_modules/amqplib` affiche des fichiers réels — le symlink pointait vers un chemin relatif (`../../../node_modules/.pnpm/...`) au lieu d'un chemin absolu, cassé après la réécriture côté conteneur. Retenir : **tout** `pnpm install --filter <app>...` lancé dans un conteneur dev est suspect pour **tous** les `packages/*` dont `<app>` dépend, pas seulement celui explicitement visé par `--filter`.

**Incident apparenté (Phase 8)** — même famille, déclenchement différent : `docker compose up -d worker api` échoue avec `failed to create symlink: ... file exists` sur un volume `pgd_node_modules_messaging` **fraîchement créé** (pas un ancien volume corrompu). Cause : au premier montage d'un volume nommé vide, Docker le peuple depuis le contenu de l'image à ce chemin ; si ce peuplement est interrompu ou entre en conflit (bind mount hôte Windows + image Linux), la copie initiale laisse un mélange fichier réel / lien symbolique incohérent. Geste : `docker compose down <services>` puis `docker volume rm <volume>` puis `docker compose up -d <services>` — le volume vide se repeuple proprement depuis l'image reconstruite. Ne pas tenter de réparer le contenu du volume à la main.

**Leçon distincte (Phase 8)** — `docker compose restart <service>` ne rejoue **jamais** le `Dockerfile` : il relance le même conteneur depuis la même image. Un correctif posé dans le `Dockerfile` (ex. `apt-get install procps`, déjà présent dans `apps/worker/Dockerfile` pour fournir `ps` à `nest start --watch` en mode watch) ne s'applique qu'aux conteneurs **recréés** depuis une image reconstruite. Un conteneur démarré avant l'ajout de cette ligne continue de tourner sans `ps`, et `nest start --watch` plante au premier redémarrage à chaud (`Error: spawn ps ENOENT`, `tree-kill` interne à l'incapacité de lister les PID enfants) sans rapport avec le code applicatif. Geste : `docker compose build <service> && docker compose up -d <service>` — jamais `restart` seul — après **tout** changement de `Dockerfile`, même correctif déjà documenté dans une phase antérieure.

**Rappel** — ajouter une dépendance npm à un `apps/*/package.json` (ex. `pdfkit` en Phase 8) et lancer `pnpm install` **depuis l'hôte** ne l'installe pas dans le `node_modules` du conteneur dev : c'est un volume nommé séparé (`pgd_node_modules_api`), non partagé avec l'hôte. Symptôme en conteneur : `Cannot find module 'pdfkit'` (TS2307) en compilation watch, alors que `pnpm typecheck`/`build` sur l'hôte est vert. Geste : appliquer le geste `EACCES` ci-dessus (install en root dans le conteneur concerné, puis `chown`) après tout ajout de dépendance, avant de considérer la vérification live terminée.

**`web` (Next.js dev, bind-mount) — le file-watcher peut silencieusement rater un changement (trouvé Phase 9.3, audit visuel `LoginScreen`)** : plusieurs éditions consécutives de `LoginScreen.tsx` depuis l'hôte n'ont déclenché qu'une recompilation partielle par `next dev` dans le conteneur — mtime et contenu du fichier confirmés identiques hôte/conteneur (`stat`/`grep` direct dans le conteneur), donc pas un problème de bind-mount cassé ni de symlink (familles d'incidents déjà documentées ci-dessus) : le HTML servi ne portait tout simplement pas les blocs JSX ajoutés (vérifié par `curl` direct sur le HTML rendu, pas par supposition). Aucune erreur de compilation dans `docker compose logs web`, juste un rendu périmé. Geste : `docker compose restart web` force une recompilation complète et suffit à resynchroniser — pas besoin de `build`/`up` (contrairement à un changement de `Dockerfile`, cf. leçon Phase 8 ci-dessus, qui elle exige une image reconstruite). Si un écran de l'audit visuel en cours semble ignorer une édition qui vient d'être faite, vérifier le HTML réellement servi (`curl`) avant de conclure à un défaut de code.

### `ldap-provider.integration.spec.ts` — Testcontainers ne tourne pas dans le conteneur `api` (trouvé en revue, Phase 9.2)

**Symptôme** : `pnpm --filter @pgd/api test` lancé via `docker compose exec api ...` échoue systématiquement sur `LdapProvider (OpenLDAP réel via Testcontainers)` (3 tests) avec `Could not find a working container runtime strategy` — les 163 autres tests passent. Signalé une fois comme « échec sans rapport » avant vérification explicite du contraire ; ne plus reproduire cette approximation.

**Cause, confirmée, pas supposée** : ce test (introduit Phase 2, `ded70d5`) démarre son propre serveur OpenLDAP éphémère via Testcontainers (`GenericContainer(...).start()`) — mécanisme *Docker-outside-of-Docker*, qui exige un accès au démon Docker de l'hôte. Ni `docker-compose.yml` ni `docker-compose.override.yml` ne montent `/var/run/docker.sock` (ou l'équivalent Windows) dans le conteneur `api` — vérifié par recherche, aucune occurrence. Exécuté depuis l'intérieur du conteneur `api`, Testcontainers n'a donc littéralement aucun démon à qui parler. Ce n'est pas un problème d'installation ni de version.

**Ce n'est pas un problème d'environnement durable** : exécuté **depuis l'hôte** (`pnpm --filter @pgd/api test`, PowerShell/Git Bash, pas `docker compose exec`), ce même test passe (vérifié en direct) — le poste de dev a Docker Desktop, donc un démon Docker directement joignable, et `.env` à la racine pointe déjà `DATABASE_URL`/`REDIS_URL` sur `localhost` (ports publiés) pour ce cas d'usage. Suite complète depuis l'hôte : **166/166**, LdapProvider compris.

**Geste** : pour toute vérification qui doit inclure `ldap-provider.integration.spec.ts` (recette, sweep de fin de phase), lancer `pnpm --filter @pgd/api test` **depuis l'hôte**, pas via `docker compose exec`. Pour un sweep ciblé qui ne touche pas `LdapProvider`, `docker compose exec api ...` reste légitime — mais alors dire explicitement « N/166, LdapProvider exclu (Testcontainers, conteneur sans accès au démon Docker) », jamais un total qui laisse croire à un échec sans rapport ou à une régression.

**Ne pas** : monter `/var/run/docker.sock` dans le conteneur `api` pour « corriger » ceci en conteneur — donner à un conteneur applicatif le contrôle du démon Docker de l'hôte est un changement de posture de sécurité, pas un correctif de test, et ne serait pris qu'après arbitrage explicite.

### Git Bash + RabbitMQ — une variable d'environnement commençant par `/` se fait mangler en chemin Windows, et le symptôme est un hang, pas une erreur claire (trouvé Phase 10.6septies, clôture)

**Symptôme** : un `pnpm --filter @pgd/api test:e2e -- mint-session-tmp.e2e-spec.ts` lancé **depuis Git Bash** (`export RABBITMQ_VHOST="/pgd"` puis la commande) échoue en ~10 s sur `Expected ConnectionOpenOk; got <ConnectionClose channel:0>` (amqplib) — mais le process Jest ne rend jamais la main : « Jest did not exit one second after the test run has completed », `pnpm` reste bloqué indéfiniment (observé une fois pendant plus d'une heure avant d'être tué manuellement). Rejouer la même commande plusieurs fois donne le même échec rapide suivi du même hang — déterministe, pas un flake réseau.

**Cause, confirmée par les logs RabbitMQ, pas supposée** : `docker compose logs rabbitmq` montre `Error on AMQP connection ... vhost C:/Program Files/Git/pgd not found` — MSYS (le runtime POSIX sous Git Bash) réécrit automatiquement toute valeur d'argument/variable qui *ressemble* à un chemin POSIX absolu (commençant par `/`) en chemin Windows avant de la transmettre au processus `node.exe` lancé (qui n'est pas un binaire MSYS). `RABBITMQ_VHOST=/pgd`, exporté depuis Git Bash, devient donc `C:/Program Files/Git/pgd` au moment où `apps/api` le lit via `process.env.RABBITMQ_VHOST` — un vhost qui n'existe évidemment pas côté RabbitMQ, qui ferme la connexion pendant la négociation. Le même piège affecte n'importe quelle commande `rabbitmqctl -p /pgd` lancée directement en Git Bash (contournable avec `MSYS_NO_PATHCONV=1` préfixé, déjà rencontré et documenté ailleurs dans ce fichier pour d'autres commandes) — mais pour une variable d'environnement exportée en amont d'une commande `pnpm`/`node`, ce correctif ponctuel n'aide pas.

**Le hang, pas seulement l'échec de connexion, est la vraie surprise** : `ConnexionRabbitMQ.connecter()` (`packages/messaging/src/connexion.ts`) est appelé par le module `RabbitMQModule` d'`apps/api` au démarrage de l'app Nest de test — l'échec de connexion initiale laisse un handle asynchrone ouvert (écouteur de socket, timer de reconnexion `planifierReconnexion`) que Jest ne peut jamais fermer proprement, d'où le blocage indéfini plutôt qu'un échec net. Un symptôme de connexion ("ConnectionClose") a donc pour conséquence pratique un symptôme totalement différent ("le process ne répond plus") — ne pas chercher la cause dans Jest ou dans le test lui-même quand ce motif apparaît, remonter directement aux logs du broker.

**Geste** : pour toute commande qui exporte des variables d'environnement RabbitMQ (ou toute variable dont la valeur commence par `/`) avant de lancer `pnpm test`/`test:e2e` depuis l'hôte, utiliser **PowerShell**, pas Git Bash — `$env:RABBITMQ_VHOST = "/pgd"` n'est jamais réécrit, PowerShell n'a pas de couche MSYS. Si un hang de ce type se reproduit malgré tout : vérifier `docker compose logs rabbitmq --tail 50` en premier réflexe, pas les logs Jest — le message d'erreur exact (`vhost ... not found`) apparaît là, jamais dans la sortie du test.

**Ne pas** : diagnostiquer un hang de ce type en supposant une lenteur réseau ou une surcharge Docker et le relancer en boucle — le même hang se reproduit indéfiniment tant que la variable reste mal transmise, une nouvelle tentative depuis le même shell ne change rien.

### Trois routes de liste paginée renvoyaient `data: null` — enveloppe HTTP silencieusement vidée (trouvé en construisant l'API client d'AdminScreen, Phase 9.2)

**Symptôme, jamais observé en test** : `GET /api/demandes` (`DemandesController.lister`), `GET /api/audit/securite` (`AuditController.journalSecurite`) et `GET /api/notifications` (`NotificationsController.lister`) renvoyaient chacun un corps HTTP `{ data: null, error: null, meta: { total: N } }` — `meta.total` correct, mais la liste elle-même absente. Aucun des trois n'a de test qui les appelle réellement en HTTP (`supertest`) : tous les tests existants appellent le service directement (`DemandeService.lister(...)`), jamais le contrôleur via une requête — ce qui explique que le défaut soit resté invisible malgré une suite verte.

**Cause, confirmée en lisant `ResponseEnvelopeInterceptor`, pas supposée** : les trois contrôleurs retournaient `{ demandes, meta: { total } }` / `{ entrees, meta }` / `{ notifications, meta }` — une clé de liste **autre que `data`**, à côté de `meta`. `estDejaEnveloppe()` (`response-envelope.interceptor.ts`) ne vérifie que la **présence** d'une clé `data`, `error` ou `meta` pour décider qu'un contrôleur a déjà enveloppé sa réponse lui-même ; dès que `meta` est présent, l'intercepteur fait `{ data: valeur.data ?? null, error: valeur.error ?? null, meta: valeur.meta ?? null }` — `valeur.data` n'existant pas, la liste réelle (`demandes`/`entrees`/`notifications`) est purement et simplement perdue.

**Corrigé** en alignant les trois contrôleurs sur la forme que l'intercepteur attend réellement : `return { data: demandes, meta: { total } }` (idem `entrees`/`notifications`) — zéro changement à l'intercepteur partagé, qui gérait déjà correctement ce cas (son propre commentaire le dit : « Un contrôleur peut déjà retourner `{ data, meta }` »). Vérifié : `pnpm --filter @pgd/api typecheck` vert, et un nouveau test unitaire `apps/api/test/response-envelope.interceptor.spec.ts` (aucune dépendance DB/Redis, isole l'intercepteur seul) pin le contrat : une réponse `{ data, meta }` traverse intacte ; une réponse `{ <autre-clé>, meta }` — la forme fautive exacte trouvée ici — perd sa liste (test qui documente le piège plutôt que d'empêcher l'intercepteur de le faire, cf. « Ne pas » ci-dessous).

**Ne pas** : élargir `estDejaEnveloppe()` pour deviner/fusionner une clé de liste arbitraire — l'intercepteur est un composant partagé et générique (tout contrôleur de l'API le traverse) ; lui apprendre à reconnaître des formes de contrôleur ad hoc serait exactement le genre de couplage fragile que la convention `{ data, meta }` existe pour éviter. Le contrat reste simple et à la charge du contrôleur : **toute réponse paginée doit être `{ data: [...], meta: { total } }`**, jamais une autre clé.

**Balayage complet effectué** (pas seulement les 3 routes trouvées en construisant AdminScreen) : les 18 fichiers `*.controller.ts` d'`apps/api/src` ont été inspectés — chaque type de retour `Promise<{...}>` inline et chaque type nommé (`TachesListeReponse`, `PaliersListeReponse`, `ImportCrmReponse`, `SanteDetail`, `CompteAvecLignes`, `FormulesDeLigne`, etc.). Aucune autre route ne porte une clé `meta` à côté d'une clé de liste autre que `data`. Deux formes cohabitent légitimement et ne se confondent jamais avec le piège : le total porté **dans** le DTO lui-même (`{taches, total}`, `{paliers, trous}` — jamais de clé `meta`, `estDejaEnveloppe()` ne se déclenche pas dessus), et `{data, meta}` — la forme correcte.

**Généralisé en test structurel, pas seulement pin des 3 cas connus** : `apps/api/test/envelope-contract.spec.ts` lit l'AST réel (compilateur TypeScript, déjà une dépendance) de chaque `*.controller.ts`, extrait tout `return { ... }` littéral, et échoue si une réponse porte `meta` sans `data` — sur **n'importe quelle route future**, sans table à tenir à jour. Contrairement à `guard-coverage.spec.ts` (une table route → guards attendus est nécessaire faute de décorateur marquant « ceci est une route de liste »), l'invariant « `meta` implique `data` » est universel et vérifiable sans connaître à l'avance quelles routes existeront — structurellement supérieur à une table pour ce cas précis, mais ce n'est pas un pattern général : un contrôle qui a besoin de savoir QUELLES routes sont concernées (guards de portée) reste nécessairement porté par une table, l'AST ne sait pas deviner une intention métier. Preuve que le test mord réellement : `{ demandes, meta }` réintroduit temporairement dans `DemandesController.lister` → échec avec fichier/ligne exacts ; revert (diff vide confirmé) → vert. Un test de non-régression qui n'a jamais été vu échouer sur le défaut qu'il prétend couvrir n'est qu'une hypothèse.

### `si-service.integration.spec.ts` — un test peut échouer parce que le conteneur `worker` réel consomme le message avant l'assertion (trouvé en sweep de fin d'étape, Phase 9.2)

**Symptôme** : `pnpm --filter @pgd/api test` lancé depuis l'hôte échoue de façon intermittente sur « rejeu manuel sous le plafond republie si.push sans lever d'erreur » — attend `{ etat: "ERREUR" }`, reçoit `{ etat: "ENVOYE" }`. Les 172 autres tests passent.

**Cause, confirmée en désactivant la variable, pas supposée** : ce test appelle `SiService.rejouerManuel()` directement (pas de mock de messagerie) contre la **vraie** connexion RabbitMQ (`ConnexionRabbitMQ`, `@pgd/messaging`) — il republie un message réel sur `si.push`. Si le conteneur `worker` du `docker compose` de dev tourne au même moment (cas normal : il tourne en continu pendant le développement), son consumer réel (`SiPushService`, `prefetch=1`) peut traiter ce message et faire passer `siEtat` à `ENVOYE` en base **avant** que l'assertion du test ne s'exécute — une vraie course entre le test et un consumer de production actif sur la même base/le même broker, pas une corruption de données. Reproduit dans les deux sens : `docker compose stop worker` puis relancer seulement ce fichier → **5/5 vert** ; `worker` actif → échec intermittent selon la latence du consumer.

**Ce n'est pas lié aux changements de cette session** : ni `si.service.ts` ni ce fichier de test n'ont été touchés depuis la Phase 4 (`git log` vérifié) — le défaut est pré-existant, simplement pas encore rencontré ou documenté.

**Geste** : si ce test échoue seul (les 172 autres verts) pendant un sweep depuis l'hôte, revérifier `docker compose ps worker` avant de le lire comme une régression — un `worker` actif suffit à l'expliquer. Pour une vérification déterministe de ce fichier précis, `docker compose stop worker` le temps du run, puis `docker compose start worker` ensuite (jamais `down`/`rm`, pas de perte d'état à ce prix).

**Ne pas** : mocker `ConnexionRabbitMQ` dans ce test pour le rendre silencieux vis-à-vis du worker — le test vérifie explicitement (commentaire du fichier) que le rejeu republie réellement sur le broker, un mock rendrait cette assertion vide de sens. Le bon correctif, s'il est fait un jour, isolerait le test (routing key ou file dédiée à l'exécution de test) plutôt que de couper la publication réelle — décision hors du périmètre de cette session, non prise ici.

**Ne pas confondre avec `ldap-provider.integration.spec.ts` ci-dessus — deux natures différentes, pas la même dette.** `ldap-provider` est un problème de **contexte d'exécution** (conteneur vs hôte) : déterministe et vert à 100 % depuis l'hôte, la couverture existe réellement, elle n'est simplement pas disponible dans tous les contextes de lancement. `si-service` est une **vraie course de concurrence** contre un processus tiers réel, reproductible peu importe où `pnpm test` est lancé (hôte ou conteneur) tant que `worker` tourne — une dette non résolue, pas une question de disponibilité. Les deux restent en l'état (non « réparés »), mais ne pas les traiter comme une seule et même catégorie de problème dans un futur compte-rendu.

### `GET /api/audit/securite` totalement inatteignable depuis la Phase 8 — collision d'ordre de routes (trouvé en vérifiant le périmètre ADMIN_PGD avant l'écran de consultation d'audit, Phase 9.2)

**Symptôme, jamais observé avant vérification en direct** : `curl` authentifié (n'importe quel rôle, y compris `ADMIN_PGD`) sur `GET /api/audit/securite` renvoyait systématiquement `500 ERREUR_INTERNE`, jamais le journal de sécurité ni un `403`. Le message serveur exact : `Inconsistent column data: Error creating UUID, invalid character... found "s" at 1`.

**Cause, confirmée en lisant les logs de boot Nest, pas supposée** : `AuditController` déclarait `@Get(":demandeId")` (`journalDemande`) **avant** `@Get("securite")` (`journalSecurite`). Express/Nest matchent les routes dans leur **ordre d'enregistrement** — une route à paramètre déclarée en premier absorbe tout, y compris un segment qui correspond textuellement à une route littérale déclarée après elle. `GET /api/audit/securite` était donc systématiquement intercepté par `journalDemande("securite")`, qui plante en essayant de parser `"securite"` comme un UUID. **Conséquence directe pour la sécurité** : `@Roles("ADMIN_PGD")` sur `journalSecurite` n'exécutait donc **jamais**, ni pour un admin ni pour quiconque — pas une fuite (personne ne pouvait rien lire, y compris un admin légitime), mais une route de code mort depuis son introduction en Phase 8 (`PGD-071`/`SF-PGD-006`), invisible à tous les tests existants (aucun n'appelle ces routes en HTTP réel — même défaut de couverture que le bug d'enveloppe ci-dessus).

**Corrigé** en réordonnant : `securite` (littéral) déclaré avant `:demandeId` (paramètre) dans le fichier. Aucun changement de logique, seulement l'ordre des méthodes. `:demandeId/export` n'était pas concerné (profondeur différente, deux segments contre un). Vérifié en direct dans les deux sens : session non-`ADMIN_PGD` → `403 ACCES_REFUSE` propre ; session `ADMIN_PGD` → `200` avec les entrées réelles (`meta.total: 344` au moment du test) ; `GET /api/audit/{demandeId}` toujours fonctionnel après le réordonnancement.

**Généralisé en test structurel** (même méthode AST que `envelope-contract.spec.ts`) : `apps/api/test/route-order.spec.ts` extrait, pour chaque contrôleur et chaque verbe HTTP, les routes dans leur ordre de déclaration réel, et échoue si une route à paramètre précède une route littérale de même profondeur — sur n'importe quel contrôleur futur, sans table à tenir à jour. Preuve que le test mord réellement : l'ancien ordre restauré temporairement → échec avec fichier/ligne exacts (`audit.controller.ts:26` avant `:32`) ; fix restauré → vert.

**Ne pas** : supposer qu'un test qui appelle le service directement (`AuditService.journalSecurite(...)`) aurait suffi à couvrir ceci — le bug est au niveau du **routage HTTP**, invisible tant qu'aucun test ne passe par une vraie requête. C'est la troisième fois cette phase que ce défaut de couverture précis (service testé, route jamais appelée en HTTP réel) cache un bug réel (cf. enveloppe HTTP ci-dessus) — un signal que la convention de test de ce projet (service-level, jamais HTTP réel) a un angle mort structurel, pas seulement trois coïncidences isolées.

### R6 (re-routage) est bien plus étroit que la question initialement posée ne le supposait (trouvé en construisant le déclencheur `Modifier` de `DossierDetailScreen`, Phase 9.2)

**Prémisse initiale, non vérifiée jusqu'à ce tour** : la question ouverte de longue date supposait qu'un « changement de montant ou de circuit » pouvait survenir sur un dossier déjà partiellement décidé, et que le mécanisme méritant vérification était la **préservation** des décisions déjà prises au milieu d'un re-routage. Les deux volets de cette prémisse sont faux, confirmés en lisant `packages/contracts/src/demande.ts` et `demande-workflow.service.ts`, pas supposés.

**`circuit` n'est pas modifiable par `PATCH /api/demandes/{id}`** : `modifierDemandeRequeteSchema = creerDemandeRequeteSchema.omit({ circuit: true }).partial()` — le champ est structurellement absent du schéma de requête, pas seulement ignoré s'il est envoyé. Le segment et le routage en dépendent, donc le circuit ne se change jamais après création.

**Le montant n'est jamais un champ direct de cette route** : `montantTtc` ne fait pas partie de `modifierDemandeRequeteSchema` non plus — le montant découle exclusivement des lignes retenues (`PUT /api/demandes/{id}/lignes`, `R18`), jamais d'une valeur saisie sur la demande elle-même.

**La fenêtre d'éligibilité de R6 est binaire, pas un état intermédiaire à préserver** : `DemandeWorkflowService.verifierAucuneDecision()` (partagée par `modifier`/`rappeler`/`abandonner`) lève `422 DECISION_DEJA_PRISE` dès qu'**une seule** étape porte `etat` dans `[APPROUVEE, REJETEE]` — définitivement, pour le reste du cycle de vie du dossier. Il n'existe donc aucun scénario réel où un re-routage doit « fusionner autour » de décisions déjà prises : soit zéro décision n'a encore été prise (le re-routage supprime et réinstancie toute la chaîne sans rien à préserver), soit au moins une décision existe (le serveur refuse la modification avant de toucher quoi que ce soit). La préservation vient du **refus avant écriture**, pas d'une logique de fusion.

**Vérifié en direct dans les deux sens** (Phase 9.2, déclencheur `Modifier` construit dans le même tour) :
- 0 décision (`DOBB-2026-629A73`) : `PATCH` réussi → chaîne de tâches supprimée puis réinstanciée, compteur d'escalade remis à zéro, nouvelle entrée `journalAudit` « re-routage », champs modifiés visibles.
- 1 décision (`DF-2026-56EB9D`, étape `FRA` déjà `APPROUVEE`) : bouton `Modifier` absent côté UI (`aucuneDecisionPrise` calculé côté client, confort d'affichage seulement) ; appel API direct → `422 DECISION_DEJA_PRISE` ; requête DB avant/après confirmant la tâche décidée (id, `dateDecision`, tous les champs) strictement inchangée.

**Ne pas** : rouvrir cette question comme si elle restait ouverte — elle est maintenant vérifiée en conditions réelles, dans les deux sens, et documentée ici pour ne pas la reposer en Phase 10+.

### `demande.statut` n'atteint jamais `EN_COURS` dans le backend réel — valeur d'énumération morte (trouvé en construisant `MesDemandesScreen`, confirmé en revue Phase 9.2)

**Constat, par recherche exhaustive, pas par lecture partielle** : `grep -rn "EN_COURS" apps/api/src` ne retourne aucune occurrence hors la déclaration de l'enum elle-même (`packages/contracts`, `schema.prisma`). Aucun chemin de code n'assigne jamais `statut = 'EN_COURS'` à une demande.

**Cause** : les transitions réelles de `demande.statut` ne mènent qu'à `VALIDE` (validation finale) ou `REJETE` (rejet) — `R10`. Chaque approbation intermédiaire fait avancer `etapeCourante`, jamais `statut`, qui reste `SOUMIS` pendant tout le cycle de vie « en cours de circuit ». `EN_COURS` existe dans l'énumération sans qu'aucun service ne l'atteigne jamais.

**Conséquences déjà présentes dans le code livré, aucune n'est un bug — des branches mortes inoffensives, à connaître avant d'en ajouter d'autres** :
- `MesDemandesScreen` : l'option de filtre « En cours » du sélecteur de statut ne retournera jamais de résultat (correcte par construction — elle interroge `statut = EN_COURS` fidèlement, c'est le backend qui ne produit jamais cette valeur).
- Badge Sidebar « Mes demandes » (`app/page.tsx`, `rafraichirCompteMesDemandes`) : calculé comme `SOUMIS + EN_COURS` — le second terme vaut toujours 0, le badge reflète en pratique uniquement `SOUMIS`. Documenté ici plutôt que simplifié : la formule reste correcte si `EN_COURS` devient un jour atteignable (cf. hypothèse ci-dessous), la simplifier maintenant serait recréer le même trou plus tard.
- `DossierDetailScreen` : `peutAbandonnerOuRappeler`/`peutModifier` testent `statut === "SOUMIS" || statut === "EN_COURS"` — la seconde moitié du test est une branche morte, `SOUMIS` seul suffit aujourd'hui.

**Hypothèse non vérifiée, à ne pas coder dessus** : `EN_COURS` a peut-être été prévu pour distinguer « soumis, aucune étape encore traitée » de « au moins une étape déjà décidée, circuit en cours » — une transition qui n'a jamais été implémentée, pas nécessairement une valeur à supprimer. Question business, pas technique : à trancher avec le même arbitrage que les autres valeurs d'énumération surnuméraires du projet (cf. « 34 rôles maquette vs 25 seedés »), pas une décision à prendre en passant.

**Ne pas** : retirer `EN_COURS` de l'énumération ou des trois endroits ci-dessus pour « nettoyer » — sans arbitrage métier explicite, ce serait deviner que la valeur est définitivement inutile plutôt que simplement jamais encore câblée.

### R24 — SoD étendu au contrôle a posteriori (formalisé, Phase 9, clôture)

**Non documenté avant ce tour, découvert en conditions réelles, pas en lisant le code d'abord** : lors de l'essai de bout en bout complet (connexion → soumission → claim/approbation → contrôle a posteriori sur un dossier DF réel, `DF-2026-AF5715`), la même identité (`jean.kouassi`, alors porteuse du rôle `DF`) a approuvé l'étape bloquante `ordre=4` (`DF`) puis tenté le contrôle FRA `ordre=5` sur le même dossier. La tentative a été refusée : `403`, `Refus SoD` journalisé dans `JOURNAL_SECURITE`, message « Vous êtes déjà intervenu à l'étape précédente de ce dossier. »

**Cause, confirmée en lisant `SodService.verifier`, pas supposée** : la vérification compare l'acteur courant à l'acteur de `JournalAudit` pour la tâche d'`ordre - 1` du même dossier, sans distinguer une étape de vérification (`typeActeur = 'V'`) d'une étape de contrôle (`typeActeur = 'C'`) — R3 s'applique donc uniformément à toute étape avec un prédécesseur direct, contrôle FRA compris, dès lors que le contrôle suit immédiatement (`ordre - 1`) l'étape que le même acteur a décidée. Ce n'était pas une hypothèse à vérifier avant ce tour : la question ne s'était simplement jamais posée, faute d'avoir jusqu'ici exercé claim+approbation ET contrôle a posteriori par le même acteur sur un même dossier réel.

**Formalisé en règle à part entière, pas laissé comme simple constat de session** : `R24` (table « Règles métier » ci-dessus) — même statut que `R21` en son temps pour la délégation, une clause qui étend R3 à un cas que la formulation d'origine ne nommait pas explicitement. Verrouillé structurellement par `apps/api/test/sod-service.integration.spec.ts`, nouveau describe `SodService — R24 (SoD étendu au contrôle a posteriori, typeActeur='C')` : construit une tâche bloquante `typeActeur='V'` à `ordre=1` (approuvée par un acteur) suivie d'une tâche de contrôle `typeActeur='C'` à `ordre=2`, puis vérifie le conflit (même acteur → conflit ; acteur distinct → pas de conflit). **Preuve que le test mord réellement, pas une hypothèse** : `SodService.verifier` temporairement modifié pour court-circuiter la vérification quand `typeActeur='C'` (simulant l'erreur qu'un futur refactor pourrait introduire, par exemple en pensant à tort qu'un contrôle « hors chemin bloquant » est hors SoD) → le nouveau test échoue précisément et uniquement sur l'assertion `conflit === true`, les 7 autres tests du fichier restent verts ; réversion → 8/8 verts. Sans ce test, la seule preuve de R24 aurait été l'observation ponctuelle de ce tour — jamais rejouée, donc jamais protégée contre une régression future.

**Conséquence pratique pour la suite des vérifications live** : réutiliser une seule identité de test à travers toute une chaîne DF (comme fait par commodité dans des vérifications précédentes plus ciblées) fonctionne pour les étapes de vérification classiques mais échoue avec un vrai `403` dès que cette identité touche à la fois la dernière étape bloquante et le contrôle FRA qui la suit. Une identité distincte est nécessaire pour le contrôle dès que l'acteur de l'étape précédente est connu.

**Ne pas** : conclure que R3/R24 sont mal implémentées — le comportement observé est cohérent avec la lettre de la règle (« un agent ne valide pas l'étape N s'il a agi à l'étape N-1 du même dossier », sans exception pour les étapes de contrôle) et a empêché exactement le scénario qu'il est censé empêcher. C'est une confirmation, pas un bug.

**Effet de bord découvert en construisant une identité de contrôle distincte** (`fra.controleur`, bootstrap TOTP) : reformulé et déplacé dans « Questions ouvertes » ci-dessous — ce n'est plus une hypothèse de bootstrap de test, c'est un blocage utilisateur réel confirmé (cf. « Repli DUO indisponible — bloquant confirmé, plus une hypothèse »).

### État des lieux Phase 9 — clôture (après `LoginScreen`, `MesDemandesScreen`, `AuditSecuriteScreen`, déclencheur `Modifier`)

Inventaire vérifié contre le contenu réel de `docs/design/` (noms de composants exportés), pas contre la mémoire d'une session précédente.

**Construit** : coquille (Sidebar/Topbar), `LoginScreen` (identifiants LDAP + défi MFA DUO/TOTP, cf. section dédiée ci-dessus), `HomeScreen`, `NouvelleDemandeScreen` (avec le correctif montant), `MesDemandesScreen`, `CorbeillesScreen`, `DossierDetailScreen`, `ControleScreen`, `AdminScreen` (7 onglets — clôt les écrans de `screens3.jsx`), `AuditSecuriteScreen`.

**Lacune résolue — `AuditSecuriteScreen`.** `GET /api/audit/securite` — inatteignable depuis la Phase 8 (cf. section dédiée ci-dessus) avant d'être corrigée dans le même tour — a désormais un écran réel (`components/screens/audit/`), volontairement restreint au journal de **sécurité** (connexions, MFA, refus RBAC/SoD), jamais présenté comme l'audit complet du système : l'en-tête de l'écran le dit explicitement, pour ne pas confondre avec le journal d'audit métier par dossier (déjà consultable dans `DossierDetailScreen`, toujours sans vue transversale côté serveur). Composant de table dédié (`JournalSecuriteTable`), pas de réutilisation forcée de `DossierTable` : les deux entités (`JournalSecuriteVue` vs `Demande`) n'ont aucun champ en commun. `JournalSecuriteVue.identifiantAd` ajouté côté serveur dans le même tour (résolu par jointure) pour éviter d'afficher des UUID bruts. Vérifié en direct : filtres compte/événement/dates fonctionnels seuls et combinés sur 383 événements réels, fallback « Compte inconnu ou supprimé » affiché correctement pour un compte de test supprimé plus tôt dans la session.

**Lacune résolue — `MesDemandesScreen`.** `GET /api/demandes?profil=initiateur` a désormais un écran réel (`components/screens/mes-demandes/MesDemandesScreen.tsx`) — filtres circuit/statut/recherche envoyés au serveur, pagination réelle, aucun sélecteur « initiateur » (structurellement inutile : un seul initiateur possible sur cet écran, déjà forcé côté serveur). `DossierTable` extrait en composant partagé (`components/shared/`), pensé pour être repris par l'écran de consultation d'audit à venir. Badge Sidebar « Mes demandes » câblé (SOUMIS + EN_COURS) — jusqu'ici jamais alimenté malgré la prop déjà présente sur `AppShell`. Vérifié en direct : 6 dossiers réels correctement scopés, filtre par statut, ouverture d'un dossier, badge exact (5).

**Lacune résolue — écran de connexion.** `screens_auth.jsx` (`LoginScreen`) a désormais une contrepartie réelle dans `apps/web` (`components/screens/auth/LoginScreen.tsx`, routes `/` et `/login`). Vérifié en direct par l'interface elle-même (pas par script) : identifiants+TOTP réussis → session → `HomeScreen` ; identifiants invalides → message lisible ; déconnexion → retour à `LoginScreen` ; compte `TOTP` sans secret enrôlé → message de non-provisionnement, sans champ de code ; DUO indisponible → `503 MFA_INDISPONIBLE` affiché lisiblement. Chaque vérification live des étapes précédentes de cette session (Phase 9.2 avant ce commit) avait posé un cookie de session hors application (`fetch` direct, login+MFA scriptés) — ce n'est plus nécessaire. `MfaChallenge`/`DemoAccounts`/sélecteur de persona du mockup restent hors périmètre (logique cliente jamais portée, même traitement que `RoleMenu`).

**Jalon — premier parcours complet.** Avec `LoginScreen`, l'application cesse d'être une collection d'écrans vérifiés isolément : un utilisateur peut désormais entrer (identifiants + MFA réels), agir (saisir/router/valider/administrer selon son rôle) et sortir (déconnexion) sans aucune intervention hors application. Chaque écran précédent avait été vérifié en direct individuellement, cookie posé à la main ; c'est la première fois que le chemin d'entrée lui-même fait partie du parcours vérifié.

**Autres écrans du mockup sans contrepartie** :
- `ConsultationScreen` (`screens2.jsx`) — réutilise `DossierExplorer` sur l'ensemble des dossiers, sans scope `profil`. Distinct du journal de sécurité (`AuditSecuriteScreen`, construit ce tour) : c'est une vue globale des demandes, pas un journal d'événements. Route backend déjà ouverte (`GET /api/demandes` sans `profil`, docs/06 §4), jamais câblée à un écran.
- La « Transitions des dossiers » de la maquette `AuditScreen` (`screens3.jsx`) — agrégation `JournalAuditVue` à travers tous les dossiers — n'a toujours aucune route serveur (cf. Questions ouvertes, « Aucun flux d'activité récent transversal n'existe »). `AuditSecuriteScreen` ne couvre que l'onglet « Sécurité » de cette même maquette, le seul avec une route réelle.
- `MasseScreen`, `IntegrationsScreen` (`screens4.jsx`) — aucune route backend, aucune trace nulle part : même famille que Utilisateurs/Moniteur (AdminScreen), jamais nommée comme telle jusqu'ici.

**Lacune résolue — déclencheur `Modifier` (R6).** `DossierDetailScreen` expose désormais le bouton conditionné à initiateur + statut ∈ {SOUMIS, EN_COURS} + aucune décision déjà prise ; formulaire minimal (`nomClient`/`libelle`/`commentaire` — ni `circuit` ni montant, non modifiables via cette route). Voir la section dédiée ci-dessus pour le détail de la vérification live et la correction de la prémisse initiale de la question (R6 est plus étroit qu'on ne le supposait). Entrée Sidebar « Modules », redondante avec l'onglet « Paramètres système » d'AdminScreen, retirée dans le même tour.

**Dans `DossierDetailScreen`, actions du mockup encore non couvertes** : `Déléguer`/`Réaffecter` restent exclus (route de recherche d'agent manquante, déjà consignée) ; Approuver/Rejeter sont des actions simples, pas la revue champ par champ **spécifiée par `PGD-055`/`SF-PGD-080`/`081`** (pas seulement montrée par le mockup `ApproveModal`) — backend déjà prêt (`approuverRequeteSchema.revue`), jamais peuplé côté écran. Entrée dédiée dans `docs/design/DIVERGENCES.md` (« Fonctionnalité spécifiée ET maquettée, non construite à l'écran »), pas seulement cette ligne — un écart de contrat d'interaction, pas un détail.

**Questions métier non tranchées** (inchangées, cf. « Questions ouvertes » plus bas) : seuils/rôles de subdélégation DOBB/DXC, N1/N2 jamais câblés dans aucun palier, destinataires des notifications ESCALADE/ERREUR_SI, rôle habilité au rejeu SI manuel et à l'export d'audit, rattachement agent→direction/service pour le scoping KPI, escalade SLA sans effet de déblocage réel.

**Essai de bout en bout unique, exécuté en clôture** : connexion → création de demande DF → ajout de lignes → aperçu de routage → soumission → claim et approbation (quatre étapes bloquantes, quatre identités distinctes — cf. section R3/SoD dédiée ci-dessus) → contrôle a posteriori FRA → consultation dans Mes demandes → vérification dans le journal de sécurité, sur un dossier fraîchement créé (`DF-2026-AF5715`), sans aucune intervention hors application au-delà du bootstrap des identités de test. Trouvaille non anticipée au passage : R3 (SoD) s'applique aussi au contrôle a posteriori, cf. section dédiée. Identités de test et groupes LDAP éphémères retirés de l'annuaire dev après vérification — **mais côté LDAP seulement, cf. section dédiée ci-dessous : le nettoyage Postgres, lui, n'avait pas eu lieu.**

---

## Identités de test persistantes (dev)

Outil de travail, pas une question ouverte — **ne pas nettoyer automatiquement en fin de session**, contrairement aux identités jetables des vérifications e2e ponctuelles (cf. essai de bout en bout de clôture de Phase 9 ci-dessus, ou la convention `mint-session-tmp.e2e-spec.ts` utilisée pendant l'audit visuel de Phase 9.3). Quinze identités, dev uniquement (`docker/openldap/`, jamais l'AD Orange CI réel), même mot de passe LDAP pour toutes : `MotDePasseTest123!`. Regroupées par circuit ci-dessous plutôt qu'en sections séparées — vu le nombre de comptes, une seule table reste plus lisible qu'un historique éclaté par chantier.

**Domaine `@orange.com` depuis le 18/08/2026** (cf. section dédiée en fin de ce chantier, ci-dessous) — le domaine `@orange.ci` cité dans les sections narratives qui précèdent (Phase 9, Phase 9.2, R24, etc.) reflète fidèlement ce qui était vrai **au moment où ces vérifications ont eu lieu** ; ces passages ne sont pas rétroactivement corrigés, pour ne pas fabriquer un historique de vérifications qui n'ont jamais eu lieu sous ce domaine. Seule la table ci-dessous (état courant) et tout ce qui suit le 18/08/2026 utilisent `@orange.com`.

| Circuit | Identifiant | Rôle | MFA |
|---|---|---|---|
| Pilotage | `jean.kouassi@orange.com` | `INITIATEUR_DOBB`, `ADMIN_PGD` | TOTP (secret régénéré) |
| DF | `responsable.df@orange.com` | `RESPONSABLE_DF` | Aucun — `requiertMfa=false` |
| DF | `manager.df@orange.com` | `MANAGER_DF` | Aucun — `requiertMfa=false` |
| DF | `senior.df@orange.com` | `MANAGER_SENIOR_DF` | Aucun — `requiertMfa=false` |
| DF | `validateur.df@orange.com` | `DF` | TOTP (secret neuf) |
| DF | `fra.controleur@orange.com` | `FRA` | TOTP (secret neuf, écrase l'ancien) |
| DF | `dga.dg@orange.com` | `DGA_DG` | TOTP (secret neuf) |
| DOBB | `responsable.dobb@orange.com` | `RESPONSABLE_DOBB` | Aucun — `requiertMfa=false` |
| DOBB | `manager.dobb@orange.com` | `MANAGER_DOBB` | Aucun — `requiertMfa=false` |
| DOBB | `senior.dobb@orange.com` | `MANAGER_SENIOR_DOBB` | Aucun — `requiertMfa=false` |
| DOBB | `dobb@orange.com` | `DOBB` | TOTP (secret neuf) |
| DXC | `responsable.dxc@orange.com` | `RESPONSABLE_DXC` | Aucun — `requiertMfa=false` |
| DXC | `manager.dxc@orange.com` | `MANAGER_DXC` | Aucun — `requiertMfa=false` |
| DXC | `senior.dxc@orange.com` | `MANAGER_SENIOR_DXC` | Aucun — `requiertMfa=false` |
| DXC | `dxc@orange.com` | `DXC` | TOTP (secret neuf) |

Trois chaînes complètes vérifiées en direct (login + code TOTP réel) : `RESPONSABLE_DF → MANAGER_DF → MANAGER_SENIOR_DF → DF → FRA` (clôture de Phase 9), `RESPONSABLE_DOBB → MANAGER_DOBB → MANAGER_SENIOR_DOBB → DOBB` et `RESPONSABLE_DXC → MANAGER_DXC → MANAGER_SENIOR_DXC → DXC` — les trois suivent la même convention générique de nommage (`RESPONSABLE_<CIRCUIT>`/`MANAGER_<CIRCUIT>`/`MANAGER_SENIOR_<CIRCUIT>`/`<CIRCUIT>`), lue en base (`etape_regle`) avant toute création, pas supposée par analogie — **aucune des chaînes DOBB/DXC ne référence l'un des 9 rôles manquants du catalogue** (cf. `docs/design/DIVERGENCES.md`, « 34 rôles vs 25 seedés », nuance ajoutée à cette occasion). `jean.kouassi` élargi à `ADMIN_PGD` pour AdminScreen/AuditSecuriteScreen sans perdre `INITIATEUR_DOBB` (NouvelleDemandeScreen/MesDemandesScreen). LDAP : `docker/openldap/seed.ldif` (14 utilisateurs + 15 groupes `GG-DGR-*` ajoutés au total, même mécanique que `jean.kouassi`/`GG-DGR-INITIATEUR-DOBB` déjà documentée dans `docker/openldap/README.md`) — reproductible sur un conteneur LDAP reconstruit, pas seulement écrit à la main dans le conteneur courant.

**`dga.dg` — 7ème identité DF, ajoutée après coup en corrigeant une hypothèse fausse sur le nombre de paliers.** Le socle DF initial (5 identités : responsable/manager/senior/validateur/FRA) avait été calibré sur le **seul palier intermédiaire** de DF (5M–50M, 5 étapes RESPONSABLE_DF→MANAGER_DF→MANAGER_SENIOR_DF→DF→FRA), pas sur les trois paliers réels — DF a en fait **trois paliers**, pas un seul comme DOBB/DXC (0–5M : 3 étapes, aucune validation DF ni contrôle FRA ; 5M–50M : 5 étapes ; >50M : 6 étapes, `DGA_DG` s'intercale entre `DF` et `FRA`, R2 « au-delà des seuils, terminaison DF puis DGA/DG »). `DGA_DG` (`requiertMfa=true`, vérifié en base) n'avait donc aucune identité de test dédiée — trou trouvé en vérifiant le nombre d'étapes réel plutôt que de réutiliser le chiffre déjà écrit dans CLAUDE.md (section 10.3, qui ne décrivait que le palier intermédiaire testé en Phase 9). Comblé avec la même mécanique que le reste du socle.

**Seuls 5 des 15 comptes ont un second facteur** : `RESPONSABLE_*`/`MANAGER_*`/`MANAGER_SENIOR_*` des trois circuits ont `requiertMfa=false` en base (vérifié, pas supposé — `SELECT requiert_mfa FROM role`) — leur connexion ne déclenche jamais de défi MFA, un secret TOTP pour ces neuf-là n'aurait jamais servi. Seuls `DF`/`FRA`/`DGA_DG`/`ADMIN_PGD`/`DOBB`/`DXC` l'exigent, mais `ADMIN_PGD` (porté par `jean.kouassi`) est le seul qui ne soit pas une étape de validation finale de circuit.

**Secrets TOTP générés avec le même mécanisme que l'enrôlement réel de l'app** (`authenticator.generateSecret()`/`authenticator.keyuri()` d'otplib, QR généré avec `qrcode`, secret chiffré avec `chiffrerSecretTotp` avant écriture en base) — jamais un raccourci de test. Script jetable (`apps/api/scripts/bootstrap-totp-tmp.ts`), supprimé après chaque usage, même convention que `mint-session-tmp.e2e-spec.ts`.

**`jean.kouassi` — secret régénéré, l'ancien est désormais invalide.** S'il avait déjà été scanné dans une application d'authentification lors d'une session précédente, ce QR-là ne fonctionne plus : seul le secret le plus récemment communiqué est valable. À rappeler à chaque régénération future, pas seulement celle-ci — un secret TOTP écrasé en base invalide silencieusement toute app d'authentification qui portait l'ancien, sans message d'erreur avant la prochaine tentative de connexion.

**Réactivation de lignes `Utilisateur` existantes pour 4 des 15 comptes — trouvé en vérifiant, pas supposé.** `responsable.df`/`manager.df`/`senior.df`/`fra.controleur` réutilisent un nom déjà employé pendant l'essai de bout en bout de clôture de Phase 9 : le nettoyage de l'époque avait bien vidé **LDAP** (confirmé : aucune de ces entrées n'existait avant ce chantier), mais **jamais la ligne `Utilisateur` correspondante en base** — `RbacResolutionService.resoudre()` fait un `upsert` par `identifiantAd`, donc une connexion sur un nom déjà connu réactive la ligne existante plutôt que d'en créer une neuve (confirmé par `journal_securite.horodatage` : les quatre premières traces datent du 2026-07-25, jour de clôture de Phase 9, pas du jour de création). Les 9 autres comptes (`validateur.df` et les huit identités DOBB/DXC, noms jamais utilisés avant) sont des lignes authentiquement neuves — vérifié par l'absence de toute ligne `Utilisateur` préexistante avant leur création.

- `responsable.df`/`manager.df`/`senior.df` : réactivation sans conséquence, aucune donnée métier (`Controle`/`Demande`/`Delegation`) n'y était rattachée ; rôle resynchronisé à l'unique rôle attendu dès la première connexion de ce chantier.
- `fra.controleur` : la ligne réactivée porte un **vrai** enregistrement `Controle` (le contrôle FRA réel du dossier `DF-2026-AF5715`, 2026-07-25 16:25) — **et ce n'est pas un oubli de nettoyage isolé, c'est structurel** : `controle.controleur_id` porte `ON DELETE RESTRICT` (pas `CASCADE`), donc supprimer cette ligne `Utilisateur` aurait échoué tant que ce `Controle` existe. Une suppression complète de cette identité n'a jamais été possible sans supprimer d'abord une trace d'audit réelle — chose que ce projet ne fait jamais (cf. immuabilité de `JOURNAL_AUDIT`, même principe appliqué ici à `Controle`). Secret TOTP et rôle sont désormais corrects (écrasés par des valeurs neuves), mais la ligne elle-même — et son historique du 25/07 — reste la même qu'en Phase 9.
- Aucune des quatre réactivations n'a de conséquence pratique sur l'usage de ce socle aujourd'hui — signalé pour qu'une future tentative de « nettoyage complet » ne soit pas surprise par une violation de contrainte de clé étrangère sur `fra.controleur`, ou par une hypothèse silencieuse de fraîcheur sur les trois autres.

### Migration de domaine `orange.ci` → `orange.com` (18/08/2026)

Suite directe de la confirmation métier du domaine réel (cf. « API AD réelle » ci-dessus) — décision explicite : **basculer tout le socle dev/test**, pas seulement les fichiers de production déjà corrigés dans un premier temps. `orange.ci` reste utilisé nulle part comme domaine réel après ce chantier ; seule la distinction structurelle « CI » = Côte d'Ivoire (nom de l'entité, `TOTP_ISSUER`, `LDAP_ORGANISATION` — inchangés, ce ne sont pas des domaines) survit.

**Périmètre traité** : structure DN LDAP (`docker-compose.override.yml` — `LDAP_DOMAIN: pgd.orange.com` —, `docker/openldap/seed.ldif`, `docker/openldap/README.md`, `apps/api/test/ldap-provider.integration.spec.ts` — `BASE_DN`/`LDAP_DOMAIN`, jamais `LDAP_ORGANISATION` qui reste « PGD Orange CI Test »), `.env`/`.env.example`/`apps/api/test/setup-env.ts`/`apps/worker/test/setup-env.ts` (`LDAP_BIND_DN`/`LDAP_BASE_DN`/`LDAP_USER_DOMAIN`), `packages/config/src/env.schema.ts` (défaut Zod), `docker-compose.yml` (défaut `LDAP_USER_DOMAIN`), 34 fichiers de test (`apps/api/test/*.spec.ts`, `apps/worker/test/*.spec.ts`) où `@orange.ci` n'est qu'une chaîne de fixture arbitraire (aucune dépendance à la structure DN réelle — remplacement mécanique, vérifié un par un avant le remplacement pour écarter tout fichier qui construirait lui-même un annuaire LDAP réel), UI (`LoginScreen.tsx` — suffixe auto-complété et affiché —, `AuditSecuriteScreen.tsx`, `AnnuaireRechercheModal.tsx` — placeholders et message d'erreur), `packages/contracts/src/admin-utilisateur.ts` (message d'erreur d'exemple + commentaire, qui affirmait à tort que le choix du domaine restait une question ouverte), scripts (`bootstrap-premier-admin.ts`, `exemple-comptes.json`, `enrolement-lot-v1-tmp.ts` — commentaire d'exemple).

**Ce qui n'a délibérément PAS été touché** : les sections narratives de ce fichier qui décrivent des vérifications déjà effectuées sous `@orange.ci` (Phase 9, R24, l'essai de bout en bout de clôture, etc.) — les réécrire en `@orange.com` fabriquerait un historique de vérifications qui n'ont jamais eu lieu sous ce domaine, alors que la règle déjà posée ailleurs dans ce fichier (« le silence de la maquette n'est pas une décision de conception ») a son miroir ici : un historique n'est pas un état courant, corriger un fait métier ne réécrit pas le passé. `docs/*.md` (PRD, spécifications, dossiers de réalisation) et `docs/design/` (export maquette figé) sont aussi restés hors périmètre — ce chantier porte l'infrastructure dev/test exécutable, pas les livrables documentaires historiques.

**Incident trouvé en cours de route, pas anticipé — récidive de la « disparition du socle d'identités » déjà documentée (17/08/2026, section dédiée ci-dessus).** Avant de migrer les identifiants Postgres des 15 identités persistantes, vérification de leur état réel (au lieu de supposer la reconstruction du 17/08 encore en place) : `SELECT identifiant_ad FROM utilisateur` ne renvoyait que les 7 fixtures orphelines déjà connues (`test.*@orange.ci`, 0 `MembreRole`) — les 15 lignes reconstruites le 17/08 n'existaient plus. Même signature exacte que l'incident déjà documenté : `role`=35 (référentiels intacts), `utilisateur`=7, `membre_role`=0, `demande`=0, `controle`=0, `journal_audit`/`journal_securite` intacts et plus élevés qu'au 17/08 (4549/1299 contre 4209/1153 — le temps a continué de s'écouler, l'écriture d'audit n'est pas affectée). Cause non réinvestiguée ici — hors périmètre de ce chantier, déjà signalé comme suivi séparément par la personne pilotant le projet ; ne pas rouvrir cette enquête à chaque nouvelle occurrence sans mandat explicite.

**Reconstruit directement sous `@orange.com`**, avec les deux scripts opérationnels permanents déjà construits pour l'onboarding V1 (`enrolement-comptes.ts` + `lib/enrolement.ts`) plutôt qu'un nouveau script jetable ad hoc — preuve pratique que ces scripts servent au-delà de leur motivation initiale. Fichier JSON des 15 entrées écrit dans le scratchpad de session (jamais dans le dépôt), `mfaMethode` omis partout (défaut `TOTP` du script, cohérent avec la posture actée le 18/08/2026 pour la bascule d'urgence — même si 9 des 15 rôles n'exigent aucun second facteur et n'exercent donc jamais ce secret). `fra.controleur` : aucune précaution particulière requise pour la re-création — la ligne `Controle` historique du 25/07 (`DF-2026-AF5715`) a disparu avec la ligne `Utilisateur` cette fois (`controle`=0 confirmé), donc aucune contrainte `ON DELETE RESTRICT` à contourner ; la nouvelle ligne est une création authentiquement neuve, pas une réactivation.

**Vérifié en direct, dans les deux sens, sous le nouveau domaine** : conteneur `openldap` recréé (`docker compose up -d --force-recreate openldap`, arbre DN entièrement dépendant de `LDAP_DOMAIN` à la création du conteneur — pas de volume persistant à purger séparément) puis reseedé (`docker/openldap/seed.ldif` mis à jour, 32 entrées `ldapadd` sous `dc=pgd,dc=orange,dc=com`) ; conteneurs `api`/`worker` recréés pour reprendre le nouveau `.env`. `responsable.df@orange.com` (rôle `RESPONSABLE_DF`, `requiertMfa=false`) → `POST /api/auth/login` → `200`, session directe sans défi MFA. `dobb@orange.com` (rôle `DOBB`, `requiertMfa=true`) → `login` → `requiresMfa:true, methode:"TOTP"` → code TOTP réel généré à partir du secret déchiffré en base (script jetable, supprimé après usage) → `POST /api/auth/mfa/verify` → `200` → `GET /api/auth/session` confirme `roles:["DOBB"]`. Les deux preuves couvrent exactement les deux familles de la table (`requiertMfa=false` direct, `requiertMfa=true` avec TOTP réel) sous le nouveau domaine, bind LDAP réel compris.

Sweep complet revérifié après le chantier (hôte, `worker` arrêté pour la déterminisme de `si-service`, `--maxWorkers=4` pour la flakiness Node 24 déjà documentée) : `apps/api` 39/39 suites, 234/234 tests (dont `ldap-provider.integration.spec.ts` contre le nouvel arbre DN) ; e2e 7/7, 20/20 ; `apps/worker` 7/7, 25/25 — zéro régression. `pnpm lint`/`pnpm typecheck`/`pnpm build` verts sur les 13 packages/apps.

---

## Codes d'erreur

| Code | Usage |
|---|---|
| `400` | Validation de schéma |
| `401` | Non authentifié |
| `403` | RBAC ou SoD |
| `404` | Ressource introuvable |
| `409` | Conflit de verrou (claim) |
| `422` | Règle métier violée (`R13`–`R17`) |
| `429` | Rate limit |

Enveloppe : `{ data, error: { code, message, details }, meta }`.

---

## Décisions actées (ne pas rouvrir sans arbitrage)

| # | Décision |
|---|---|
| D1 | Backend en **NestJS** — le schéma technique indiquant « Next.js » est une erreur de libellé |
| D2 | **RabbitMQ** comme bus de messages (exchange topic + DLX) ; **BullMQ écarté**. Redis conserve cache et verrous, sans fonction de file |
| D3 | MFA : interface `MfaPort` avec **DUO et TOTP** — le TOTP sert de repli hors ligne |
| D4 | **BSCS et GAIA** derrière un `BillingSiPort` unique, routés par circuit |
| D5 | Frontend **inclus** dans le monorepo |
| D6 | Le **ND** est l'identifiant fonctionnel de ligne ; le compte reste l'entité de rattachement |
| D7 | Les tranches de routage deviennent des **paliers de subdélégation** (`label_palier`) — structure pivot inchangée |
| D8 | `JadeProvider` ajouté aux ports (bouchon phase 1) |

---

## Dépendances externes en attente

Distinct des « Questions ouvertes » ci-dessous : celles-là attendent une décision de la personne pilotant le projet (métier, produit, arbitrage technique) — celles-ci attendent qu'une **autre équipe** livre une infrastructure qui n'existe pas encore. Rien à trancher côté PGD, seulement à attendre.

- **Registre Nexus interne (`DOCKER_REGISTRY`) — l'équipe DevOps n'a pas encore l'infrastructure nécessaire (confirmé 19/08/2026).** Bloque `docker-compose.prod.yml` (variante registre — images déjà construites et poussées, référencées par tag). Ne bloque pas `docker-compose.prod.build.yml` (build local sur l'hôte de déploiement, aucun registre requis), utilisable dès aujourd'hui en attendant. Les deux fichiers restent corrects et prêts indépendamment de cette dépendance — c'est leur *usage* pour la variante registre qui attend Nexus, pas leur construction. Question technique connexe, plus étroite, toujours posée séparément : « quel processus produit réellement les images poussées vers Nexus ? » (cf. section « Sécurité — vulnérabilités CRITICAL des images Docker », qui reste pertinente une fois Nexus disponible).

## Questions ouvertes

- Seuils et rôles définitifs de la fiche de subdélégation (défaut = tranches actuelles, paramétrable). **Conséquence concrète, pas seulement théorique** : faute de ces seuils pour DOBB/DXC, R12 (contrôle FRA) ne peut jamais s'y appliquer — voir l'entrée dédiée plus bas (« Aucun dossier DOBB ou DXC au-dessus de 5 000 000 FCFA ne peut être soumis aujourd'hui »), les deux se résolvent ensemble.
- Politique par défaut sur ligne `RESILIE` (défaut = `BLOQUANT`).
- Contrat exact des API BSCS / GAIA (bouchon en attendant).
- **Repli DUO indisponible — résolu pour la fenêtre du déploiement V1 (18/08/2026), pas une fermeture définitive.** Historique : `Utilisateur.mfaMethode` a pour défaut `DUO` et, jusqu'au pré-enregistrement (Temps 1, 12/08/2026), aucun chemin de code ne le faisait jamais passer à `TOTP`. `PATCH /api/admin/utilisateurs/{id}` a ensuite ouvert la route de bascule (par compte, une décision par utilisateur). Ce qui restait non résolu — provisionner un `totpSecret` réel en masse, avec une transmission hors canal applicatif — est traité par la mesure d'urgence du 18/08/2026 (cf. section dédiée « Déploiement V1 — bascule MFA d'urgence »), pas par une capacité admin permanente : `mfaMethode=TOTP` a été basculé pour **tous** les comptes existants d'un seul geste (script jetable, pas une route), et le socle réel de 20 utilisateurs a été enrôlé par un script de lot qui génère secret + QR individuel — même mécanisme que `MfaService.demarrerEnrolementTotp`, hors du flux applicatif. La question de sécurité opérationnelle (transmission du secret) a été tranchée dans l'urgence : un fichier QR par personne, jamais un lot groupé — mais **rien n'a été construit comme capacité admin réutilisable** ; un futur besoin similaire referait un script jetable, pas un bouton.
- **Génération d'un secret TOTP côté admin — construite (Priorité 1, 19/08/2026) ; transmission — toujours pas résolue, question distincte et délibérément non refermée par ce chantier.** `POST /api/admin/utilisateurs/{id}/totp-secret` (`ADMIN_PGD`, `AdminUtilisateursService.genererSecretTotp`) génère/régénère un secret réel pour un compte déjà pré-enregistré à `mfaMethode=TOTP` — même mécanisme que le self-service (`TotpProvider.genererEnrolement`) et que le script d'urgence du 18/08/2026 (`chiffrerSecretTotp`, une seule source de vérité), exposé cette fois depuis `UtilisateursAdminTab`/`UtilisateurModal` (bouton « Générer un QR TOTP » / « Régénérer le secret TOTP », panneau QR affiché en clair une seule fois à l'écran — image + secret en saisie manuelle + téléchargement PNG). Persistance immédiate sans étape de confirmation par code (contrairement au self-service) : un admin ne peut structurellement pas fournir un code généré par le téléphone de la personne cible. Journalisé (`JournalSecurite`, nouvel événement `TOTP_ENROLEMENT_ADMIN`) — limite assumée et documentée dans le code : la colonne ne porte que l'identité de la personne CIBLE (`utilisateurId`), pas celle de l'admin qui a déclenché l'action — `JournalSecurite` n'a qu'un seul champ d'identité, pas de distinction acteur/cible comme `HISTORIQUE_MONTANT` (R23). **Ce que ce chantier résout et ce qu'il ne résout pas, explicitement** : il ferme la moitié « comment produire un secret + QR pour un compte donné » du trou déjà documenté (cf. entrée précédente et la suivante) — un compte `mfaMethode=TOTP` jamais enrôlé n'est plus définitivement bloqué, un admin peut désormais le débloquer en un clic (vérifié en direct sur un compte de fixture réellement dans cet état, `FGGK6451` — badge « Non enrôlé » disparu après génération, secret confirmé en base). Il ne résout PAS la moitié « comment ce secret/QR atteint la bonne personne sans transiter par un canal compromis » — un bandeau d'avertissement non escamotable le rappelle dans l'écran lui-même (« transmettez-le par un canal sécurisé — jamais par e-mail ou messagerie interne non chiffrée »), mais aucun mécanisme de transmission n'est construit. Si un besoin récurrent de transmission sécurisée apparaît, la décision de sécurité opérationnelle reste entièrement à prendre — ne pas déduire de ce chantier qu'elle est validée ou même entamée.
- **L'enrôlement TOTP doit-il être en libre-service depuis l'écran de connexion, ou rester un provisionnement administrateur ?** Question de sécurité, pas d'ergonomie — à trancher avec la même conversation que le point précédent (procédure de repli DUO). Trouvé en construisant `LoginScreen` (Phase 9.2) : `POST /api/auth/mfa/enroll/totp` et `.../confirm` exigent `@Authenticated()` (une session déjà valide) — un utilisateur dont `mfaMethode=TOTP` sans secret enrôlé ne peut jamais obtenir de session pour venir enrôler (`mfa/verify` échoue toujours sans `totpSecret`), déjà signalé dans le code (`auth.controller.ts`, commentaire « LIMITE NON RÉSOLUE ») comme délibérément non résolu. Une extension technique existe (amorcer l'enrôlement via le `challengeId` émis après la seule vérification LDAP+mot de passe, avant tout second facteur) mais a été refusée en revue : elle ouvrirait un chemin où un second facteur s'établit avec un seul facteur vérifié — exactement ce que le MFA existe pour empêcher, en particulier sur les rôles à `requiertMfa` (les plus sensibles). `LoginScreen` affiche donc un message explicite (« compte non provisionné pour l'authentification à deux facteurs, contactez votre administrateur ») plutôt qu'un échec silencieux ou une tentative d'enrôlement inline — décision délibérée, pas une lacune d'écran.
- **Aucune route de provisionnement TOTP n'existait — fermé pour le cas admin (Priorité 1, 19/08/2026), toujours vrai pour le self-service.** Constat d'origine (avant ce chantier) : une seule écriture de `totpSecret` dans `apps/api/src` (`confirmEnrollTotp`, self-service authentifié) et aucune en seed — un nouvel utilisateur assigné dès la création à un rôle exigeant la MFA ne pouvait **jamais** enrôler de TOTP, par aucun chemin. `POST /api/admin/utilisateurs/{id}/totp-secret` (cf. entrée dédiée ci-dessus) ferme ce trou côté admin — un tel compte peut désormais être débloqué en un clic depuis `UtilisateursAdminTab`, sans dépendre de l'astuce qui avait permis d'enrôler `jean.kouassi` à l'origine (compte alors sans rôle à `requiertMfa`). Le chemin self-service (`POST /api/auth/mfa/enroll/totp`, `@Authenticated()`) reste, lui, inchangé et toujours structurellement inaccessible à un utilisateur qui n'a encore aucune session — cf. l'entrée précédente sur ce point, non rouverte par ce chantier.
- **Sémantique de `ENUM_TYPE_ACTEUR` (`V`/`A`/`C`)** : non glosée dans les sources. Ne pas deviner l'expansion. Stocker les codes, porter le libellé en seed.
- **`JournalSecurite.codeEchec`/`messageEchec` (19/08/2026) capturés et persistés, jamais exposés à l'écran.** `journalSecuriteVueSchema` (`packages/contracts/src/audit.ts`) et `AuditSecuriteScreen` n'ont pas été mis à jour lors du chantier qui a ajouté ces deux colonnes (`AdApiProvider`/`JournalSecuriteService`, cf. section dédiée « `JOURNAL_SECURITE` — `codeEchec`/`messageEchec` ») — portée délibérément limitée à migration/implémentation/tests à ce moment-là. Les deux colonnes existent réellement en base, correctement peuplées pour `AdApiProvider` (jamais pour `LdapProvider`, cf. section dédiée), mais un admin consultant `AuditSecuriteScreen` aujourd'hui ne les voit nulle part — write-only tant que cette entrée reste ouverte. À traiter en un geste : ajouter les deux champs à `journalSecuriteVueSchema`, à la requête serveur qui construit `JournalSecuriteVue`, et à l'affichage `AuditSecuriteScreen` (probablement une colonne ou un détail dépliable, visible seulement quand `succes=false` et que l'un des deux champs est non nul).
- **Écart AD de dev → AD Orange réel — prémisse initiale corrigée (12/08/2026, cf. « API AD réelle » ci-dessus), client HTTP construit (19/08/2026, `AdApiProvider`, cf. section dédiée).** Cette question supposait jusqu'ici que l'écart dev→prod serait un écart de **configuration LDAP** (bind DN, schéma d'annuaire, groupes) sur le même protocole que `LdapProvider` (dev, `ldapjs`, bind LDAP/LDAPS réel). **Ce n'est pas le cas** : l'API réelle communiquée est un **endpoint REST/JSON** (`POST http://192.168.31.78:8080/ci.orange.ldap/rs-interface/authenticate`, body `{username, password}`, réponse `{check, nom, prenom, email, ...}`) — aucun bind LDAP, aucune notion de DN/OU/`memberOf` visible dans ce qui a été communiqué. La prédiction s'est confirmée : `AdApiProvider` est bien un nouveau client HTTP, pas une reconfiguration de `LdapProvider`, et coexiste avec lui derrière `LDAP_PORT`. Les points ci-dessous restent des questions réelles, jamais vérifiées contre le vrai système :
  - schéma d'annuaire réel (OU, attributs `mail`/`memberOf` ou équivalent) — **question probablement caduque** si l'accès réel passe uniquement par ce endpoint REST plutôt que par un bind LDAP direct ; à reposer explicitement plutôt que supposer l'ancienne réponse encore valide ;
  - convention `GG-DGR-*` à confirmer nom pour nom face aux groupes AD réels — l'API REST communiquée ne renvoie d'ailleurs **aucun groupe** dans sa réponse de succès (`department`/`fonction` oui, `memberOf`/groupes non) ; si aucun autre mécanisme ne fournit les groupes, la résolution de rôle par groupe AD (déjà en tension avec le retrait de la resynchronisation, cf. « Pré-enregistrement des utilisateurs AD » ci-dessus) devient d'autant plus non pertinente pour cette API précise ;
  - compte de service (`LDAP_BIND_DN`/`LDAP_BIND_PASSWORD`) — l'API REST communiquée n'a **aucune authentification de service visible** (cf. section dédiée), donc ce point ne s'applique probablement pas tel quel à cette API ;
  - cohérence entre les groupes AD réels et les 25 rôles seedés (`packages/database/prisma/seed/referentiels/roles.seed.ts`) — recouvre partiellement le point « catalogue de rôles 25/34 » déjà signalé, reste posée indépendamment du mécanisme d'authentification.
- **Confirmation réseau nécessaire avant d'accepter HTTP (non chiffré) sur l'API AD réelle.** Le serveur de production PGD sera-t-il sur le même segment réseau interne que `192.168.31.78:8080` — justifiant l'absence de HTTPS — ou l'appel traversera-t-il un réseau moins sûr ? Question de sécurité réseau, pas technique, portée par la personne pilotant le projet (cf. section « API AD réelle » ci-dessus). Ne rien construire qui suppose la réponse.
- **Forme d'une réponse d'échec de l'API AD réelle — toujours inconnue empiriquement, mais sans conséquence sur la sécurité de `AdApiProvider`.** Aucune source ne la donne encore ; l'hôte reste injoignable depuis l'environnement de développement (cf. section « API AD réelle » ci-dessus). Devenu sans objet pour la **sûreté** du code (19/08/2026) : `AdApiProvider` échoue fermé sur *toute* forme qui n'est pas exactement `{"check": "true", ...}` — `{"check": "false"}`, un code HTTP différent, un corps différent, une exception serveur sont tous traités de façon identique (refus), quelle que soit la forme réelle jamais observée. Reste ouvert pour l'**observabilité** seulement (le log `warn` distinguera moins bien une vraie erreur d'authentification d'un problème réseau tant que la forme réelle n'est pas connue) — à affiner une fois la revérification contre le vrai système faite (cf. section dédiée).
- **L'escalade — automatique ET manuelle — n'a aucun effet de déblocage tant que la hiérarchie de rôles d'escalade n'est pas définie, et le déclencheur manuel existant est mal positionné — question unique, resserrée en Phase 9.0 puis complétée le 12/08/2026 (`docs/12` diapositive 17).** `SlaEscalationService.escalader()` (`apps/worker/src/jobs/sla-escalation.service.ts`, automatique sur dépassement SLA) et `EscaladeManuelleService`/`POST /api/admin/escalade-manuelle/{tacheId}` (Phase 6, manuel) incrémentent tous deux `niveau_escalade` et journalisent, mais **aucun des deux ne réaffecte jamais la tâche** : en l'état, une tâche escaladée — automatiquement ou manuellement — reste dans la même corbeille avec le même rôle, et un acteur absent continue de bloquer le circuit. Même défaut, même cause : ni l'un ni l'autre ne sait vers quoi réaffecter, faute de réponse à la question ci-dessous.

  Le volet manuel porte un second défaut, distinct et propre à lui : `docs/12` confirme qu'un déclenchement manuel est attendu (« la solution doit permettre d'escalader automatiquement **ou manuellement** une tâche vers un supérieur hiérarchique ou un autre acteur »), mais le mécanisme construit est restreint `@Roles("ADMIN_PGD")` uniquement — un administrateur escalade n'importe quelle tâche, jamais l'**acteur de la tâche lui-même**, alors que la source suggère un déclenchement par un acteur du circuit, pas par l'administration.

  **Question fermée à poser au métier** (la maquette `docs/design/engine.jsx:319-337` — `escalader()`/`runAutoEngine()` — suggère une réponse cohérente avec le modèle existant, sans faire foi : elle avance simplement le dossier à `dossier.taches[ordre+1]`, l'étape suivante déjà instanciée dans la MÊME chaîne, pas un rôle externe) : **« l'escalade transfère-t-elle la tâche à l'étape suivante de la chaîne du dossier, ou à un rôle superviseur externe ? Et le déclenchement manuel doit-il rester réservé à l'administration, ou être ouvert à l'acteur de la tâche (voire à un tiers qu'il désigne) ? »** Si la réponse au premier volet est « étape suivante de la chaîne », l'implémentation est nettement plus simple que ce qu'imaginait la question initiale (pas de table de succession entre corbeilles à construire) — et la même réponse résout mécaniquement la réaffectation manquante des deux mécanismes, automatique et manuel, à la fois. Deviner resterait inventer une règle métier (règle non négociable 1).
- **Création de processus personnalisés via interface graphique — ambition à clarifier, rien construit (12/08/2026, `docs/12` diapositive 17).** La source décrit une capacité de conception complète : étapes du workflow, acteurs, règles de gestion, **conditions de transition (logiques conditionnelles)**, actions automatiques et notifications associées, le tout configurable par un administrateur sans code. `PaliersAdminTab`/`AdminPaliersService` (Phase 5.5, déjà construit) ne couvre qu'un sous-ensemble : CRUD de paliers avec une chaîne d'étapes ordonnée (rôle, type d'acteur, bloquant, SLA) par borne de montant — aucune logique conditionnelle au-delà de la sélection de palier par montant/sous-flux, aucune action automatique configurable, aucun designer visuel. **Statut à confirmer, pas à construire** : vision produit large jamais destinée à une V1 littérale (l'admin des paliers existant suffirait alors comme réalisation « suffisamment fidèle » de l'esprit de l'exigence), ou exigence réellement incomplètement couverte qui mériterait un vrai chantier de designer visuel. Ne rien construire sans cette clarification — le risque d'un designer complet non demandé est disproportionné par rapport à ce que la plateforme utilise réellement aujourd'hui (paliers + étapes simples).
- **Destinataire de la notification superviseur à l'escalade SLA.** Même trou, symétrique : aucune source ne désigne qui reçoit la notification (le titulaire du rôle actuel ? un superviseur de circuit ? un rôle nommé ?). Non implémenté tant que non tranché. Le destinataire est un **superviseur métier** — à ne pas confondre avec le point suivant (exploitation).
- **Destinataire de la notification `ERREUR_SI`.** Question distincte de la précédente, à ne pas fusionner : le destinataire d'une escalade SLA relève du métier (un superviseur de circuit), celui d'une erreur de restitution SI relève de l'**exploitation** (équipe SI-ops ou support technique) — deux publics différents, deux réponses différentes. Non implémenté (Phase 8, notifications) tant que non tranché.
- **Envoi d'e-mail réel — état vérifié, rien à construire avant feu vert.** Vérification demandée avant toute construction (12/08/2026) : aujourd'hui, **aucun envoi d'e-mail réel n'existe nulle part dans le dépôt**. `SmtpPort` (`apps/worker/src/notifications/smtp.port.ts`) est une interface réelle, mais son unique implémentation est `SmtpStubAdapter` (journalise seulement, `worker.module.ts` la câble en dur sur `SMTP_PORT` — aucun `SMTP_PROVIDER` réellement lu, `.env.example` le liste commenté dans la section « Phases suivantes — non validées », même statut que `JADE_PROVIDER`/`GED_PROVIDER`). Le canal in-app (table `Notification`) reste la seule source de vérité fonctionnelle des notifications ; `SmtpPort` n'a jamais été un canal secondaire réellement branché, juste une interface prête pour ça. Un relais SMTP interne a été signalé comme piste d'infrastructure disponible pour PGD (adresse/port non documentés ici — confirmation en attente sur la destination réelle du relais et sur la sûreté de committer ces coordonnées, même en variable d'environnement nommée). **Rien construit** : ni adaptateur réel, ni configuration, en attendant cette double confirmation.
  **Portée du futur adaptateur — élargie (12/08/2026, `docs/12` diapositive 18)** : « l'initiateur doit être informé de **tout changement de statut** de sa demande », pas seulement escalade/rejet comme envisagé au démarrage de ce chantier. Le futur adaptateur réel devra donc couvrir, dès sa première version, les **6 types déjà émis par `NotificationService`** (`traiterNouvelleTache`/`traiterAvancement`/`traiterRejet`/`traiterValidation`/`traiterEscalade`/`traiterErreurSi` — chacun appelle déjà `this.smtp.envoyer()` via `creerEtEnvoyer`/`notifierParRoleConfigurable`, `notification.service.ts:21-96`), jamais un sous-ensemble restreint choisi par commodité de mise en œuvre. Aucun code écrit avec un périmètre restreint à élargir — cette note fixe la portée cible avant que la construction ne commence, pas un correctif après coup.
- **Rôle habilité au rejeu manuel SI** (`POST /api/demandes/{id}/si/pousser`, PGD-062) — `ADMIN_PGD` par défaut (précédent `escalade-manuelle`), un rôle SI-ops dédié serait plus cohérent avec la nature financière de l'action. Contrairement à `escalade-manuelle` ou `import-crm` (réversibles, sans effet externe), pousser au SI de facturation a une conséquence financière réelle — le précédent ne transfère pas automatiquement. À trancher avec le métier.
- **Rôle habilité à l'export de l'audit** (`GET /api/audit/{demandeId}/export`, PGD-072) — aucune source (`docs/01` SF-PGD-141) ne nomme de rôle de contrôle pour cette extraction nominative sur dix ans. `ADMIN_PGD` par défaut provisoire (même famille de décision que le rejeu manuel SI ci-dessus). La consultation simple (`GET /api/audit/{demandeId}`, sans export) reste ouverte à tout utilisateur authentifié — même niveau d'accès que la lecture de la demande elle-même (docs/06 §4), pas une divulgation nouvelle. Le journal de sécurité (`GET /api/audit/securite`) est `ADMIN_PGD` sans ambiguïté : événements croisant tous les utilisateurs, fonction d'administration par nature.
- **Rattachement utilisateur → direction/service : faut-il le modéliser ?** (docs/06 §10, PGD-074) — question requalifiée en Phase 9.0. Le trou initial (« quel rôle pour `profil=pilotage` ? ») était mal posé : `profil=initiateur`/`valideur` sont vérifiés contre des données réelles (`initiateurId`, `roleCorbeille` détenu) depuis la Phase 8.4bis, et `profil=pilotage` est restreint à `ADMIN_PGD` (cf. section « KPI (PGD-074, Phase 8.4) » ci-dessus) — ce n'était pas un oubli de modélisation RBAC, c'est qu'**aucun rattachement utilisateur → direction/service n'existe dans le schéma**, point confirmé en Phase 9.0 : la maquette (`docs/design/data.jsx` — `USERS[].direction`/`.service`, table `DIRECTIONS`) invente ce rattachement pour sa démo précisément parce qu'il n'a jamais été modélisé côté serveur. La question qui reste n'est donc plus « quel rôle pour pilotage » (tranchée : `ADMIN_PGD`, provisoire) mais **« faut-il ajouter un rattachement agent → direction/service au modèle, pour un scoping KPI plus fin que pilotage-vs-le-reste ? »** — à trancher avec le métier, pas une décision technique.
- **`GET /api/notifications` — question fermée et implémentée (Phase 9.2).** `NotificationsController` (nouveau module `apps/api/src/modules/notifications/`) expose `GET /api/notifications` (pagination `page`/`limit`, filtre optionnel `lu`) et `PATCH /api/notifications/{id}/lu`. Périmètre forcé par construction sur `lister` (`where.destinataireId = utilisateur.id`, jamais un paramètre client — même garde que `profil=initiateur`), donc **pas de guard de propriété nécessaire sur cette route**, même raisonnement que `DemandesController.creer`. `marquerLue` agit sur une ressource déjà identifiée par id : porte `NotificationDestinataireGuard` (nouveau, même structure que `InitiateurDemandeGuard`), avec sa propre entrée `guard-coverage.spec.ts` (`TABLE_NOTIFICATIONS`). Vérifié dans les deux sens : `notification-destinataire.guard.spec.ts` (destinataire réel → autorisé ; tiers → 403 `PAS_DESTINATAIRE`, `RBAC_REFUS` journalisé) et `notifications.integration.spec.ts` (`lister` d'un destinataire n'expose jamais celles d'un autre ; filtre `lu` correctement borné ; `marquerLue` rejette 422 `NOTIFICATION_DEJA_LUE` sur une notification déjà lue et 404 sur un id inexistant). `docs/design/app.jsx` (`NotifDropdown`) n'a jamais fait foi ici — sa forme (`myTasks`/`myAlerts`) vient de la simulation client (`engine.jsx`/`data.jsx`, jamais portée), pas du modèle réel `Notification` (`destinataireId`/`type`/`canal`/`lu`/`horodatage`). Reste à faire côté écran : `apps/web` ne consomme pas encore cette route — à câbler avec la cloche de notification quand `DossierDetailScreen`/corbeilles seront repris.
- **« Mes demandes » — question fermée et implémentée (Phase 9.2).** `listerDemandesQuerySchema` porte désormais `profil: z.enum(["initiateur"]).optional()` ; `DemandesController.lister` injecte `@CurrentUser()` et transmet `utilisateur.id` à `DemandeService.lister(query, utilisateurId)`, qui force `where.initiateurId = utilisateurId` quand `profil === "initiateur"` — jamais un `initiateurId` accepté en paramètre client, même garde que `GET /api/kpi?profil=initiateur`. Additif, pas une restriction : sans `profil`, la liste reste non scopée (docs/06 §4, choix documenté, inchangé). Vérifié dans les deux sens par un test d'intégration dédié (`demande-workflow.integration.spec.ts`, describe `DemandeService.lister`) : avec `profil=initiateur`, le dossier d'un autre agent reste invisible ; sans `profil`, il reste visible. **Pas d'entrée `guard-coverage.spec.ts`** — `lister` reste une route `GET`, structurellement exclue de `listerRoutesEcriture()` (vérifié, pas supposé) ; la portée y est un filtre de `WHERE`, pas un guard de route. Reste à faire côté écran : `apps/web` ne consomme pas encore `profil=initiateur` — à câbler avec la tuile « mes demandes » quand `DossierDetailScreen`/corbeilles seront repris.
- **Aucune route ne sert le fichier réel d'une pièce jointe — touche `GedPort`, pas seulement une route manquante (trouvé en construisant `PiecesTab`, `DossierDetailScreen`, Phase 9.2).** `POST /api/demandes/{id}/pieces` et `DELETE .../pieces/{pieceId}` existent ; `GedStubAdapter.stocker` écrit un vrai fichier sur disque (`GED_STORAGE_PATH`), et `PieceJointe.gedRef` porte déjà la référence — mais `GedPort` (`apps/api/src/modules/demandes/ports/ged.port.ts`) n'expose aujourd'hui que `stocker`/`supprimer`, aucune méthode de lecture. Servir le fichier exige donc d'étendre le **port**, pas juste d'ajouter une route qui l'appellerait : un `GedPort.lire(gedRef): Promise<Buffer>` (ou équivalent stream) à ajouter à l'interface ET à `GedStubAdapter`, avant que la route `GET /api/demandes/{id}/pieces/{pieceId}` ait quoi que ce soit à appeler. La GED est un bouchon commutable depuis la Phase 1 (`Ports d'intégration`) — un vrai adaptateur GED (Orange) devra implémenter cette même méthode. `PiecesTab` affiche nom/type/taille et permet ajout/suppression, sans lien de téléchargement : absence constatée, pas un lien mort ajouté en devinant une route. Ouverture de la future route : même principe que le reste du dossier (docs/06 §4) — la pièce appartient à un dossier déjà lisible.
- **Aucune route ne liste ou ne recherche les utilisateurs — trois besoins distincts qui appellent la même route absente (Phase 9.2), regroupés ici plutôt que traités séparément.** (1) `POST /api/taches/{id}/deleguer` (`TacheActionBanner`) exige `delegataireId` (uuid) sans aucun moyen de le résoudre depuis un nom/identifiant AD — un champ texte demandant un UUID brut aurait été une UX qui ne fonctionne pas, omis plutôt que construit sur une supposition. (2) La résolution `agentClaimId` → nom pour un collègue de corbeille (`CorbeillesScreen`) reste une comparaison d'identifiants (« récupéré par vous » vs générique), jamais un nom, faute de route. (3) L'onglet « Utilisateurs » de la maquette `AdminScreen` (`docs/design/screens3.jsx`, `UsersView`) n'a aucune route derrière — exclu du rendu, pas remplacé par un onglet vide. Les trois se résolvent par la MÊME route manquante (recherche/listing d'utilisateurs, probablement bornée par rôle ou par corbeille plutôt qu'un annuaire ouvert — question de portée à trancher en même temps que la route elle-même) : à traiter comme un seul chantier, pas trois tickets séparés.
- **Le contrôle a posteriori N1/N2 est-il prévu sur un circuit, ou seul le contrôle FRA (R12, > 5 M) s'applique ?** Trouvé en construisant `ControleScreen` (Phase 9.2) : `ControleService.NIVEAU_PAR_ROLE` sait mapper trois rôles (`FRA`/`CONTROLE_N1`/`CONTROLE_N2`) vers l'enum `EnumNiveauControle`, les trois existent au catalogue (`roles.seed.ts`) — mais `packages/database/prisma/seed/referentiels/paliers.seed.ts` ne câble QUE `FRA` dans une `EtapeRegle` (déclenché par le montant TTC > 5M, R12, indépendant du circuit). Aucune `ConfigurationCircuit` ne porte `CONTROLE_N1` ou `CONTROLE_N2` : dans l'état actuel du référentiel, **aucun dossier, sur aucun circuit, ne peut produire de tâche de contrôle N1 ou N2** — le mécanisme de contrôle a posteriori se réduit en pratique au FRA de R12. Ce n'est pas un défaut de code (le service et le seed reflètent fidèlement les sources disponibles en Phase 8), mais une fonctionnalité livrée partiellement inerte. Question business, pas technique, à rapprocher de la question des « 34 rôles maquette vs 25 seedés » (`docs/design/DIVERGENCES.md`, même famille : référentiel incomplet ou surnuméraire selon le cas) : soit N1/N2 doivent être configurés (il manque des paliers dans les fiches de subdélégation), soit ils ne servent jamais et `CONTROLE_N1`/`CONTROLE_N2` sont deux rôles morts à retirer du catalogue. `ControleScreen` n'invente aucun découpage par circuit en attendant — il affiche les niveaux réellement présents dans les données, sans ensemble présupposé (le CRUD admin des paliers, Phase 5.5, permet de configurer N1/N2 plus tard sans toucher au code).
- **Aucun dossier DOBB ou DXC au-dessus de 5 000 000 FCFA ne peut être soumis aujourd'hui — R12 rejette systématiquement.** Trouvé en cherchant un dossier réel pour tester `ControleScreen` (Phase 9.2) : `paliers.seed.ts` ne seede qu'un palier unique non borné pour `DOBB` et `DXC` (`seedPalierMetierUnique`, sans étape `FRA`) — seul `DF` a des bornes chiffrées et câble `FRA` dans ses brackets 5M–50M et >50M (04_MCD_MLD_PGD_PROD.md §3.3, le seul endroit où des seuils réels existent). Conséquence directe, pas hypothétique : un dossier B2B (DOBB) ou B2C (DXC) dont le TTC dépasse 5M reçoit systématiquement `422 R12_CONTROLE_FRA` à la soumission, sans recours — aucune configuration ne peut jamais satisfaire R12 pour ces deux circuits en l'état. Un blocage fonctionnel réel sur deux circuits sur trois, précisément sur les dossiers à fort montant, pas un simple manque de configuration abstrait. Le message d'erreur renvoyé (`"Contrôle FRA obligatoire au-delà de 5 000 000 XOF — absent du palier sélectionné."`, affiché verbatim par `NouvelleDemandeScreen`) est factuellement exact mais n'indique pas à l'initiateur qu'il s'agit d'une lacune de configuration hors de son contrôle plutôt que d'une erreur de saisie corrigible. **Question à porter au métier** : « R12 (contrôle FRA obligatoire au-delà de 5 M, PO6-07) est-il applicable aux trois circuits, ou spécifique au DF ? Si applicable partout, les paliers DOBB et DXC au-dessus de 5 M doivent inclure une étape FRA — en leur absence, tout dossier B2B ou B2C dépassant 5 M est aujourd'hui insoumissible. » Corrigeable sans code via le CRUD admin des paliers (Phase 5.5, PGD-042) dès que la réponse est connue — une décision métier, pas technique. À rapprocher directement du premier point de cette liste (« seuils et rôles définitifs de la fiche de subdélégation ») : ce blocage est la conséquence concrète de ce seuil jamais fourni, pas un problème distinct — les deux se résolvent ensemble le jour où les fiches de subdélégation réelles de DOBB/DXC arrivent.
- **`Controle` s'écrit depuis la Phase 8, ne se lit nulle part — même situation que les notifications avant cette semaine.** `ControleService.soumettre` crée une ligne `Controle` et renvoie son `ControleVue`, mais aucune route ne liste les contrôles déjà réalisés (ni par dossier au-delà de l'entrée `JournalAudit` correspondante, ni en agrégat). `ControleScreen` (Phase 9.2) n'affiche donc que la file en attente (`GET /api/taches?etat=POST_CLOTURE`), jamais de section « Contrôles réalisés » — absence constatée, pas une table vide construite en devinant une forme de réponse. À traiter comme étape API dédiée (probablement `GET /api/controle?...` avec pagination) quand un lot backend sera repris, même famille de trou que `GET /api/notifications` avant sa fermeture.
- **Aucun flux d'activité récente transversal n'existe — même lot, trouvé au même moment.** `GET /api/audit/{demandeId}` (`AuditController`) est scopé à UN seul dossier ; `GET /api/audit/securite` est un flux transversal mais sémantiquement différent (journal de SÉCURITÉ — connexions, refus RBAC/SoD — pas les actions métier « approuvé »/« rejeté »/« soumis »), et réservé `ADMIN_PGD`. Il n'existe aujourd'hui aucune route qui agrège `JournalAudit` à travers plusieurs dossiers pour un flux « activité récente » de type tableau de bord. La section « Activité récente » de `docs/design/screens1.jsx` (`HomeScreen`, fonction `recentActivity`) n'a donc pas été portée — omise plutôt qu'alimentée par un appel inventé. À traiter comme sa propre étape API (nouvelle route d'agrégation, avec sa propre décision de portée — probablement scopée aux dossiers où l'utilisateur est initiateur ou détient une tâche, à trancher explicitement, pas par défaut) plutôt que construite en passant pendant l'écran d'accueil.
- **`DELETE /api/demandes/{id}` (suppression physique d'un BROUILLON) — question fermée et implémentée (Phase 9.2).** Restreinte au statut `BROUILLON` (422 `DEMANDE_NON_SUPPRIMABLE` sinon) et à l'initiateur (`InitiateurDemandeGuard`, entrée dans `guard-coverage.spec.ts`). Suppression physique en cascade (`DemandeLigne`/`HistoriqueMontant`/`PieceJointe`, `onDelete: Cascade`), plus nettoyage explicite des fichiers GED réels (`GedPort.supprimer`, jamais couvert par la seule cascade SQL) ; aucune trace `JournalAudit` à trancher, `creer`/`definirLignes` n'y écrivent jamais. **Reste à faire, pas encore une lacune côté serveur** : `NouvelleDemandeScreen` (apps/web) ne câble pas encore d'action « Supprimer ce brouillon » — l'endpoint existe, l'écran ne l'appelle pas. La déférence de création au premier `PUT .../lignes` (déjà en place) réduit le flux de brouillons orphelins, mais ne couvre pas ceux déjà créés avant cette route — un nettoyage périodique ou un écran dédié reste à envisager si le stock existant devient un problème réel.
- **Renvoi/clôture d'un dossier rejeté — question fermée et implémentée (13/08/2026).** Cf. section dédiée « Renvoi ou clôture d'un dossier rejeté » ci-dessus (Mécanismes structurants) pour le mécanisme construit et vérifié. Plus une question ouverte.

**Deux points du même inventaire (Phase 10.6bis), investigués puis résolus, pas laissés ouverts** — cf. `docs/design/DIVERGENCES.md` pour le détail complet et les preuves :
- **Sidebar/nav et `DashboardScreen` (vues Initiateur/Validateur/Pilotage), masquage par `isInit`/`isVA`/`isCtrl`/`isValid`** : vérifié contre le catalogue réel (`SELECT code, type FROM role`) — `EnumTypeRole` est `METIER`/`PIVOT`/`SYSTEME`, aucune valeur `I`/`V`/`A`/`C` n'existe. Ces quatre indicateurs sont donc la **même invention de taxonomie** déjà rejetée pour les badges `RolesAdminTab` (catégorie 3) — pas une question UX à choix libre. L'affichage actuel (Sidebar : tous les items visibles, seul `ADMIN_PGD` gate le groupe Pilotage) est donc le comportement correct par précédent, pas à « corriger » en recréant cette taxonomie.
- **`DashboardScreen` — un vrai défaut trouvé, pas acté sans preuve** : les trois vues (Initiateur/Validateur/Pilotage) ont été capturées séparément (`aya.koffi`/`jean.brou`/`awa.diomande`) et comparées à `HomeScreen.tsx` réel. `SectionKpi` empile **toujours** « Mes dossiers initiés » et « Mes dossiers à traiter » (jamais de choix exclusif par rôle, contrairement au sélecteur segmenté de la maquette) — un vrai défaut d'implémentation, non corrigé dans cette passe (portée : inventaire, pas reconstruction d'écran). La portée « Pilotage » réelle est aussi plus étroite que la maquette (`ADMIN_PGD` strictement, vs `SUPERVISEUR`/`ADMIN`/`DF`/`DGA_DG` — `isPilote` — côté maquette) mais **cette restriction reste correcte**, cohérente avec la décision déjà actée pour `profil=pilotage` de `GET /api/kpi` (CLAUDE.md, section KPI) — aucune action requise sur ce point précis. Détail : `herve.aka`/`SUPERVISEUR` est une persona de démo inerte dans `docs/design/` (son propre `pruneRoles()` la vide de tout rôle) — ne pas l'utiliser pour tester un chemin pilotage/superviseur, utiliser `awa.diomande` (`DF`) à la place.
- **Formule TVA (assiette HT seul vs HT+TSC) — fermée, pas seulement construite : confirmation métier reçue et défaut basculé le 2026-08-11.** L'assiette est configurable par circuit (`ParametreCalcul.assietteTvaDefaut`) et par dossier (`Demande.assietteTva`) depuis Phase 10.6septies ; le défaut est désormais `HT` sur les trois circuits, cf. section dédiée ci-dessus (« Défaut `assietteTva`/`assietteTvaDefaut` — bascule confirmée »). Ne plus lister cette ligne comme ouverte dans un futur audit.
- **Convention R12 (FRA = `roleCode='FRA'` ET `typeActeur='C'`) — confirmation métier en attente sur la chaîne DF, contrainte de conception déjà posée pour quand le chantier sera lancé.** Cf. section « Convention R12 — contrôle FRA » ci-dessus pour la fragilité déjà documentée de cette convention par nom de rôle. **Contrainte de conception pour l'implémentation, à respecter quand le chantier rôles/chaînes sera lancé** (dépend de la reconstruction de la chaîne DF, encore en attente de confirmation) : remplacer la déduction par nom de rôle par un champ explicite et administrable sur `EtapeRegle` (ex. `estControleFra: boolean`, ou un type de contrôle énuméré plus large couvrant aussi N1/N2) — jamais une convention qui casse silencieusement si le rôle `FRA` est renommé. Rien construit sur ce point tant que la confirmation métier n'est pas arrivée.

---

## Tests contre référentiels (convention posée en revue de Phase 5)

Les tests d'intégration tournent contre le Postgres de dev réel (pas de mock), partagé entre fichiers de test **exécutés en parallèle par Jest** (pas de `runInBand`). Deux incidents distincts l'ont révélé : un test a un temps écrasé les 12 jours fériés seedés (pollution silencieuse, détectée seulement par comptage de lignes) ; un service de recalcul a planté en heurtant, dans un autre worker, une demande créée par un fichier de test différent (course, pas de corruption persistante). Deux modes d'échec différents, même cause racine : une écriture sur une table partagée sans convention d'isolement.

**Clé fermée** (espace de clés fixe, seedé — aucune fixture jetable équivalente n'a de sens) : `Circuit` (3 valeurs ENUM), `ParametreCalcul` (1 ligne par circuit), `ParametreGlobal` (clés seedées fixes), `CalendrierSla` (une seule ligne "Calendrier CI"), `Module`. Sur ces tables : **snapshot en `beforeAll`, restauration INCONDITIONNELLE en `afterEach`** — jamais un `try/finally` en fin de corps de test (si une assertion échoue avant la restauration manuelle, la donnée reste corrompue et contamine les tests suivants, avec un échec qui n'apparaît que plus tard, ailleurs, sans lien apparent).

**Clé ouverte** (`Role`, `Motif`/`PieceAfferente`, `ConfigurationCircuit`/`EtapeRegle` — l'admin peut créer une nouvelle ligne à volonté) : chaque test crée sa propre ligne jetable à clé unique (`TEST_..._${Date.now()}`) et la supprime en fin de test ; ne jamais lire-puis-écrire une ligne seedée partagée.

**Requêtes qui scannent un ensemble partagé sans le borner à ses propres lignes** (ex. `WHERE circuit = 'DOBB' AND statut = 'BROUILLON'`, sans filtre de propriété) : le code de production lui-même doit tolérer qu'une ligne du lot ait bougé entre la lecture et l'écriture (un utilisateur peut soumettre/supprimer son brouillon pendant qu'un admin change le taux) — `updateMany` avec la condition répétée plutôt qu'un `update` par id, cf. `AdminParametresCalculService.recalculerBrouillons`.

Vérifié en revue de Phase 5 : les 65 tests de `apps/api/test/` respectent cette convention (état au 21/07/2026) — seuls `Circuit`, `ParametreCalcul`, `ParametreGlobal`, `CalendrierSla` y sont réellement exercés en écriture, tous via snapshot/restore. Si une collision réapparaît malgré cette convention en Phase 6+, la base de test dédiée (schéma Postgres séparé, migré/seedé à part) devient l'option de repli — pas par précaution, mais sur constat.

**`JournalAudit` ne se nettoie JAMAIS en fin de test** (Phase 8, T6) — un middleware Prisma (`interdireMutationAudit`, `packages/database/src/audit-immuable.util.ts`) rejette structurellement tout `update`/`updateMany`/`delete`/`deleteMany`/`upsert` sur ce modèle, y compris depuis un test. Avant T6, huit fichiers de test faisaient `journalAudit.deleteMany({ where: { demandeId } })` en `afterEach` ; tous cassaient dès le middleware activé. Corrigé en retirant ces appels : `demande_id`/`tache_id` passent à `NULL` par `ON DELETE SET NULL` quand la demande/tâche de test est supprimée, et les lignes d'audit de test survivent, orphelines — exactement le comportement voulu en production. N'écrivez jamais de nettoyage `journalAudit.delete*` dans un nouveau test, il échouera.

**`pnpm typecheck` couvre aussi `test/` depuis la Phase 6** (`tsconfig.test.json`) — `tsc -p tsconfig.json` seul n'a jamais typé que `src` (`rootDir`), et `ts-jest` tourne en `isolatedModules` (transpile par fichier, sans vérification croisée) : un changement de signature de constructeur dans `src` pouvait rester invisible dans `test` jusqu'à l'exécution, voire jamais si le chemin de code concerné n'était pas exercé par le test. Trouvé en Phase 6 quand `TacheService` a gagné un troisième paramètre (`DelegationService`) sans que le test de claim ne soit mis à jour — le test passait quand même (`delegationService` valait `undefined`, jamais lu par les méthodes exercées). Ne jamais retirer `tsconfig.test.json` du script `typecheck`.

---

## Convention de commit

Deux niveaux :

1. **Commit d'étape** — à chaque étape numérotée terminée ET vérifiée (sweep vert : `lint` + `typecheck` + `build` + `test`). Une étape = un commit, créé sans validation préalable : c'est du travail déjà vérifié.
2. **Commit de clôture de phase** — après validation explicite de la phase. Marque le point de reprise stable.

**Ne jamais commiter :**
- du code dont le sweep n'est pas vert ;
- un travail en cours interrompu par une question posée à l'utilisateur ;
- des fixtures de test ou données de démo modifiées temporairement (cf. l'incident `calendrier_sla` en Phase 5 — douze jours fériés réels remplacés par une ligne unique pour un test destructif, jamais commité) ;
- des fichiers de configuration locale, secrets, `.env` réel, volumes Docker.

Vérifier le `.gitignore` avant tout commit (`node_modules`, `.pnpm-store`, `dist`, `.env`, `*.tsbuildinfo`, `coverage`, `.next` — tout ce qui relève de l'environnement local doit y figurer).

**Format des messages** — Conventional Commits, sujet en français, avec le numéro d'étape ou de phase :
```
feat(ui): Modal sur Radix Dialog (9.1)
fix(api): CorbeilleRoleGuard sur les routes de tâches (8.1)
chore(web): migration Tailwind v4 vers @theme (9.1)
test(worker): T8 volets ack et dead-letter (6.5)
docs: convention R12 et questions ouvertes
```
Types : `feat`, `fix`, `chore`, `test`, `docs`, `refactor`, `perf`. Portées : `api`, `worker`, `web`, `ui`, `database`, `config`, `contracts`, `messaging`, `docs`.

Le corps du message, quand il apporte quelque chose, dit : ce qui a été vérifié (en direct, en test, les deux) ; les bugs trouvés en chemin ; les points laissés ouverts. Pour un correctif de sécurité, le corps dit **ce qui était exploitable** — c'est ce qui rend l'historique lisible dans six mois :
```
fix(api): vérification d'appartenance à la corbeille (8.1)

Avant ce correctif, un utilisateur authentifié connaissant un id de tâche
pouvait la réclamer et la traiter hors de son rôle : le filtrage par rôle
n'existait qu'à l'affichage (TacheService.lister), jamais à l'action.
R4 non vérifié sur claim/unclaim/approuver/rejeter.

Vérifié en direct : agent hors rôle → 403 journalisé, agent du rôle → 200,
délégataire actif → 200, délégation expirée → 403.
```

---

## Phase 10 — Qualité (docs/05 §10, en cours)

### 10.1 — Couverture `src/modules/**` (baseline puis progrès ciblé)

Baseline mesurée en ouverture de phase : **73.84 % statements / 66.55 % branches / 64.31 % functions / 72.79 % lines**, sous la cible ≥80 %. Écart concentré sur `auth/` (33.09 %) plutôt que réparti uniformément — `session.service.ts` (20.8 %) et `rate-limit.service.ts` (33.3 %) portent respectivement la révocation de session JWT et le verrouillage anti-bruteforce, la couche la moins couverte du module le plus sensible (porte d'entrée de toute session applicative). Aucun test n'existait pour l'un ou l'autre avant ce tour.

Comblé par `apps/api/test/session-service.integration.spec.ts` (intégration réelle contre Redis, pas de mock — révocation immédiate vérifiée en supprimant la session Redis plutôt qu'en attendant l'expiration de la signature JWT, `marquerMfaSatisfaite`, `rafraichir` avec jeton invalide/session révoquée) et `rate-limit-service.integration.spec.ts` (seuil lu depuis l'environnement réel via `loadEnv()`, jamais supposé ; isolation par action et par identifiant — un utilisateur ne peut jamais consommer le quota d'un autre). `session.service.ts` : 20.8 % → 97.9 %. `rate-limit.service.ts` : 33.3 % → 100 %. `auth/** ` global : 33.09 % → 60 %.

**Ports à 0 % (`ldap.port.ts`, `mfa.port.ts`, `crm.port.ts`, `ged.port.ts`) vérifiés, pas exclus par supposition** : lecture des quatre fichiers confirme des interfaces TypeScript pures (effacées à la compilation) plus une seule ligne `export const XXX_PORT = "..."` chacune — le 0 % ne porte que sur cette ligne unique de constante, pas un trou de couverture réel. Aucun effort à y consacrer.

**Restant, volontairement pas traité dans ce tour** : `lignes/services` (47.77 %), `demandes.controller.ts` (57.74 %). Distinction posée avant d'y revenir : `CrmStubAdapter` (bouchon, remplacé avant recette, cf. « Ports d'intégration ») ne mérite pas le même investissement de couverture que `crm-import.service.ts` (logique métier qui consomme `CrmPort` via l'interface, survit intégralement au remplacement de l'adaptateur). Le même principe vaudra pour `GedStubAdapter`/`BscsAdapter`/`GaiaAdapter`.

### 10.2 / 10.4 — Pipeline CI (premier pipeline du projet)

**Aucune CI n'existait avant ce tour** (`.github/workflows` absent, confirmé par recherche, pas supposé) — chaque sweep de fin de phase de ce projet, jusqu'ici, a été rejoué manuellement sur un poste de dev. `.github/workflows/ci.yml` : services Postgres 16/Redis 7/RabbitMQ 3-management réels (pas de mock, cohérent avec la convention « tests contre référentiels »), configurés avec les identifiants/ports par défaut qu'`apps/api/test/setup-env.ts` et `apps/worker/test/setup-env.ts` posent déjà (`??=`) — aucune variable `DATABASE_URL`/`REDIS_URL`/`RABBITMQ_*` dupliquée dans le workflow, la coïncidence avec les mêmes valeurs qu'un `pnpm test` lancé depuis un poste de dev est délibérée.

**T1 (claim concurrent, PGD-051/R7) posé comme étape dédiée et nommée**, avant la suite complète — pas noyé dans un résumé générique : jusqu'ici rejoué uniquement à la main (« rejoué cinq fois pour écarter le flakiness », Phase 6). Un échec futur s'identifie immédiatement dans l'UI CI comme « la concurrence du claim est cassée », et ne peut pas disparaître silencieusement si la tâche `test` générique est réorganisée plus tard.

**Vérifié en conditions réelles avant d'être considéré fiable, pas seulement écrit** : trois conteneurs Postgres/Redis/RabbitMQ jetables montés sur des ports distincts de la stack de dev (pour partir d'un état réellement vierge, pas d'une base déjà migrée/seedée), migrations + seed rejoués dessus, puis suite complète — `apps/api` (35/35 suites, 195/195 tests, `ldap-provider.integration.spec.ts` et `si-service.integration.spec.ts` compris, aucun worker actif pour entrer en course avec ce dernier) et `apps/worker` (7/7 suites, 25/25 tests, RabbitMQ réel — topologie, si-push, sla-escalation, locks-sweeper, notifications, retry). Conteneurs jetables détruits après vérification.

**Le run #1 (premier push réel) a échoué — la validation locale précédente n'était pas la reproduction fidèle qu'elle prétendait être.** Étape « Appliquer les migrations » en échec. Cause confirmée, pas supposée : `apps/api/test/setup-env.ts`/`apps/worker/test/setup-env.ts` ne s'exécutent que comme `setupFiles` Jest — ils ne couvrent jamais les étapes qui invoquent Prisma en CLI brute (`migrate`, `seed`), qui tournent avant tout process Jest et sans `.env` (fichier gitignored, absent d'un checkout propre ; `packages/database/.env` existe aussi en local et est gitignoré de la même façon, découvert à cette occasion). Le commentaire du workflow affirmant qu'aucune variable `DATABASE_URL`/`REDIS_URL`/`RABBITMQ_*` n'était nécessaire était une supposition non vérifiée, pas un fait — et la « validation locale contre un environnement vierge » réalisée avant le premier push avait involontairement masqué exactement ce trou : chaque commande avait été lancée avec un export manuel de ces variables dans le shell, jamais rejouée depuis le fichier de workflow tel qu'il est réellement écrit. C'est précisément ce que le push réel, demandé explicitement plutôt que déduit d'une répétition locale, a fait remonter — la différence entre une hypothèse et une garantie.

**Corrigé** en posant `DATABASE_URL`/`REDIS_URL`/`RABBITMQ_*` au niveau du job, alignées sur les services déjà définis. **Revalidé localement une seconde fois, plus rigoureusement** : les deux fichiers `.env` (racine et `packages/database/.env`) temporairement déplacés hors du dépôt (pas seulement des variables shell explicites, qui avaient déjà failli une fois à représenter fidèlement le fichier de workflow), sur des conteneurs Postgres/Redis/RabbitMQ jetables distincts de la stack de dev — `generate` → `migrate` → `seed` → T1 → suite complète `apps/api` (35/35) et `apps/worker` (7/7), tout vert sans aucune variable héritée d'un `.env` local. `.env` restaurés, conteneurs détruits après vérification.

**Run #2, sur un vrai runner GitHub Actions, toutes les 17 étapes en succès** — `checkout` → `pnpm install` → `prisma generate` → migrations → seed → `lint` → `typecheck` → `build` → **T1 (SF-PGD-072, R7)** → suite complète (`api`+`worker`+`ui`). Premier pipeline de ce projet réellement vert de bout en bout sur une infrastructure CI, pas seulement reproduit localement.

### 10.3 — e2e par circuit, en HTTP réel

Formalise le parcours vérifié à la main en clôture de Phase 9 (connexion, création, lignes, aperçu de routage, soumission, claim+approbation, contrôle a posteriori) — un fichier par circuit, **véritables requêtes HTTP via `supertest`** contre l'app Nest complète (`apps/api/test/helpers/e2e-app.ts`, même montage que `main.ts` : cookie-parser, préfixe `/api`, filtre d'exception, intercepteurs), pas des appels de service directs. Authentification par session mintée directement (`SessionService.creerSession`, cookie posé sur la requête) plutôt que par un login LDAP réel — l'authentification AD/MFA est déjà couverte ailleurs (`ldap-provider.integration.spec.ts`, `LoginScreen`), ce que ces e2e vérifient est le **circuit** une fois authentifié, pas l'authentification elle-même. `CorbeilleRoleGuard`/`RbacGuard` ne relisent jamais `MembreRole` en base (confirmé en lisant les deux guards, pas supposé) — ils font confiance aux rôles portés par le JWT de la session, donc aucun bootstrap LDAP/`MembreRole` n'est nécessaire pour ces tests.

**Ferme un angle mort structurel déjà documenté trois fois cette phase** (bug d'enveloppe HTTP, collision d'ordre de routes, route d'audit inatteignable — CLAUDE.md, Phase 9.2) : aucun test de ce projet n'appelait ces routes en HTTP réel avant ce tour, seulement au niveau service. `dobb-circuit.e2e-spec.ts` / `dxc-circuit.e2e-spec.ts` / `df-circuit.e2e-spec.ts` sont les trois premiers à le faire pour le parcours de validation complet.

**DOBB et DXC restent délibérément sous 5M (R12)** — aucun palier DOBB/DXC ne câble d'étape FRA (confirmé par requête directe contre les paliers seedés avant d'écrire ces tests, pas supposé), question métier non tranchée (cf. Questions ouvertes, « Aucun dossier DOBB ou DXC au-dessus de 5 000 000 FCFA ne peut être soumis aujourd'hui »). Le parcours nominal de chaque test ne dépend donc pas d'un arbitrage à venir. Le rejet R12 au-dessus du seuil est vérifié **explicitement**, dans son propre test par circuit (`422 R12_CONTROLE_FRA`), plutôt que simplement évité — si les paliers DOBB/DXC gagnent un jour une étape FRA, ce test précis échouera et signalera le changement, pas un silence.

**DF couvre le seul PALIER (5M–50M) réellement doté d'un contrôle FRA aujourd'hui — pas « le seul palier » au sens où DF n'en aurait qu'un.** Affirmation corrigée : DF a en réalité **trois paliers réels** (0–5M, 5M–50M, >50M — vérifié en base, `configuration_circuit`, pas supposé), avec des chaînes de longueur différente (3, 5 et 6 étapes respectivement — le palier haut ajoute `DGA_DG` entre `DF` et `FRA`, R2). Le test `df-circuit.e2e-spec.ts` porte un montant de 6,5M HT (même ordre de grandeur que le dossier `DF-2026-AF5715` vérifié à la main en Phase 9), donc **exclusivement le palier intermédiaire** — cinq étapes (`RESPONSABLE_DF`/`MANAGER_DF`/`MANAGER_SENIOR_DF`/`DF` bloquantes, `FRA` en contrôle post-clôture). Acteur du contrôle FRA délibérément distinct du dernier approbateur bloquant (`DF`) — R24 (cf. section dédiée ci-dessus) interdirait sinon le contrôle par la même identité ; reproduit ici par construction, pas revérifié (R24 est déjà couvert unitairement par `sod-service.integration.spec.ts`).

**Dette de couverture — comblée (Phase 10.6, décomposition NouvelleDemandeScreen).** Trouvée en vérifiant le nombre de paliers DF, pas en auditant 10.3 pour elle-même : le parcours manuel de clôture de Phase 9 n'avait exercé que le palier intermédiaire (dossier `DF-2026-AF5715`, 6,5M HT), et `df-circuit.e2e-spec.ts` avait été écrit contre ce même cas — un seul des trois paliers réels de DF avait donc jamais été exercé de bout en bout, ni manuellement ni automatiquement. Comblé par deux fichiers dédiés, même discipline d'isolement par cas que le reste de cette phase : `df-circuit-palier1.e2e-spec.ts` (0–5M, HT 2M, chaîne à 3 étapes — `RESPONSABLE_DF`/`MANAGER_DF`/`MANAGER_SENIOR_DF` — et preuve négative explicite qu'aucune tâche `FRA` n'est jamais instanciée sur ce dossier, complémentaire du test R12 qui prouve le déclenchement au-dessus du seuil) et `df-circuit-palier3.e2e-spec.ts` (>50M, HT 45M, chaîne à 6 étapes avec `DGA_DG` bloquante en avant-dernière position avant `FRA`, contrôle FRA mené par une identité distincte de `DGA_DG` par construction — R24). `df-circuit-palier3.e2e-spec.ts` est le premier test à exercer le rôle `DGA_DG` (identité persistante `dga.dg@orange.ci` ajoutée après coup au socle, jamais utilisée avant ce fichier — cf. section dédiée). Vérifié : les deux nouveaux fichiers passent isolément et aux côtés des cinq specs 10.3 existants (6 suites, 17 tests, aucune interférence), sweep complet (`typecheck`/`lint`/`build`) vert.

**Preuve que le test DF mord réellement, pas une hypothèse** : `RuleEngineService.instancierChaine` temporairement saboté (`if (false && etape.typeActeur === "C")`, empêchant toute instanciation de tâche de contrôle) → le test échoue précisément sur l'assertion `statut === "VALIDE"` (reste à `SOUMIS`) ; revert → vert. Les cinq tests (`dobb`×2, `dxc`×2, `df`×1) tournent aussi bien isolément qu'en parallèle (Jest, un process par fichier, aucune interférence observée).

**Intégrés au pipeline CI comme étape dédiée**, immédiatement après T1 — même traitement, pas noyés dans la suite générique : `pnpm --filter @pgd/api test:e2e` (`**/test/**/*.e2e-spec.ts`, script déjà présent dans `apps/api/package.json` avant ce tour, jamais utilisé jusqu'ici). Tournent contre les mêmes conteneurs jetables que T1/la suite complète — pas la stack de dev persistante, aucune raison de s'en écarter une fois le pipeline en place.

### 10.1 (suite) — `lignes/services` et `demandes.controller.ts` : le seuil ≥80 % franchi

Baseline Phase 10.1 (73.84 %) → **81.05 %** sur `src/modules/**` (statements) après ce lot, en plus de la tranche `auth/` déjà traitée plus haut.

**`lignes/services`** : 47.77 % → 95.55 %. `compte.service.ts` (37.5 % → 100 %, `compte-service.integration.spec.ts`, contre les comptes réellement seedés — pas de fixture jetable, lecture seule). `ligne.service.ts` (63.41 % → 87.8 %, `trouverParId`/`formulesDeLigne` ajoutés à `ligne-service.integration.spec.ts`). `crm-import.service.ts` (33.33 % → 100 %, `crm-import-service.integration.spec.ts`) — **`CrmPort` mocké, pas `CrmStubAdapter` délégué**, décision explicitement actée avant d'écrire ce test : `CrmStubAdapter` est un bouchon voué à être remplacé avant recette (`CRM_PROVIDER`), y investir de la couverture durcirait du code jetable ; `CrmImportService` — ce qu'il fait de ce que le port renvoie (upsert idempotent, résolution de la formule courante) — survit intégralement au remplacement de l'adaptateur, c'est cette logique-là qui est couverte, contre un mock respectant strictement la forme `CompteImporte[]`. Même principe à réappliquer pour `GedStubAdapter`/`BscsAdapter`/`GaiaAdapter`.

**Test de concurrence abandonné, pas masqué — dette distincte de T1, pas redondante avec lui.** Une tentative de couvrir le catch `P2002` de `assignerFormuleCourante` par deux appels concurrents réels (même schéma que T1) s'est révélée flaky (3 échecs sur 5 relances) — la contrainte partielle `uq_formule_courante` ne produit pas de conflit de façon fiable sous l'isolation READ COMMITTED par défaut de Postgres à cette vitesse. Retiré plutôt que gardé « la plupart du temps vert » (même discipline que l'incident `calendrier_sla`, Phase 5 : un test qu'on ne peut pas faire confiance à chaque run n'a pas sa place dans la suite).

Ce n'est **pas** la même garantie que T1 : `TacheService.claim` pose un verrou Redis explicite *avant* toute écriture Postgres (contention absorbée en amont, déterministe par construction — c'est ce qui rend 50 requêtes concurrentes fiables) ; `assignerFormuleCourante` n'a aucun verrou équivalent, seulement la contrainte base + un `catch` générique côté service. **Dette réelle, pas comblée** : rien ne vérifie aujourd'hui, sous une contention authentique, que ce `catch` traduit fidèlement la collision en `422 R19_FORMULE_COURANTE_UNIQUE` plutôt que de laisser fuiter une erreur Prisma brute — seule la préservation de l'intégrité en base (un seul gagnant) est prouvée, au niveau contrainte, séquentiellement, pas via le service sous concurrence réelle. Si R19 devient un point sensible (comme le claim l'a été pour R7), le même double-verrou Redis+Postgres que `TacheService.claim` serait le correctif naturel — pas seulement un test plus robuste.

**`demandes.controller.ts`** : 57.74 % → **100 %** statements/lignes/fonctions (73.46 % branches). Complété en deux temps : les trois e2e par circuit (10.3) couvraient déjà `creer`/`definirLignes`/`apercuRoutage`/`soumettre`/`obtenirDetail`/`listerTaches` en HTTP réel, faisant mécaniquement remonter la couverture de 57.74 % à 74.64 % avant tout travail dédié. `demandes-controller.e2e-spec.ts` (nouveau) ferme le reste : `lister` (scopé `profil=initiateur`), `DELETE` (brouillon), `PATCH` (modification simple), `POST /calcul` (recalcul), `POST /abandonner`+`/rappeler`, pièces jointes (upload/suppression multipart), état SI (lecture + rejeu manuel `ADMIN_PGD`-only avec la découverte que `siEtat` par défaut est `EN_ATTENTE`, pas une valeur "non applicable" supposée), et le 404 de `apercu-routage` sur un id inconnu.

**Découverte en écrivant le test, hypothèse initiale fausse, corrigée avant d'être documentée** : `abandonner`/`rappeler` rejettent un `BROUILLON` (422 `DEMANDE_NON_ELIGIBLE` — `terminerSiAucuneDecision` exclut explicitement `BROUILLON`/`ABANDONNE`/`VALIDE`/`REJETE`), corrigé en soumettant réellement le dossier avant d'appeler ces routes dans le test. **Ce n'est PAS un trou du cycle de vie** — un brouillon reste supprimable via `DELETE /api/demandes/{id}`, question déjà fermée et implémentée en Phase 9.2 (restreinte au statut `BROUILLON` et à l'initiateur), reprouvée en HTTP réel par ce même lot de tests (`"DELETE /api/demandes/{id} — supprime un brouillon appartenant à l'initiateur"`, vert). Les deux opérations sont complémentaires, pas concurrentes : `DELETE` pour un brouillon jamais engagé dans un circuit, `abandonner`/`rappeler` pour un dossier déjà soumis — chacune a exactement le périmètre où elle a un sens métier, aucune n'a de trou à combler ici.

### 10.5 — OpenAPI générée depuis les schémas Zod réels (docs/05 §10.5)

`apps/api/src/common/swagger/zod-schema.ts` (`ApiZodBody`/`ApiZodQuery`/`ApiZodResponse`) dérive la documentation Swagger directement des schémas Zod déjà utilisés par chaque contrôleur pour parser body/query — jamais un DTO Swagger resaisi à la main en parallèle, qui diverge du comportement réel dès le premier changement de contrat non répercuté (même mode de défaillance que `route-order.spec.ts`/`envelope-contract.spec.ts` traquent côté routes, appliqué ici côté documentation). `$refStrategy: "none"` — schéma inline par opération, pas de registre `components.schemas` partagé (suffisant pour 61 routes sur 18 contrôleurs ; à revoir seulement si la taille du document devient un problème réel).

**18/18 contrôleurs annotés.** `TachesController` (dernier restant, 8 routes) suit exactement le même principe que les 17 précédents : `@ApiZodQuery`/`@ApiZodResponse` sur `lister` (le seul contrôleur, avec `demandes.lister`, dont le total est porté dans le DTO littéral `{taches, total}` plutôt que par l'enveloppe `{data, meta}` — `tachesListeReponseSchema` documente donc directement le retour réel, contrairement à `demandes.lister` qui n'a volontairement pas reçu de `@ApiZodResponse` faute de schéma correspondant à l'enveloppe). **Exception délibérée** : `deleguer` ne reçoit pas de `@ApiZodBody` — `creerDelegationRequeteSchema` ne valide que l'objet APRÈS fusion avec `roleCode` injecté serveur (jamais un champ client réel) ; le documenter tel quel aurait présenté à tort `roleCode` comme un champ attendu du client.

**Bug de placement `eslint-disable-next-line` trouvé et corrigé avant de clore ce lot** : le commentaire multi-lignes justifiant le `any` de `zodToJsonSchema(schema as any, ...)` (TS2589, inférence trop profonde) était posé au-dessus de la signature de fonction, pas au-dessus de la ligne portant réellement le `any` — `eslint-disable-next-line` ne désactive que la toute prochaine ligne après le marqueur, jamais un bloc de commentaires qui le suit. Résultat : le warning ESLint restait actif sur la vraie ligne, invisible tant que `pnpm lint` n'avait pas encore été exécuté sur ce fichier. Déplacé juste au-dessus de la ligne concernée.

**Script d'export (`apps/api/scripts/export-openapi.ts`) — piège Nest silencieux, pas un bug du script.** Première exécution : `NestFactory.create(AppModule, { logger: false })` termine le process avec un code de sortie 1, **sans aucune sortie console** — aucune exception attrapée par le `.catch()` du script, aucun `uncaughtException`/`unhandledRejection`. Cause, confirmée en retirant temporairement `{ logger: false }` (pas supposée) : `loadEnv()` (`packages/config`) rejette l'environnement (`REDIS_URL`/`RABBITMQ_*`/`JWT_SECRET`/`LDAP_*`/`DUO_*`/`TOTP_ENCRYPTION_KEY` absents) et lève une erreur — Nest l'attrape et l'imprime via son propre `Logger.error`, **entièrement supprimé par `logger: false`**, avant même que l'erreur ne remonte au `.catch()` du script (elle ne remonte jamais : Nest gère l'échec de bootstrap en interne et appelle `process.exit(1)` lui-même). Un script silencieux avec code 1 sur un bootstrap Nest raté n'est donc pas un signe de crash mystérieux, c'est la signature exacte de `logger: false` combiné à un environnement invalide.

**Cause racine de l'environnement invalide, la même que Phase 10.2** : aucun mécanisme (ni `main.ts`, ni ce script) ne charge `.env` dans `process.env` — `loadEnv()` lit `process.env` tel quel, jamais un fichier. En conteneur, Docker Compose injecte les variables directement (`environment:`) ; en Jest, `test/setup-env.ts` pose des valeurs par défaut (`??=`) ; **depuis l'hôte, hors test, rien ne charge `.env` automatiquement** — il faut l'exporter explicitement dans le shell avant d'invoquer un script qui appelle `loadEnv()`. Même leçon que le run CI #1 raté (Phase 10.2) : une hypothèse non vérifiée (« le script tourne, `.env` est à la racine ») n'est pas une garantie.

**Vérifié en direct, depuis l'hôte, `.env` exporté dans le shell** (`set -a && source .env && set +a`, conteneurs Postgres/Redis/RabbitMQ de dev déjà debout) : `pnpm --filter @pgd/api openapi:export` → `docs/openapi.json` généré, **61 routes documentées** sur 18 contrôleurs. Spot-check `POST /api/taches/{id}/approuver` : `requestBody` dérivé fidèlement de `approuverRequeteSchema` (`revue[].{champ,vu,correction}`), `responses.200` présent. Sweep complet ensuite : `pnpm --filter @pgd/api typecheck`/`lint`/`build` verts, `pnpm --filter @pgd/api test` — **211/211 tests, 37/37 suites** (dont `ldap-provider.integration.spec.ts`, exécuté depuis l'hôte).

`docs/openapi.json` est **versionné** (commité), pas régénéré à la demande — un artefact partageable et diffable, cohérent avec le commentaire du script lui-même. Régénérer après tout changement de contrat/contrôleur qui affecte une route documentée, avant de commiter.

---

## Sécurité — vulnérabilités CRITICAL des images Docker (Trivy, 17/08/2026)

Rapport Trivy sur les trois images (`api`/`web`/`worker`) : 8 vulnérabilités CRITICAL, identiques sur les trois (image de base `node:20-slim` partagée, Debian 12 bookworm). Vérification avant correction pour chacune — pas de patch aveugle. Traité uniquement les 8 CRITICAL, jamais les HIGH (136/image) ni les MEDIUM (260) — hors périmètre de cette passe.

**Prérequis découvert avant toute vérification : les trois Dockerfiles ne construisaient jamais réellement l'image de production.** `docker build --target runtime` échouait avec 343 erreurs `TS7006` (any implicite) sur `apps/api` avant tout correctif de sécurité — cause : `RUN pnpm --filter @pgd/contracts... --filter @pgd/api build` (les trois Dockerfiles avaient la même forme, adaptée à leur propre app) ne construit **ni** `@pgd/database` **ni** `@pgd/config` **ni** `@pgd/messaging`/`@pgd/ui` — le `...` en suffixe sur `@pgd/contracts` sélectionne les dépendances DE `contracts` (aucune), pas celles de l'app cible ; `@pgd/api` seul (sans `...`) ne tire pas non plus ses propres dépendances workspace. `packages/database/dist` restait donc absent, et `apps/api` n'a aucun autre chemin vers les types Prisma (`PrismaService` importe `PrismaClient` depuis `@pgd/database`, jamais `@prisma/client` directement — cf. `apps/api/src/infra/prisma/prisma.service.ts`). **Jamais détecté avant** : `.github/workflows/ci.yml` ne construit aucune image Docker (`grep -n "docker build" .github/workflows/ci.yml` — aucune occurrence), et `docker-compose.override.yml` (dev) cible `target: deps`, jamais `build`/`runtime`. Corrigé dans les trois Dockerfiles : `--filter <app>...` (suffixe sur l'app elle-même, pas sur contracts) — sélectionne l'app ET toutes ses dépendances workspace, construites dans l'ordre topologique, sans liste explicite à maintenir. Reconstruit et vérifié : `apps/api` boot complet contre le vrai réseau Docker (Postgres/Redis/RabbitMQ réels, toutes les routes mappées) ; `apps/web` sert `/login` en `200`. Sans ce correctif, aucune vérification par reconstruction n'aurait été possible pour les CVE ci-dessous — pas seulement pour `api`, `web` avait le même défaut (`@pgd/ui`, dépendance directe, jamais construit non plus).

| CVE | Paquet | Action | Vérifié comment | Risque résiduel |
|---|---|---|---|---|
| CVE-2026-33845, CVE-2026-42010 | `libgnutls30` 3.7.9-2+deb12u6 | `apt-get install --only-upgrade -y libgnutls30` ajouté au stage `base` des trois Dockerfiles (`apt-get update` d'abord — candidat u7 confirmé disponible dans les dépôts Debian security au moment du build, via `apt-cache policy`) | Reconstruction des trois images, `dpkg -l libgnutls30` → `3.7.9-2+deb12u7` sur les trois ; `apps/api` re-vérifié en boot complet après ce correctif | Aucun — correctif appliqué et confirmé |
| CVE-2026-13221, CVE-2026-42496, CVE-2026-57433, CVE-2026-8376 | `perl-base` 5.36.0-7+deb12u3 | Aucune action — aucun correctif Debian disponible (`apt-cache policy perl-base` : candidat = installé) | `grep -rn "perl"` sur les trois Dockerfiles et tout `apps/`/`packages/` (hors `node_modules`) — aucune invocation réelle, seuls des faux positifs (« proprement », commentaires) ; `perl-base` a `Essential: yes`/`Priority: required` (dpkg-query), aucune dépendance inverse listée — paquet de base de l'image, jamais appelé par l'application, retrait non tenté (un paquet Essential ne se désinstalle pas sans risque pour `dpkg`/`apt` eux-mêmes) | **Accepté** — composant transitif de `node:20-slim`, jamais invoqué par le code applicatif ni par aucun script du monorepo ; aucun correctif amont à ce jour |
| CVE-2023-45853 | `zlib1g` 1:1.2.13.dfsg-1 | Aucune action — CVE spécifique à l'API contrib MiniZip de zlib, pas à zlib lui-même ; aucun correctif Debian disponible non plus (candidat = installé) | Recherche exhaustive de toute bibliothèque d'archive ZIP (`adm-zip`/`yauzl`/`yazl`/`jszip`/`unzipper`/`archiver`/`minizip`) dans les `package.json` des trois apps + `packages/*` et dans `node_modules/.pnpm` — aucune ; aucune référence à « zip »/« Zip » dans `apps/api/src` | **Accepté** — MiniZip n'est utilisé par aucun composant de la stack (zlib lui-même reste présent, sans rapport avec ce CVE précis) |
| CVE-2026-59873 | `tar` (npm) 6.2.1 | Supprimé du stage `runtime` des trois Dockerfiles (`rm -rf /usr/local/lib/node_modules/npm /usr/local/bin/npm /usr/local/bin/npx`) | `tar@6.2.1` n'est **ni une dépendance directe ni transitive** de notre `pnpm-lock.yaml` (confirmé : aucune entrée `tar@` dans le lockfile, aucun `package.json` du monorepo ne le référence) — c'est un composant interne de l'installation `npm` elle-même, livrée avec l'image `node:20-slim` (`/usr/local/lib/node_modules/npm/node_modules/tar/package.json`, confirmé sur l'image construite). npm n'est **jamais invoqué au runtime** sur les trois apps (`CMD` lance `node`/`next` directement, jamais `npm run`) — vérifié en supprimant le dossier et en rebootant les trois images avec succès (`apps/api` boot complet, `apps/web` sert `200` sur `/login`) | Aucun — le composant vulnérable est retiré de l'image livrée, pas simplement mis à jour vers une version majeure jamais auditée pour notre usage |

**Pourquoi suppression plutôt que mise à jour pour `tar`** : aucun override/resolution pnpm ne peut agir sur cette copie — elle n'appartient pas à notre graphe de dépendances (`pnpm-lock.yaml` ne la référence pas), c'est un composant vendored à l'intérieur de npm lui-même. Un bump vers `tar@7.x` (breaking change majeur) n'aurait de toute façon aucun sens à appliquer à un binaire que nous ne construisons pas ; retirer npm (jamais utilisé après le build) élimine le CVE à la racine sans introduire de version jamais testée pour un usage réel.

**Reconstruction + reboot réel, pas seulement inspection statique** : les trois images `runtime` ont été reconstruites depuis zéro après les quatre correctifs ; `apps/api` démarrée contre le vrai réseau Docker Compose (Postgres/Redis/RabbitMQ réels, toutes les routes Nest mappées, aucune erreur) ; `apps/web` démarrée et interrogée en HTTP réel (`200` sur `/login`) ; `apps/worker` reconstruite et versions de paquets confirmées (boot complet non retesté, worker suit le même Dockerfile qu'api sans logique HTTP à sonder). Images de vérification jetables supprimées après usage (`pgd-*-prod-fixed`, `pgd-api-deps-scan`, `pgd-api-prod-scan`).

**Reste à faire, pas dans cette passe** : redemander un scan Trivy réel à l'équipe sécurité sur les images reconstruites pour confirmer la disparition effective des 8 CRITICAL — cette section documente ce qui a été vérifié localement (versions de paquets, boot réel), pas un résultat Trivy réel. Niveaux HIGH/MEDIUM non traités, à reprendre dans un second temps avec la même discipline.

**Conséquence non vérifiée du bug de filtre pnpm ci-dessus, à confirmer avant de considérer ce chantier clos** : la découverte que les trois `docker build --target runtime` échouaient jusqu'ici (jamais construits avec succès, jamais exercés par la CI de ce dépôt) pose une question qui dépasse le périmètre de ce commit — **quel processus produit réellement les images poussées vers Nexus ?** Deux possibilités, jamais tranchées ici :
- Si ce processus est un pipeline CI/CD séparé de ce dépôt (Jenkins, GitLab CI externe, etc.) qui construit lui-même l'image à partir de ces Dockerfiles, il faut confirmer explicitement qu'il pointe bien sur la version corrigée ce soir (`--filter <app>...`, upgrade `libgnutls30`, retrait de `npm`) — pas une version antérieure mise en cache ou un checkout figé sur un ancien commit.
- Si un tel pipeline existe et construisait malgré tout des images exploitables jusqu'ici (le rapport Trivy prouve qu'une image a été scannée avec succès, donc *une* image a bien été produite quelque part), c'est la preuve que **ce pipeline n'utilise pas ces Dockerfiles tels qu'ils sont dans ce dépôt** — sans quoi le même échec `TS7006` s'y serait produit. Ce qui construit réellement l'image Nexus est donc soit un Dockerfile différent (non présent ici, à localiser), soit une étape de contournement (`--filter` différent, image pré-construite réutilisée) — à identifier avant de supposer que ce correctif s'appliquera au prochain déploiement.

**Rien construit ni supposé ici** : sans accès au pipeline de déploiement réel (hors du périmètre de ce dépôt et de cette session), impossible de vérifier lequel des deux scénarios est le bon. Tant que ce n'est pas confirmé, le risque concret est double : soit le prochain build de déploiement échoue purement et simplement (si le pipeline utilise bel et bien ces Dockerfiles, jamais construits avec succès avant ce soir), soit il réussit mais reproduit une image non corrigée (`tar`/`libgnutls30` toujours vulnérables, si le pipeline utilise un chemin de build différent qui n'a jamais vu ce commit). Action attendue, hors périmètre de ce dépôt : identifier le pipeline réel qui pousse vers Nexus et confirmer qu'il consomme bien `apps/*/Dockerfile` tels que corrigés ici, avant le prochain déploiement.

---

## Migration image de base Node 20 → 24 (17/08/2026)

Suite directe du chantier Trivy ci-dessus, pas un sujet séparé. `node:20-slim` est en fin de vie officielle depuis le 30/04/2026 (aucun correctif de sécurité à venir, de façon définitive). Cible : Node 24, LTS active (support jusqu'en avril 2028) — Node 22 écartée (déjà en maintenance, fenêtre de support plus courte), Node 26 écartée (pas encore LTS avant octobre 2026).

**Vérification de compatibilité, avant tout changement de Dockerfile** — rapport complet produit et validé avant construction, même discipline que pour le chantier Trivy :
- Aucun `engines.node` du monorepo (seul le `package.json` racine en déclare un, `>=20`, sans plafond) n'exclut Node 24 ; toutes les dépendances clés inspectées (`jest`, `ts-jest`, `next`, `@nestjs/cli`, `@prisma/client`/`prisma`, `ioredis`, `amqplib`) déclarent des bornes ouvertes.
- Aucun module natif installé dans `node_modules` (aucun `.node`, aucun `binding.gyp`) — `@tailwindcss/oxide` (build-time only, `apps/web`) est basé napi-rs, conçu pour la stabilité ABI inter-versions Node, pas un risque. Prisma utilise un moteur binaire autonome, insensible à la version de Node.
- Zéro appel `fetch()` réel dans le code applicatif (les seuls hits grep étaient `channel.prefetch()` d'amqplib, un faux positif de sous-chaîne vérifié explicitement) — les changements Undici 7/fetch plus strict de Node 24 sont sans objet.
- Timers : usage limité à `setTimeout`/`setInterval`/`clearTimeout` standards — les API supprimées dans Node 24 (`timers.enroll()`/`unenroll()`/`active()`) ne sont utilisées nulle part.
- Crypto (`totp-secret-crypto.ts`) utilise `createCipheriv`/`createDecipheriv` (conservées), pas `createCipher`/`createDecipher` (supprimées). Zéro usage de `url.parse()`, `tls.createSecurePair`, `SlowBuffer`, flags `--experimental-permission`.
- Tous les `package.json` du monorepo sont en CommonJS implicite, uniforme — aucun risque d'interop ESM/CJS.
- `node:24-slim` confirmé présent sur Docker Hub (`docker manifest inspect`, manifestes `amd64`/`arm64`) avant utilisation, pas supposé.

Aucun point de rupture identifié. Étape intermédiaire par Node 22 jugée inutile — chaque rupture documentée du saut 20→22 (ESM/CJS, timers, crypto, permission model) vérifiée absente du code réel.

**`FROM node:20-slim` → `FROM node:24-slim`** dans les trois Dockerfiles — seule ligne changée, le reste (upgrade `libgnutls30`, retrait de `npm`, `--filter <app>...`) reste identique et continue de s'appliquer sur la nouvelle base.

**Sweep — lint/typecheck/build verts sur l'hôte** (Node 24.16.0, déjà la version installée sur ce poste) : `pnpm lint`/`pnpm typecheck`/`pnpm build`, 13/13 tâches Turborepo, aucune erreur.

**Suite de tests — flakiness réelle trouvée, investiguée à fond, pas ignorée.** `pnpm --filter @pgd/api test` (parallélisme par défaut de Jest, PowerShell, `.env` chargé) a échoué de façon variable sur six exécutions consécutives (36, 70, 21, 11, 14, 69 tests en échec selon la run), toujours avec la même famille de symptôme — `Can't reach database server`, `Connection is closed` (Redis), puis `read ECONNRESET` — **jamais** une assertion métier en échec (sauf un cas isolé, expliqué ci-dessous). Diagnostic mené avant de conclure, pas supposé :
- **Épuisement du pool de connexions Postgres écarté explicitement** : surveillance de `pg_stat_activity` pendant une exécution complète (parallélisme par défaut) — pic observé à 12 connexions, largement sous `max_connections=100`.
- **Contention par builds Docker concurrents écartée** : le premier échec coïncidait avec un build `web` encore en cours ; reproduit à l'identique après confirmation que plus aucun build ne tournait (`docker ps`, `pg_isready`, `redis-cli ping` tous verts juste avant le run).
- **Redémarrage propre de `postgres`/`redis` sans effet** — le symptôme persiste après un cycle `restart` complet des deux conteneurs.
- **Corrélation directe avec le parallélisme Jest** : `--maxWorkers=4` → 21 échecs (net progrès) ; `--runInBand` (série complète) → 11 échecs, **avec un changement de nature** — plus aucune erreur de connexion, uniquement une assertion `compte-service.integration.spec.ts` cohérente avec la convention documentée plus haut dans ce fichier (« tests contre référentiels », suite explicitement conçue pour tourner **en parallèle**, jamais en `--runInBand` — cet artefact-là n'est donc pas un bug non plus, juste un mode d'exécution non supporté par la suite).
- **Confusion écartée séparément, pas confondue avec ce qui précède** : `pnpm --filter @pgd/worker test` échouait aussi (`ECONNRESET`) tant que le conteneur `worker` dev tournait en parallèle de sa propre suite de tests — les deux consomment les mêmes files RabbitMQ/la même base. Une fois `docker compose stop worker` fait (comme pour la suite `api`, déjà la convention documentée), la suite `worker` passe à 100 % (7/7, 25/25, T8 compris) — un vrai défaut de méthode trouvé et corrigé, distinct de la flakiness `api` ci-dessus (`worker` était déjà arrêté pendant les six runs `api`).

**Conclusion honnête, pas forcée** : la cause la plus probable est une limite de débit du relais réseau de Docker Desktop sous Windows face à une rafale de connexions quasi simultanées (chaque worker Jest ouvre son propre pool Prisma + client ioredis au démarrage) — un nombre de connexions élevé, pas un nombre de connexions soutenu (d'où le pic Postgres à seulement 12). **Impossible de certifier à 100 % un lien avec Node 24** : aucune autre version de Node n'est installée sur ce poste (pas de nvm) pour un vrai contrôle A/B, et cette machine n'avait par ailleurs jamais été revérifiée sous forte charge Docker avant ce chantier. Élément à charge tout de même : l'historique de ce fichier documente de nombreux sweeps `234/234` parfaitement déterministes avec la même méthode (PowerShell + `.env`) sur ce même poste — jamais cette signature d'échec avant aujourd'hui. **Recommandation, pas une conclusion imposée** : si cette flakiness doit être définitivement close, la reproduire sur un poste avec Node 20 encore installé (contrôle réel) plutôt que déduire d'une seule machine. En attendant, la suite reste fiable à `--maxWorkers=4` ou en CI (runner Linux, pas de relais réseau Windows) — le code applicatif lui-même n'est pas en cause : aucun test n'a échoué sur une assertion métier en dehors de l'artefact `runInBand` déjà expliqué.

**Vérification live, en conditions réelles, pas seulement au niveau code** — trois images `runtime` reconstruites sur `node:24-slim`, démarrées contre le vrai réseau Docker Compose (Postgres/Redis/RabbitMQ réels) :
- `apps/api` : `GET /api/health/ready` → `postgresql:true, redis:true, rabbitmq:true, ad:true` (`mfa:false` attendu, DUO non configuré dans cet environnement jetable).
- `apps/worker` : logs de démarrage confirment `Consumers démarrés : q.locks-sweeper, q.sla-escalation, q.si-push, q.notifications` ; `GET /health/ready` → `rabbitmq:true`. « Rejeu de T8 » satisfait par la suite `apps/worker` elle-même (ci-dessus, 25/25 verts, RabbitMQ réel) — même mécanisme de consommation que celui exercé par le conteneur live.
- `apps/web` : `/login` sert `200` avec un vrai formulaire (champs identifiant/mot de passe, bouton submit) et des chunks JS `_next/static` qui se chargent en `200` — navigation/hydratation fonctionnelle, pas une coquille statique.

**Re-scan Trivy réel, pas documenté pour plus tard** — `trivy` non installé localement, mais l'image `aquasec/trivy` (Docker Hub) accessible et utilisée directement (`docker run --rm -v /var/run/docker.sock:/var/run/docker.sock aquasec/trivy image --severity CRITICAL,HIGH,MEDIUM <image>` — sur Git Bash Windows, préfixer `MSYS_NO_PATHCONV=1` pour éviter le même mangling de chemin déjà documenté ailleurs dans ce fichier). Comparaison directe, même Dockerfile (déjà corrigé, chantier précédent) sur les deux bases :

| Couche | Node 20 (Debian 12.13) | Node 24 (Debian 12.15) | Delta |
|---|---|---|---|
| OS (`os-pkgs`) — CRITICAL | 5 | 5 | 0 — les 5 mêmes (`perl-base`×4, `zlib1g`×1), déjà documentées comme risque accepté ci-dessus ; `libgnutls30` et `tar`/npm confirmés toujours absents sur les deux |
| OS (`os-pkgs`) — HIGH | 19 | 18 | −1 |
| OS (`os-pkgs`) — MEDIUM | 85 | 67 | **−18** |
| npm (`lang-pkgs`, `package.json`) — tout niveau | 61 (33 MEDIUM, 28 HIGH, 0 CRITICAL) | 61 (identique) | 0 — attendu, ce niveau dépend de `pnpm-lock.yaml`, pas de l'image de base ; **hors périmètre de cette migration**, une passe de mise à jour npm distincte serait nécessaire (`undici`, `sharp`, `qs`, `tmp`, `uuid` notamment repérés en HIGH/MEDIUM) |

Confirme l'objectif posé : **le passage à Node 24 réduit bien le compte de vulnérabilités du runtime lui-même** (−19 vulnérabilités OS au total, aucune régression CRITICAL), sans toucher au périmètre déjà traité (`libgnutls30`/`tar` restent corrigés) ni au périmètre npm applicatif (distinct, non couvert par ce chantier). Vérifié sur les trois images (`api`/`worker`/`web` partagent la même couche OS — confirmé identique, 5 CRITICAL sur les trois).

**Rappel du chantier précédent, toujours valable ici** : la question du pipeline Nexus reste entière — ce correctif (comme celui de `libgnutls30`/`tar`) ne s'appliquera au prochain déploiement que si le pipeline réel qui construit l'image Nexus consomme bien ces Dockerfiles.

---

## Incident — secrets exposés dans l'historique Git, socle d'identités disparu (17/08/2026)

Deux événements distincts, traités dans la même intervention parce que découverts l'un après l'autre, pas parce qu'ils partagent une cause commune établie.

### Rotation JWT_SECRET / REFRESH_TOKEN_SECRET / TOTP_ENCRYPTION_KEY

Un scan Gitleaks (branche `dev`) a signalé trois « Generic API Key » dans `.env.example`. Vérification par `git show` (pas par déduction) : le fichier actuel est déjà propre (placeholders factices depuis le commit `42f6046b`, 22/07/2026) — Gitleaks scanne l'historique complet, pas seulement HEAD, et a trouvé le commit **antérieur** au correctif (`b7de46a7`, scaffold initial), où `JWT_SECRET`/`REFRESH_TOKEN_SECRET`/`TOTP_ENCRYPTION_KEY` portaient de vraies valeurs hex à ces mêmes numéros de ligne (36/38/64 — correspondance exacte).

**Découverte allant au-delà de la question posée** : ces trois valeurs, bien que retirées du fichier exemple committé, étaient encore les valeurs **réellement utilisées** par le `.env` local de ce poste (jamais régénéré depuis le correctif de juillet) — confirmé aussi dans `docker-compose.yml` (interpolation `${JWT_SECRET}` etc.). Les conteneurs `api`/`worker` tournaient avec ces secrets exposés au moment de la découverte.

**Décision actée** : régénérer les trois immédiatement plutôt que différer (« différer ne réduirait pas le travail, seulement la durée d'exposition d'une clé déjà compromise »), et **ne pas réécrire l'historique Git** — une fois les secrets rotés, les anciennes valeurs dans l'historique sont inertes (connaître un secret qui n'est plus utilisé ne donne plus d'accès) ; réécrire aurait cassé les hachages de commit existants pour un bénéfice pratique nul.

**Fait** : trois nouvelles valeurs (`openssl rand -hex 32`) écrites dans `.env`, conteneurs `api`/`worker` recréés (`docker compose up -d --force-recreate`, pas un simple `restart` — l'interpolation `.env` ne se relit qu'à la création du conteneur). Vérifié directement : `docker compose exec api sh -c 'echo $JWT_SECRET'` renvoie la nouvelle valeur, `GET /api/health/ready` confirme la connectivité complète après recréation.

### Disparition du socle des 15 identités persistantes

En tentant de ré-enrôler le TOTP des 6 identités concernées par la rotation, découverte que la table `Utilisateur` était **entièrement vide** (0 ligne) — pas seulement les 6, les 15. `Demande`/`MembreRole` vides aussi. Tous les référentiels (`role`=35, `sous_flux`=7, `circuit`=3, `configuration_circuit`=6, etc.) et les journaux d'audit/sécurité (`journal_audit`=4209, `journal_securite`=1153) intacts — **ciblé, pas une réinitialisation de schéma**.

**Investigation poussée jusqu'à sa limite réelle, pas jusqu'à ce qu'elle s'arrête d'elle-même** :
- Le script de rotation TOTP lui-même écarté avec certitude, pas par déduction : relu ligne par ligne, une seule opération DB (`update` scopé par `identifiantAd` exact), a échoué dès sa première itération (identité déjà absente **avant** son exécution), jamais atteint les 5 autres identités, jamais émis de `DELETE`.
- Recherche exhaustive dans `apps/`, `packages/` (pas seulement les tests) : aucun `utilisateur.deleteMany()` non scopé nulle part dans le code actuel.
- Deux tentatives de `DELETE FROM utilisateur WHERE 1=1` trouvées dans les logs Postgres (11:37 et 11:40 UTC ce jour) — **toutes les deux échouées** sur la contrainte FK `demande_initiateur_id_fkey`. Aucune n'a pu vider la table.
- **Constat qui ferme cette piste précisément, pas par manque d'effort** : `SHOW log_statement` renvoie `none` — Postgres ne journalise le texte d'une requête que si elle échoue. Toute suppression **réussie**, aujourd'hui ou lors d'une session antérieure, est structurellement invisible dans ces logs. Ce n'est pas que la recherche n'a pas été assez loin ; l'outil ne peut pas répondre à cette question avec cette configuration.

**Conclusion** : disparition ciblée, cause exacte non déterminable avec les outils disponibles dans cette session. Aucune piste ne pointe vers le code, les tests ou les scripts actuels de ce dépôt. Hypothèse la plus probable au moment de la clôture de cet incident : intervention manuelle volontaire (le fait que `Demande` ait disparu avec `Utilisateur`, sans qu'aucun référentiel ne bouge, ressemble à un nettoyage réfléchi plutôt qu'à un accident ou une compromission) — **en cours de vérification séparément par la personne pilotant le projet au moment de la rédaction de cette entrée**, à mettre à jour si elle obtient confirmation.

**Effet de bord découvert en réparant, pas anticipé — `ligne.formuleCouranteId` nul sur les 5 lignes de démo.** En revérifiant la suite e2e après reconstruction des identités, les 7 specs `*-circuit.e2e-spec.ts`/`demandes-controller.e2e-spec.ts` échouaient toutes sur `PUT .../lignes → 400 VALIDATION_ECHOUEE` (`formuleId: Expected string, received null`). Cause : `ligne.formule_courante_id` était `NULL` sur les 5 lignes de démo, alors que la `Formule` correspondante (`courante=true`) existait bien pour chacune — un pointeur cassé, pas une donnée manquante, et touchant systématiquement les 5 lignes de la même façon (même famille d'anomalie « ciblée » que `Utilisateur`/`Demande`, pas un hasard isolé — élément supplémentaire pour l'investigation en cours côté métier). Réparé par un `UPDATE` direct rétablissant le pointeur vers la formule `courante` déjà existante (aucune recréation, `formule` restée à 8 lignes tout du long).

**Bug structurel trouvé et corrigé au passage, indépendant de la cause de la nullité** : `creerLigneAvecFormules` (`packages/database/prisma/seed/demo/demo.seed.ts`) sortait immédiatement (`if (existante) return`) dès que la ligne existait déjà — avant d'atteindre le code qui renseigne `formuleCouranteId`. Concrètement : quel que soit le nombre de fois où `pnpm db:seed` est rejoué, ce pointeur ne pouvait **jamais** s'auto-réparer une fois cassé, puisque toute ligne déjà créée court-circuitait la boucle de réassignation. Corrigé : la fonction vérifie désormais, pour une ligne déjà existante, si `formuleCouranteId` est manquant et le réassigne depuis la formule `courante=true` déjà en base — sans jamais recréer de formule ni casser l'idempotence sur la création.

**Vérifié en direct après les deux réparations** : suite e2e complète, 7/7 suites, 20/20 tests verts ; suite d'intégration complète, 39/39 suites, 234/234 tests verts.

### Reconstruction du socle (17/08/2026)

Les 15 identités reconstruites à l'identique — LDAP intact (vérifié séparément, 15 entrées avec les mêmes `cn`), seule la table Postgres `Utilisateur` avait disparu. Script jetable (même discipline que d'habitude, supprimé après usage) recréant `Utilisateur` + `MembreRole` avec les mêmes `identifiantAd`/rôles/noms que documentés plus haut dans ce fichier (table « Identités de test persistantes »), et régénérant un secret TOTP réel (même mécanisme que `TotpProvider.genererEnrolement`, chiffré avec la **nouvelle** `TOTP_ENCRYPTION_KEY`) pour les 6 identités qui l'exigent.

**Vérifié en direct, dans les deux sens, pour les 6 identités TOTP** : script e2e jetable — nouveau secret → `POST /api/auth/mfa/verify` accepté, cookie de session posé, `GET /api/auth/session` confirme le bon `identifiantAd` ; code généré avec un secret arbitraire différent (jamais le vrai ancien secret, qui n'a jamais été connu de cette vérification) → `401` propre. Les deux réparations de données (identités + `formuleCouranteId`) confirmées par un sweep complet vert (ci-dessus).

### Mesure de précaution — proposée, pas construite

La personne pilotant le projet a demandé une proposition de mécanisme léger rendant un futur incident de ce type visible et attribuable le jour même, avec le coût affiché avant toute décision. Deux options, pas exclusives l'une de l'autre :

1. **`log_statement = mod` sur l'environnement de dev** (journalise les requêtes de modification — INSERT/UPDATE/DELETE — pas seulement celles qui échouent, contrairement au réglage actuel `none`). Coût : verbosité des logs nettement plus élevée (chaque écriture, pas seulement les échecs — pour un environnement de dev avec une suite de tests qui écrit abondamment, cela peut représenter un volume de logs important sur une session de travail chargée) ; aucune modification de code ni de schéma, un seul paramètre Postgres (`postgresql.conf` ou `ALTER SYSTEM`) ; n'aurait pas empêché l'incident, mais aurait rendu **immédiatement lisible** la requête exacte qui a vidé `Utilisateur`, avec son origine (adresse, utilisateur DB) — exactement ce qui a manqué dans cette investigation.
2. **Export périodique du nombre de lignes des tables clés** (`Utilisateur`, `Demande`, `MembreRole`, `Ligne` avec `formuleCouranteId` non nul, plus les référentiels) — un script trivial (quelques requêtes `count(*)`, écrites dans un fichier horodaté ou une table dédiée), déclenché par exemple à chaque démarrage de la stack dev ou via une tâche planifiée légère. Coût : quasi nul en ressources ; ne dit jamais *qui* ni *comment*, seulement *qu'*un écart a eu lieu et *quand* il est apparu pour la première fois — un signal d'alerte, pas un journal d'audit. Utile même rétroactivement pour cet incident précis : aurait borné la fenêtre de recherche à quelques heures au lieu de « tout l'historique du conteneur ».

Les deux se combinent bien : l'export périodique dit *qu'*un problème est apparu et *quand* ; `log_statement=mod`, activé en continu ou seulement pendant la fenêtre suspecte une fois l'alerte donnée, dit *quoi* exactement. Aucun des deux n'est construit — en attente de décision.

---

## Sécurité — SonarQube (17/08/2026) et migration NestJS 10 → 11 (18/08/2026)

Suite du chantier de remédiation sécurité (Gitleaks/rotation de secrets déjà traité ci-dessus, puis SonarQube, puis dépendances npm). Même discipline : rapport avant correctif, jamais de correction sur un constat non localisé précisément.

**SonarQube — 6 constats, 2 faux positifs, 1 vulnérabilité réelle corrigée, 2 risques nuls documentés, 1 déjà couvert.**
- Faux positif : `COPY --from=build /repo /repo` suivi de `USER pgd` (les trois Dockerfiles) — fichiers restent `root`-owned, non réinscriptibles par le process runtime non privilégié. C'est le pattern sécurisé recherché ; `--chown` suggéré par Sonar l'aurait dégradé.
- Faux positif : « hardcoded password » — recherche exhaustive, seules des valeurs dev triviales déjà documentées (`LDAP_ADMIN_PASSWORD=admin`, `MotDePasseTest123!`, fixtures de test).
- **Réel, corrigé** : `COPY . .` (stage `build`, trois Dockerfiles) embarquait le vrai `.env` racine **et** `packages/database/.env` (secrets réels) dans l'image de production — confirmé par reconstruction + lecture directe du fichier depuis un conteneur runtime. Second vecteur d'exposition pour la même classe de secrets que l'incident de rotation JWT/refresh/TOTP ci-dessus, indépendant de l'historique Git. Corrigé via `.dockerignore` (`.env`, `**/.env`, `!.env.example`) — zéro impact fonctionnel, le conteneur runtime n'a jamais eu besoin du fichier embarqué (variables déjà injectées par `environment:` de docker-compose).
- Risque nul, documenté : `Math.random()` — 52 occurrences, exclusivement dans des fichiers de test (`apps/api/test`, `apps/worker/test`), toutes pour générer un suffixe unique de fixture jetable (convention « tests contre référentiels » déjà posée en Phase 5) — zéro occurrence dans `src/`, zéro usage sécurité-sensible.
- Risque nul, documenté : `docker cp ... /tmp/seed.ldif` (`docker/openldap/README.md`) — procédure manuelle, jamais automatisée, jamais en CI/prod, conteneur OpenLDAP mono-développeur/éphémère/non exposé au-delà de `localhost`, contenu déjà public (identités de test documentées ci-dessus).
- HEALTHCHECK ajouté aux trois Dockerfiles (constat LOW) dans le même commit que le correctif `.dockerignore` — `node -e` plutôt que `curl`/`wget` (image slim, éviter un paquet supplémentaire pour ce seul usage) ; `api` sonde `/api/health` (liveness publique, jamais `/ready` qui refléterait une panne Postgres/Redis/RabbitMQ/AD plutôt que le process) ; `worker` sonde `/health` ; `web` sonde `/login` (seule route publique connue à répondre 200 sans session). Vérifié en conditions réelles : les trois images reconstruites, démarrées contre le vrai réseau Docker Compose, `docker inspect` confirme `healthy` pour les trois.

**Dépendances npm — 16 paquets signalés par le scan, tous confirmés transitifs, aucune ligne `package.json` à modifier isolément.** `pnpm why -r` sur chacun : aucun n'est une dépendance directe de ce dépôt ; `pnpm outdated -r` ne les signale même pas, preuve que chacun est déjà à la version maximale que sa dépendance parente autorise dans la plage semver déclarée — un `pnpm add` isolé n'aurait donc rien changé. Recoupement réel effectué via `pnpm audit --json` (Trivy `fs` s'est révélé incapable de reconnaître `pnpm-lock.yaml` en `lockfileVersion: '9.0'` — confirmé en debug, `Number of language-specific files num=0` même avec le fichier isolé accompagné d'un `package.json` ; `pnpm audit`, natif, a servi d'instrument de remplacement). Tous vulnérables sans exception à la version actuellement résolue :
- **Résolu par l'écosystème NestJS** : `uuid` (worker, CVE-2026-41907) — `@nestjs/schedule` ≥5 remplace en interne `uuid` par `crypto.randomUUID()`, confirmé après bump (`pnpm why -r uuid` ne montre plus aucune entrée sous `@pgd/worker`) ; `fast-uri` (CVE-2026-18446) — via `@nestjs/cli`, inclus dans le même bump.
- **Via NestJS, sans garantie automatique** : `multer`, `lodash` (prod, via `@nestjs/swagger`), `file-type` (via `@nestjs/common`), `qs`/`body-parser` (via Express, devrait suivre le passage à Express 5).
- **Dev-only, jamais expédié en production** : `undici`×2, `uuid` (testcontainers), `js-yaml`/`brace-expansion` (`@typescript-eslint/eslint-plugin`), `ip-address`/`hono`/`@hono/node-server` (CLI `shadcn`, MCP SDK).
- **Production frontend, hors périmètre NestJS, pas traité dans ce chantier** : `sharp` (via `next`), `postcss`/`nanoid` (via `next`+`shadcn`/`@tailwindcss/postcss`) — nécessite un bump `next`/`shadcn` séparé, décision à prendre séparément.

**NestJS 10 → 11 — rapport de compatibilité avant tout bump, aucun point de rupture trouvé pour ce dépôt.** Vérifié contre les notes de version officielles GitHub (`nestjs/nest` v11.0.0, `nestjs/schedule` v5.0.0/v6.0.0), pas depuis la mémoire d'entraînement : Node ≥20 (déjà acquis), Express 5 dans `platform-express` (aucune route avec wildcard/paramètre optionnel dans ce dépôt — recherche exhaustive négative), typage `@Inject()` resserré (les 13 usages du dépôt utilisent tous une constante de token simple, non affectés), dépréciation `class-transformer`/`plainToClass` (sans objet — validation entièrement Zod, jamais `class-validator`), `@nestjs/config`/`@nestjs/cache-manager` breaking (sans objet — ni l'un ni l'autre n'est utilisé, `packages/config`/`CacheService` maison les remplacent déjà), `@nestjs/schedule` 4→6 (breaking uniquement Node≥20 + alignement interne, nos deux `@Cron` inchangés).

**Deux régressions de typage réelles trouvées à la compilation, pas anticipées par le rapport (bibliothèques tierces, pas des breaking changes NestJS documentés)** :
- **Express 5** élargit le type de `Request.params[x]` à `string | string[] | undefined` (contre `string | undefined` en Express 4) — 6 guards (`corbeille-role`, `delegant-membre-role`, `delegation-context`, `initiateur-demande`, `notification-destinataire`, `sod`) lisaient `request.params?.id` directement dans un `where: { id }` Prisma, qui exige `string`. Corrigé par narrowing explicite (`typeof request.params?.id === "string" ? request.params.id : undefined`) — jamais un cast aveugle, un id malformé retombe sur le comportement `undefined` déjà existant (fail-open documenté : « 404 est la responsabilité du contrôleur/service »).
- **`@nestjs/jwt` 11** type `expiresIn` en `StringValue` (type de `ms`, dépendance transitive jamais hoistée par pnpm — confirmée non résolvable en import direct) plutôt qu'un `string` large ; `JWT_EXPIRES_IN`/`REFRESH_TOKEN_EXPIRES_IN` (`packages/config`, Zod `z.string()`) sont toujours de la forme `"15m"`/`"7d"` en pratique. Corrigé par un type local `DureeJwt = \`${number}${"s"|"m"|"h"|"d"}\`` (assertion ciblée sur l'invariant réel du dépôt, pas un import fragile d'un type de dépendance transitive) dans `session.service.ts`.

**Effet de bord trouvé pendant le sweep, sans rapport avec le bump — `ligne.formuleCouranteId` de nouveau nul sur la ligne de démo `0102030401`.** Même symptôme et même ND que le côté-trouvaille déjà documenté dans l'incident de disparition du socle d'identités (ci-dessus) — le pointeur était de nouveau cassé au moment de ce sweep, cause non réinvestiguée (hors périmètre de ce chantier), corrigé simplement par un `pnpm db:seed` (le mécanisme d'auto-réparation déjà construit pour cet incident a fonctionné comme prévu, sans intervention manuelle). Preuve que ce garde-fou vaut la peine d'avoir été construit : il a absorbé une deuxième occurrence du même défaut sans nouveau correctif.

**Flakiness Jest déjà documentée (migration Node 24) reproduite à l'identique, pas une nouvelle régression** : premier sweep post-bump exécuté depuis PowerShell avec parallélisme par défaut → 8 suites/35 tests en échec, tous `Can't reach database`/erreurs de connexion, jamais une assertion métier ; reproductible avec un nombre et une identité de suites différents à chaque run — signature déjà caractérisée (« Migration image de base Node 20 → 24 » ci-dessus). `--maxWorkers=4` (contournement déjà documenté) ramène à un seul échec réel (le `formuleCouranteId` ci-dessus), résolu par le reseed.

**Vérifié en conditions réelles, pas seulement en sweep hôte** : les deux conteneurs dev (`api`/`worker`) resynchronisés (`docker compose run --rm -u root --entrypoint sh <service> -c "pnpm install --filter <app>... && chown -R node:node ..."` — même geste que l'incident `EACCES` déjà documenté ; `worker` crashait en boucle avant ce geste, `Cannot find module '@nestjs/cli/bin/nest.js'`, un `exec` classique était impossible sur un conteneur qui n'atteint jamais l'état `running`, d'où le détour par `docker compose run` sur une instance jetable partageant le même volume nommé). Les deux redémarrés proprement, toutes les routes remappées, `Nest application successfully started`. `curl` réel : `GET /api/health` → 200, `GET /health` (worker) → 200, `GET /api/health/ready` → `postgresql/redis/rabbitmq/ad: true` (mfa:false attendu, DUO non configuré en dev), `POST /api/auth/login` avec corps vide → 400 (validation Zod fonctionnelle sous Express 5).

Sweep complet final : `apps/api` 39/39 suites, 234/234 tests ; e2e 7/7, 20/20 ; `apps/worker` 7/7, 25/25 — zéro régression.

### Fuite `.env`/`.dockerignore` — portée réelle, vérifiée sur l'historique Git, pas supposée

Question posée explicitement après la clôture du chantier ci-dessus : le correctif `.dockerignore` a-t-il seulement corrigé un défaut latent, ou une image contenant le vrai `.env` a-t-elle réellement pu être construite et poussée pendant la fenêtre d'exposition ? Trois points, dans l'ordre où ils ont été posés.

**1. Contenu exact de l'ancien `.dockerignore`, pas un motif contourné.** `git log -p --follow -- .dockerignore` : le fichier a été créé au commit `b7de46a7` (22/07/2026 17:35, le même commit de scaffold qui portait aussi les secrets aujourd'hui rotés — cf. section précédente) avec neuf lignes, **aucune ne mentionnant `.env` sous quelque forme que ce soit** :
```
node_modules
**/node_modules
.pnpm-store
.turbo
**/dist
**/.next
.git
.claude
docs/design
```
Ce n'est donc pas un motif d'exclusion existant contourné par un `COPY` plus permissif — `.env` était **structurellement absent** de la liste, du tout premier commit qui a introduit ce fichier jusqu'au correctif `1c91455` (17/08/2026 16:18), soit **26 jours**, pendant lesquels `COPY . .` (présent dans les trois Dockerfiles depuis leur création, même commit `b7de46a7`) n'avait aucune raison de ne pas embarquer `.env`.

**2. Recoupement avec la question déjà ouverte « quel processus construit réellement les images Nexus ? » — pas résolu, mais borné plus précisément qu'avant.**

Chronologie exacte des quatre commits qui touchent `.dockerignore`/les trois `Dockerfile` (`git log --format="%h|%ad|%s" --date=iso`) :

| Commit | Date (UTC) | Effet sur la fuite |
|---|---|---|
| `b7de46a7` | 22/07/2026 17:35:52 | Création de `COPY . .` (les trois Dockerfiles) **et** de `.dockerignore` sans `.env` — les deux conditions de la fuite réunies dès ce commit |
| `5e184fd` | 17/08/2026 10:46:34 | Corrige le bug `--filter` qui empêchait `docker build --target runtime` de **réussir ne serait-ce qu'une fois** avec ces Dockerfiles (cf. section « Sécurité — vulnérabilités CRITICAL des images Docker » ci-dessus : « les trois Dockerfiles ne construisaient jamais réellement l'image de production… `docker build --target runtime` échouait avec 343 erreurs TS7006 »). `.env` toujours pas exclu à ce stade. |
| `b6911f9` | 17/08/2026 12:07:40 | Migration Node 20→24, sans effet sur ce point |
| `1c91455` | 17/08/2026 16:18:42 | `.dockerignore` corrigé — `.env` exclu |

Cela **resserre** la fenêtre où un build réussi via **ces Dockerfiles précisément** aurait pu embarquer `.env` réel : entre `5e184fd` (10:46) et `1c91455` (16:18) le même jour, soit environ **5h30**, puisqu'avant `5e184fd` le build échouait systématiquement (confirmé, pas supposé — c'est la même investigation déjà documentée pour le correctif Trivy). **Cela ne ferme pas la question, ça la précise** : rien n'indique qu'un build ait été *déclenché* dans cette fenêtre de 5h30, ni avant.

**Le tag cité (`degrevement-dev-api:dev-8688740c`) ne correspond à aucun commit de ce dépôt.** Recherche explicite : `git log --all --format="%H" | grep -i 8688740c` — aucun résultat, ni en préfixe de hash complet ni en fragment. Ce tag n'est référencé nulle part ailleurs dans ce dépôt non plus (recherche `8688740c` sur tout `CLAUDE.md`/`docs/`, négative). Il provient donc d'une observation externe (un scan Trivy réel sur une image déjà poussée) à laquelle je n'ai pas accès depuis cette session — impossible de dater sa construction, impossible de savoir quel `.dockerignore`/quel Dockerfile était actif au moment où elle a été produite, impossible de savoir si elle a même été construite depuis ce dépôt.

**3. Réponse honnête, pas une supposition d'innocuité.** Je ne peux pas déterminer depuis ce dépôt si une image contenant le `.env` réel a été construite et poussée vers Nexus. Trois raisons cumulatives, pas une seule :
- La question « quel pipeline construit réellement les images Nexus ? » reste ouverte depuis le chantier Trivy précédent (cf. section dédiée ci-dessus) — sans savoir *qui* construit, je ne peux pas savoir *quand* ni *avec quel `.dockerignore`*.
- Même en supposant que ce pipeline utilise ces Dockerfiles tels quels, une fenêtre réelle de ~5h30 (`5e184fd` → `1c91455`, 17/08 10:46-16:18) existait où un build aurait réussi ET embarqué `.env` — rien dans ce dépôt ne dit si un build a eu lieu pendant cette fenêtre précise.
- Le tag `dev-8688740c` cité ne se rattache à aucun repère de ce dépôt (commit, date, branche) — je ne peux ni le dater ni l'exclure de cette fenêtre.

**Action, pas une clôture prématurée.** Si ce tag (ou tout autre déjà poussé vers Nexus entre le 22/07 et le 17/08 16:18) est accessible, l'action de vérification directe est : extraire ses couches (`docker save` + inspection, ou `docker run --rm <tag> sh -c "find / -iname '.env' -exec cat {} \;"` sur une copie isolée, jamais sur un environnement partagé) et chercher un fichier `.env` réel dedans — pas quelque chose que cette session peut faire sans accès à ce registre. Si un tel `.env` est trouvé dans une image encore accessible (même retirée de production), les trois secrets qu'il contiendrait sont potentiellement déjà exposés au-delà du dépôt Git — au même titre que l'historique Git déjà traité, mais via un canal de diffusion différent (registre d'images, pas `git clone`) qui n'a pas encore été audité. Hors périmètre de cette session faute d'accès ; à transmettre à la personne pilotant le projet.

---

## Déploiement V1 — bascule MFA d'urgence + pré-enregistrement sans annuaire (18/08/2026)

**État temporaire, pas une décision d'architecture.** Contexte du jour : ni DUO ni l'annuaire AD réel ne sont joignables au moment du déploiement V1. Deux mesures appliquées ensemble, à revenir en arrière (ou reconsidérer) dès que DUO et l'annuaire redeviennent disponibles — rien de ce qui suit ne remplace les questions ouvertes déjà documentées plus haut (repli DUO, provisionnement TOTP en libre-service), qui restent posées pour une solution durable.

**1. Bascule DUO → TOTP pour tous les comptes existants.** `MfaService.demarrerChallenge()` (vérifié avant d'agir, pas supposé) n'a qu'un seul point de sélection — `Utilisateur.mfaMethode` — sans aucune logique de contournement dans `AuthController`. Un changement de données suffisait donc, exactement comme demandé : script jetable (`updateMany({ mfaMethode: "TOTP" })`), exécuté puis supprimé, aucune ligne de code applicatif touchée. Résultat réel : **7 comptes** étaient en `DUO`, tous des fixtures de test orphelines déjà documentées ailleurs dans ce fichier (`test.parametres@orange.ci` et consorts — 0 `MembreRole`, jamais utilisées pour une vraie connexion) — **aucun compte réel n'était sur DUO** au moment de la bascule. La mesure reste utile en amont : tout compte créé désormais (dont les 20 ci-dessous) est explicitement `TOTP`, jamais laissé au défaut `DUO` du schéma.

**2. Saisie manuelle de l'identifiant AD — additive, pas un remplacement.** `LdapPort.rechercher()` (capacité dev-only, OpenLDAP) ne peut être le seul chemin de pré-enregistrement : l'annuaire n'est pas joignable aujourd'hui, et l'API AD réelle communiquée (`docs`, section « API AD réelle ») n'a de toute façon jamais exposé de capacité de recherche — un seul point d'accès, l'authentification. `AnnuaireRechercheModal` porte désormais un bandeau à deux modes (« Rechercher dans l'annuaire » / « Saisir manuellement ») au lieu d'un flux unique — le mode recherche est inchangé au caractère près. Le mode manuel ne vérifie que la **forme** de l'identifiant (`identifiant@domaine`, regex `^[^\s@]+@[^\s@]+\.[^\s@]+$`), jamais l'existence — impossible à vérifier aujourd'hui, et un futur faux négatif serait pire qu'une validation permissive. Le domaine n'est **volontairement pas contraint** dans le regex : au moment de l'écriture, `orange.ci` (utilisé par les 15 identités de test) contre `orange.com` restait une question ouverte non tranchée dans ce dépôt — coder en dur le mauvais domaine aurait activement rejeté des identifiants réels valides. **`orange.com` est désormais confirmé** (18/08/2026, cf. section « API AD réelle ») comme le domaine réel — la décision de ne pas contraindre le regex reste néanmoins la bonne a posteriori : `orange.ci` continue de désigner légitimement le socle de test dev dans ce même dépôt, un regex qui aurait alors imposé `orange.com` aurait cassé la saisie manuelle pour ces 15 identités. Même regex des deux côtés (contrat Zod `preEnregistrerUtilisateurRequeteSchema`, `packages/contracts/src/admin-utilisateur.ts`, **et** validation cliente dans le composant) — défense en profondeur, pas une duplication accidentelle.

**3. Script de lot pour les utilisateurs réels (`apps/api/scripts/enrolement-lot-v1-tmp.ts`, jetable).** Reprend explicitement le mécanisme déjà utilisé pour le socle de test (secret `otplib` réel, chiffrement AES-256-GCM identique à `TotpProvider`/`chiffrerSecretTotp`, QR via le paquet `qrcode`) — appliqué à travers la nouvelle saisie manuelle. Résout chaque `directionLibelle`/`serviceLibelle`/`sousFluxLibelle` contre le référentiel réel et échoue explicitement sur un libellé inconnu (jamais de création silencieuse d'un référentiel — R11) ; résout chaque code de rôle contre `Role` avant d'écrire quoi que ce soit. **Une contrainte non négociable, respectée dès la conception** : un fichier QR **par personne** (`qr-<identifiantAd-slugifié>.png`), jamais un fichier groupé qui exposerait les vingt secrets ensemble — chaque `QRCode.toFile()` est appelé séparément, à l'intérieur de la boucle, jamais accumulé dans une structure commune. Sortie écrite dans le scratchpad de session, **jamais dans le dépôt** — aucun risque de secret committé, même par erreur (la leçon de l'incident de rotation JWT/refresh/TOTP documenté plus haut dans ce fichier s'applique directement ici).

**Vérifié en direct, mécanisme complet, avant tout usage sur les 20 personnes réelles** — deux identités jetables créées dans l'annuaire OpenLDAP de dev (`verif.sansmfa.v1@orange.ci`, rôle `INITIATEUR_DOBB`, `requiertMfa=false` ; `verif.avecmfa.v1@orange.ci`, rôle `DOBB`, `requiertMfa=true`), enrôlées par le script de lot réel (secret + QR individuel), connexion HTTP réelle de bout en bout pour les deux :
- `INITIATEUR_DOBB` : `POST /api/auth/login` → `requiresMfa:false` directement (rôle sans exigence MFA, comportement inchangé), session établie, `GET /api/auth/session` confirme `roles:["INITIATEUR_DOBB"]` et `mfaMethode:"TOTP"`.
- `DOBB` : `POST /api/auth/login` → `requiresMfa:true, methode:"TOTP", totpEnrole:true` (jamais DUO) ; code TOTP réel généré depuis le secret capturé (`otplib.authenticator.generate`) ; `POST /api/auth/mfa/verify` accepté, session établie, `GET /api/auth/session` confirme `roles:["DOBB"]`.

Les deux identités (LDAP + lignes `Utilisateur`/`MembreRole`) et les deux QR de vérification ont été supprimés après ce test — jamais laissés en base, même famille de discipline que les identités jetables e2e déjà documentée dans ce fichier.

**Portée de cette vérification, explicite** : elle prouve le **mécanisme** (saisie manuelle → enrôlement TOTP par lot → connexion MFA réelle), contre l'annuaire OpenLDAP de **dev** — pas contre l'AD réel Orange CI, injoignable depuis cet environnement (même limitation déjà documentée pour l'API AD réelle ailleurs dans ce fichier). Le comportement de connexion contre le vrai AD, une fois DUO/l'annuaire rétablis, reste à vérifier séparément quand l'accès existera.

**Reste à faire, bloqué sur la liste réelle des 20 utilisateurs** : remplir `LISTE` dans `enrolement-lot-v1-tmp.ts` avec les données réelles (nom, identifiant AD, rôle(s), direction/service/sous-flux optionnels), exécuter pour de vrai, puis reprendre la vérification en direct du point précédent sur **deux comptes réels** de ce lot (pas seulement les identités jetables ci-dessus) avant de considérer le déploiement complet. Script supprimé après cet usage, comme toujours pour cette catégorie d'outil.

---

## Outillage de déploiement production (18/08/2026)

Chantier distinct du déploiement V1 ci-dessus (celui-ci porte l'**outillage**, pas une mise en production précise) : `docker-compose.prod.yml`, `docker-compose.prod.build.yml`, `.env.prod.example`, `docs/13_Guide_Deploiement_Production.md`, deux scripts opérationnels permanents. Kubernetes explicitement hors périmètre, en attente de précisions.

**Deux fichiers compose, deux variantes, jamais les deux sur le même hôte** :
- `docker-compose.prod.yml` — images déjà construites, référencées par tag (`${DOCKER_REGISTRY}/pgd-{api,worker,web}:${IMAGE_TAG}`), aucun `build:`. Le registre réel (Nexus interne) est une dépendance externe en attente, pas une simple question ouverte de ce dépôt — cf. « Dépendances externes en attente ». **Depuis le 19/08/2026, ce fichier héberge aussi Postgres/Redis/RabbitMQ** (architecture serveur unique confirmée, cf. section dédiée ci-dessous) — ce n'est plus vrai du paragraphe qui suit pour ce fichier précis, seulement pour l'autre.
- `docker-compose.prod.build.yml` — `build:` local sur les trois Dockerfiles, aucune dépendance à un registre, mais exige le dépôt complet sur l'hôte de déploiement (`build.context: .`) et refait le build à chaque déploiement plutôt qu'un simple `docker pull`. **N'a pas reçu la même révision** (non demandé au 19/08/2026) — suppose toujours un hébergement Postgres/Redis/RabbitMQ externe non confirmé. Écart connu entre les deux fichiers, pas un oubli silencieux : à réviser de façon symétrique si `docker-compose.prod.build.yml` doit un jour servir sur la même architecture serveur unique.

`docker-compose.prod.build.yml` ne fournit ni Postgres/Redis/RabbitMQ (hébergement externe, non confirmé) ni de valeur en dur : tout secret/URL sans défaut sûr utilise `${VAR:?message}` — échec explicite au démarrage si absent, vérifié via `docker compose config`. Même discipline sur `docker-compose.prod.yml`, mais désormais y compris pour les identifiants des trois services auto-hébergés (`POSTGRES_*`/`RABBITMQ_*`, cf. section dédiée).

**⚠ `docker-compose.prod.build.yml` NE REFLÈTE PAS l'architecture serveur unique révisée le 19/08/2026 — ne jamais le prendre par erreur pour la version à jour tant qu'il n'a pas reçu la même révision que `docker-compose.prod.yml`.** Il suppose toujours Postgres/Redis/RabbitMQ hébergés en externe (hypothèse abandonnée pour `docker-compose.prod.yml`, cf. section dédiée « `docker-compose.prod.yml` — Postgres/Redis/RabbitMQ auto-hébergés » ci-dessous) — un déploiement lancé avec ce fichier tel quel échouerait à trouver ces trois services, puisque rien ne les fournit plus nulle part (ni ce fichier, ni une infrastructure externe, qui n'existe pas non plus). Écart délibéré (non demandé au moment de la révision), pas un oubli — mais un piège réel pour quiconque choisirait ce fichier en pensant qu'il est équivalent à l'autre, seulement « sans registre ».

**Bug réel trouvé et corrigé, pas cosmétique — `NEXT_PUBLIC_API_URL`.** Next.js inline cette variable dans le bundle **client** au moment de `next build` (`apps/web/lib/api.ts:116`), jamais relue à l'exécution du conteneur. `apps/web/Dockerfile` ne déclarait aucun `ARG` pour la recevoir — la définir en `environment:`/`.env.prod` n'aurait donc jamais eu d'effet, l'image retombant silencieusement sur `http://localhost:3000` codé en dur, sans erreur visible avant la première tentative de connexion d'un utilisateur réel derrière une URL publique différente. Corrigé : `ARG NEXT_PUBLIC_API_URL` + `ENV NEXT_PUBLIC_API_URL=$NEXT_PUBLIC_API_URL` avant `RUN ... build` dans `apps/web/Dockerfile`.

**Conséquence directe sur le statut de cette variable, différente selon le fichier compose** — point qui aurait pu recréer la même confusion si non documenté explicitement (`.env.prod.example`) :
- `docker-compose.prod.yml` (registre) : `NEXT_PUBLIC_API_URL` ne doit **jamais** figurer dans `.env.prod` — ce fichier ne construit rien, la variable doit être fournie au pipeline externe qui construit et pousse l'image (`docker build --build-arg`), hors de ce dépôt.
- `docker-compose.prod.build.yml` (build local) : `NEXT_PUBLIC_API_URL` **doit** figurer dans `.env.prod` — ce fichier construit réellement l'image et la transmet via `services.web.build.args`.

**Vérifié en conditions réelles, deux fois, jusqu'au bundle compilé — pas seulement que le build réussit** :
1. `docker build --build-arg NEXT_PUBLIC_API_URL=https://pgd-verif-build-arg.example.ci --target build ...` (chemin `docker-compose.prod.yml`) : `grep` dans `.next/static` confirme la présence littérale de l'URL (`let av="https://pgd-verif-build-arg.example.ci"`), `localhost:3000` totalement absent — éliminé par tree-shaking une fois la valeur définie au build, pas seulement contourné.
2. `docker compose -f docker-compose.prod.build.yml --env-file ... build web` (chemin `docker-compose.prod.build.yml`, le vrai pipeline Compose, pas un `docker build` manuel) : même résultat — la valeur transmise via `.env.prod`/`build.args` se retrouve bien dans le bundle. Images de vérification supprimées après chaque contrôle.

**Deux vrais trous `.gitignore` trouvés en rédigeant le guide, corrigés, pas seulement documentés** : `.env.prod` n'était pas exclu (seuls `.env`/`.env.local` l'étaient) — même classe de faille que l'incident `.dockerignore` déjà documenté ci-dessus. Motif générique `.env.*` + exceptions explicites (`!.env.example`, `!.env.prod.example`), vérifié dans les deux sens via `git check-ignore`. Même geste appliqué préventivement à `apps/api/scripts/*.json` (sauf `exemple-comptes.json`) une fois les deux scripts d'enrôlement écrits — un fichier de comptes réels ne doit jamais pouvoir être committé par erreur.

**`docs/13_Guide_Deploiement_Production.md`** — document opérationnel (pas un livrable BMAD), écrit à partir de l'état réel du dépôt. Deux trouvailles réelles en le rédigeant, pas de simples remarques :
- `pnpm db:seed` insère les référentiels réels **et** un jeu de données de démonstration fictif (`seedDemo()`) dans le même script, sans option pour les séparer — jamais approprié en production tel quel. Non corrigé à ce jour (à traiter avant la première mise en production réelle).
- **Aucun chemin ne permet de créer le tout premier compte `ADMIN_PGD`** : `RbacResolutionService` (Temps 2) refuse toute connexion sans `MembreRole` préexistant, et l'écran d'administration qui sert normalement à pré-enregistrer un compte est lui-même protégé par `ADMIN_PGD` — chicken-and-egg. Comblé par `bootstrap-premier-admin.ts` (ci-dessous), pas seulement constaté.

**Deux scripts opérationnels PERMANENTS** (`apps/api/scripts/`, à la différence des scripts `-tmp` jetables déjà documentés partout ailleurs dans ce fichier — ceux-ci restent dans le dépôt et sont réutilisables) :
- `bootstrap-premier-admin.ts` — crée le tout premier `ADMIN_PGD`.
- `enrolement-comptes.ts` — enrôle un lot depuis un fichier JSON (`exemple-comptes.json` documente le format, données fictives, seul fichier `.json` de ce dossier committé).

Les deux partagent `scripts/lib/enrolement.ts` : réutilise **directement** `chiffrerSecretTotp` (import depuis `apps/api/src/.../totp-secret-crypto.ts`, jamais une réimplémentation) et `preEnregistrerUtilisateurRequeteSchema` (`@pgd/contracts`, mêmes validations que l'API réelle) — une seule source de vérité pour le chiffrement et la forme de l'identifiant AD. `verifierRepertoireHorsDepot` refuse tout `--qr-dir` situé à l'intérieur du dépôt, vérifiée avant toute connexion DB (échec rapide) et testée dans les deux sens. Les deux scripts compilent et s'exécutent correctement jusqu'à l'étape base de données ; l'enrôlement réel de bout en bout (écriture + QR + connexion) reste à faire une fois de vraies données disponibles — pas simulé ici, pas prétendu vérifié au-delà de ce qui l'a réellement été.

**Incident rencontré pendant la vérification, sans rapport avec le code de ce dépôt — Docker Desktop dont le moteur a cessé de répondre.** En plein milieu du build de vérification `NEXT_PUBLIC_API_URL`, `docker images`/`docker ps`/`docker version` se sont mis à renvoyer `500 Internal Server Error` de façon systématique, y compris sur l'appel le plus trivial (`/version`) — le client Docker (CLI) répondait normalement, seul le serveur (`dockerDesktopLinuxEngine`) était en cause. Cause non déterminée (pas de piste dans ce dépôt à investiguer — poste de développement, pas une infrastructure partagée). Résolu par un redémarrage de Docker Desktop fait par la personne pilotant le projet ; les quatre conteneurs de dev (api/worker/web/openldap) sont repartis normalement ensuite, aucune perte de données côté Postgres constatée après coup. Mentionné ici uniquement comme point de repère si le symptôme (« 500 sur absolument tout appel Docker, y compris `version` ») se reproduit un jour sur ce poste — pas un correctif, juste une reconnaissance de motif.

### `docker-compose.prod.yml` — Postgres/Redis/RabbitMQ auto-hébergés (19/08/2026)

Architecture réelle confirmée par la personne pilotant le projet, remplace l'hypothèse initiale d'hébergement externe (18/08/2026, ci-dessus) : **serveur local unique** qui héberge à la fois les trois services d'infrastructure et les trois applications — pas de service managé séparé. `docker-compose.prod.yml` révisé en conséquence ; `docker-compose.prod.build.yml` volontairement laissé tel quel, non demandé (cf. écart signalé ci-dessus).

**Ajouté** : services `postgres`(16)/`redis`(7-alpine)/`rabbitmq`(3-management), mêmes images que `docker-compose.yml` (dev), volumes nommés (`pgd_prod_postgres_data`/`pgd_prod_redis_data`/`pgd_prod_rabbitmq_data`) pour la persistance — Postgres et RabbitMQ en particulier (perte inacceptable au redémarrage d'un conteneur), Redis par cohérence avec le profil dev plutôt que par nécessité stricte (CLAUDE.md, règle non négociable 4 : reflet volatil intégralement reconstructible). Aucun port publié pour Redis (aucun besoin d'accès admin distinct de `docker compose exec`) ; Postgres (5432) et la console RabbitMQ (15672) publiés en **`127.0.0.1` uniquement** — accès par tunnel SSH depuis l'hôte, jamais exposés au réseau ; le port AMQP (5672) n'est publié nulle part, les apps l'atteignent par le réseau Docker interne. `api`/`worker` gagnent un `depends_on` avec `condition: service_healthy` sur les trois nouveaux services.

**`DATABASE_URL`/`REDIS_URL`/`RABBITMQ_HOST` ne sont plus des variables fournies dans `.env.prod`** — elles l'étaient quand l'hébergement était externe (URL/hôte arbitraires à renseigner) ; désormais construites/fixées en dur vers les noms de service Docker internes (`postgres`, `redis`, `rabbitmq`), seule source de vérité. `.env.prod.example` révisé en conséquence : `POSTGRES_USER`/`POSTGRES_PASSWORD`/`POSTGRES_DB` et `RABBITMQ_USER`/`RABBITMQ_PASSWORD`/`RABBITMQ_VHOST`/`RABBITMQ_ERLANG_COOKIE` remplacent les anciens `CHANGER_hote`/`CHANGER_utilisateur` orientés hébergement externe — les identifiants RabbitMQ servent une double fonction (bootstrap du service lui-même **et** identifiants que les apps utilisent pour s'y connecter, une seule paire).

**Vérifié en conditions réelles, pas seulement via `docker compose config`** : `docker compose config` confirme l'interpolation correcte avec un `.env.prod` complet, et l'échec explicite dès la première variable manquante (`POSTGRES_USER`) avec un `.env.prod` incomplet. Au-delà de la validation statique — pile complète (`postgres`/`redis`/`rabbitmq`) démarrée pour de vrai sous un nom de projet Docker Compose jetable (`pgd-prod-verif`, isolé de la pile de dev, jamais démarrée en même temps sur les mêmes ports hôte) : les trois services atteignent l'état `healthy`, les identifiants configurés (`POSTGRES_USER`/`PASSWORD`/`DB`, `RABBITMQ_VHOST`) sont bien ceux réellement actifs (`psql` avec les identifiants exacts, `rabbitmqctl list_vhosts` confirme `/pgd`) — pas seulement acceptés au démarrage sans être vérifiés. **Persistance testée, pas supposée** : table Postgres créée avec une ligne, conteneur `postgres` redémarré (`docker restart`), donnée relue intacte après redémarrage — preuve directe que le volume nommé survit bien au cycle de vie du conteneur, exactement le risque que ce chantier devait couvrir. Pile de vérification et ses volumes détruits après coup (`down -v`), pile de dev jamais interrompue pendant l'opération (conteneurs distincts, `docker compose ps` reconfirmé identique avant/après).

**Présenté pour revue avant d'être considéré prêt à déployer, comme demandé** — ce qui précède couvre la structure et un test réel de la mécanique (services, identifiants, persistance) ; ça ne couvre pas un déploiement réel de bout en bout avec les vraies images `api`/`web`/`worker` (`DOCKER_REGISTRY` reste une dépendance externe en attente, cf. section dédiée).

**`pnpm db:migrate`/`db:seed` rejoués contre ce Postgres auto-hébergé, sur le même type d'environnement jetable (pas la pile de dev) — correction d'un oubli : cette vérification avait été faite mais seulement décrite en message de commit, jamais consignée ici en prose, contrairement à la convention établie de ce fichier.** Les deux fichiers `.env` locaux (racine + `packages/database/.env`, tous deux gitignorés, pointant vers la pile de dev) temporairement déplacés hors du dépôt pendant l'opération — même précaution déjà documentée pour la validation CI — puis restaurés. 9 migrations appliquées proprement sur base vierge. Référentiels et données de démonstration confirmés peuplés après coup, pas seulement la sortie de commande : `role=34`, `circuit=3`, `configuration_circuit=5`, `sous_flux=7`, `motif=7`, `parametre_calcul=3`, `parametre_global=7`, `calendrier_sla=1`, `kpi_definition=26`, `direction_responsabilite=17`, `compte_client=3`, `ligne=5`, `formule=8` — ces trois derniers correspondent exactement aux comptes déjà documentés ailleurs dans ce fichier pour le jeu de démo. `demande=0`/`utilisateur=0` attendus (jamais peuplés par `db:seed`, seulement par l'usage réel/les scripts d'enrôlement). Pile de vérification et ses volumes détruits après coup.

### Sauvegarde Postgres — service `backup` (19/08/2026)

Chantier en parallèle de l'attente Nexus, pas bloqué par elle (cf. « Dépendances externes en attente »). Conception présentée et validée avant construction (même discipline que d'habitude), défauts de fréquence/rétention proposés puis actés explicitement par la personne pilotant le projet — pas devinés.

**Service dédié, pas un cron hôte** (cohérent avec le reste de ce fichier compose — tout déclaré ici, rien qui dépende d'un état externe non versionné) : `docker/backup/Dockerfile` (`FROM postgres:16`, **même tag exact** que le service `postgres` de `docker-compose.prod.yml` — confirmé avant construction, pas supposé — garantit `pg_dump`/`pg_restore` strictement compatibles avec le serveur sauvegardé) + `docker/backup/backup.sh`. Boucle shell simple plutôt qu'un vrai démon cron conteneurisé (pas de gestion PID 1 supplémentaire, plus simple à lire/tester) : un cycle immédiat au démarrage du conteneur, puis un cycle toutes les `BACKUP_INTERVAL_SECONDES` secondes.

**Fréquence/rétention** : quotidien (`BACKUP_INTERVAL_SECONDES=86400`) / 7 jours (`BACKUP_RETENTION_JOURS=7`) — proposition initiale, actée telle quelle par la personne pilotant le projet le 19/08/2026. Rotation locale par âge (`find -mtime +N -delete`), pas par compte fixe de fichiers.

**Destination — locale aujourd'hui, distante branchable sans supposer le protocole.** Volume nommé `pgd_prod_backups_data` (même nuance que pour Postgres/Redis/RabbitMQ : sur le même disque physique que les autres volumes par défaut, ne protège pas seul d'une panne disque/serveur complète). `BACKUP_REMOTE_TARGET` (variable d'environnement, **vide aujourd'hui** — destination distante non encore communiquée côté infrastructure Orange, confirmé par la personne pilotant le projet) pilote une copie via `rclone` (binaire copié depuis l'image officielle `rclone/rclone`, multi-stage) — le script ne connaît jamais le protocole réel, seul `secrets/rclone.conf` (fichier externe, jamais dans ce dépôt, `.gitignore` mis à jour préventivement) le portera une fois la destination connue. Tant que `BACKUP_REMOTE_TARGET` est vide, l'étape distante est explicitement sautée et journalisée comme telle (jamais un silence qui laisserait croire à une copie distante inexistante) — le mécanisme est pleinement fonctionnel en local seul.

**Échec visible, jamais silencieux** : chaque étape critique (`mkdir`, `pg_dump`, vérification du fichier produit, rotation, copie distante) vérifie explicitement son propre code retour — pas de `set -e` implicite dont l'échec passerait inaperçu dans un cas non prévu. Tout échec écrit à la fois sur stderr (`docker logs`) **et** dans une sentinelle (`/backups/.last-status`, `FAIL <horodatage> : <raison>`). **Ajout au-delà de la demande initiale, gardé sur confirmation explicite** : un `HEALTHCHECK` (`backup.sh --healthcheck`) qui échoue si la sentinelle est absente, en échec, ou plus vieille que 2× l'intervalle attendu (cycle probablement bloqué) — rend l'échec visible dans `docker ps`/toute supervision de santé de conteneur, pas seulement dans des logs qu'il faut aller lire activement. Le conteneur ne plante pas et ne redémarre pas en boucle sur un échec : le cycle suivant réessaie normalement, l'échec reste visible entre-temps sans bloquer les sauvegardes futures.

**Vérifié dans les deux sens, en conditions réelles — rien considéré construit sans les deux, comme demandé** :
- **Sens sauvegarde** : pile jetable (`postgres`+`backup`, isolée de la pile de dev), migrée/seedée, plus une ligne marqueur insérée dans une table dédiée (valeur aléatoire unique) pour tracer précisément CE dump précis au-delà d'un simple comptage. Service `backup` démarré : cycle immédiat confirmé par les logs, sentinelle `OK`, `HEALTHCHECK` manuel et Docker natif tous deux positifs. `pg_restore --list` sur le fichier produit confirme un vrai dump exploitable : 232 entrées de TOC, bonne base, bonne version Postgres (16.14) — pas seulement "le service a démarré sans erreur".
- **Sens restauration** : fichier copié hors du conteneur, **second** Postgres jetable complètement séparé (conteneur `postgres:16` autonome, aucun lien avec la pile de vérification), `pg_restore` réel. Tous les comptages référentiels/démo restaurés identiques à la source (`role=34`, `ligne=5`, `formule=8`, etc.) **et** la ligne marqueur unique retrouvée avec sa valeur exacte — preuve que c'est bien CE backup précis qui a été restauré, pas une coïncidence de comptages similaires.
- Piles de vérification et leurs volumes détruits après coup ; fichiers de dump temporaires supprimés de l'hôte ; pile de dev jamais interrompue pendant l'opération (`docker compose ps` reconfirmé identique avant/après).

**Gotcha Git Bash rencontré et documenté** (nouvelle occurrence de la famille déjà connue) : `docker cp`/`docker exec` avec des chemins commençant par `/` se font mangler par MSYS — `MSYS_NO_PATHCONV=1` nécessaire pour tout chemin **côté conteneur** ; pour un chemin **côté hôte** passé à `docker cp`, un chemin Windows profondément imbriqué avec un composant en nom court (`ABOUBA~1`) s'est fait tronquer de façon inattendue par le CLI Docker lui-même (pas MSYS cette fois) — contourné en copiant d'abord vers un chemin hôte court avant le `docker cp` réel. Symptôme distinct du gotcha MSYS déjà documenté (`RABBITMQ_VHOST`/chemins LDAP), même famille de prudence à appliquer : ne jamais supposer qu'un chemin passé à `docker` arrivera intact sous Git Bash sur ce poste, vérifier après coup plutôt que supposer.

**Reste hors périmètre, signalé** : rotation côté distant non conçue (dépend du backend réel, beaucoup d'objets de stockage ont leur propre politique de cycle de vie, préférable à une réimplémentation côté client) ; `docker-compose.prod.build.yml` n'a pas reçu ce service (cohérent avec l'écart déjà documenté, non symétrisé) ; aucune alerte externe (e-mail, Slack) sur échec — seulement logs + `HEALTHCHECK`, cf. « Envoi d'e-mail réel » déjà documenté comme non construit pour tout ce projet.

---

## Commandes

```bash
pnpm install
pnpm db:migrate          # migrations Prisma
pnpm db:seed             # référentiels + jeu de démonstration
pnpm dev                 # api + worker + web
pnpm test                # unitaires + intégration
pnpm test:e2e
pnpm lint && pnpm typecheck && pnpm build
docker compose up
```

---

## Avant de considérer une tâche terminée

- [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build` verts
- [ ] Aucune règle métier codée en dur
- [ ] Toute opération mutative journalisée
- [ ] Tout endpoint porte un décorateur de rôle
- [ ] Contrats Zod dans `packages/contracts`, pas de type dupliqué
- [ ] Migration Prisma versionnée et rejouable sur base vierge
- [ ] Libellés et messages en français
